# 发布物料

- **标题**：你按下 Esc 的那 0.1 秒，Claude Code 内部发生了什么
  - 备选：按一下 Esc，八个任务瞬间全停、文件还没写坏——源码里藏着一棵「中止树」｜AI 干到一半被你叫停，凭什么不留烂摊子？拆 Claude Code 的中止链路
- **正文/简介**：拆 Claude Code 源码 EP04。你按一下 Esc，AI 手里七八个并行任务瞬间全停，文件却一个没写坏——这背后是三层工程设计：①「中止树」：每个子任务挂一个中止开关、层层成树，按 Esc=剪树根，信号单向往下传、连「为什么停」的原因都一起下传；②打断分两种：硬打断(Esc)整棵全砍，软打断(干活时你又敲新指令)则挨个问工具能不能停——每个工具声明 cancel(立刻丢)或 block(干完再说)，默认是 block 这个保守选项，全仓只有 Sleep 工具主动声明 cancel(它本来就在干等)；③防内存泄漏的 WeakRef：父子用弱引用相连，结束的子任务能被回收、监听器触发一次自动摘掉，外加每个开关最多 50 个监听器的保险。好软件的「丝滑」都是这么抠出来的。（系列每天更一期，源码逐文件可溯源）
- **话题标签**：#ClaudeCode #源码解读 #程序员 #AI编程 #大模型
- **封面**：assets/cover.png（竖屏 9:16，baoyu-image-gen 仿 EP01 系列风格：方块像素吉祥物本体按下 Esc 键 + 背后中止树熄灭 + 「按下 Esc 的那 0.1 秒 / 内部到底发生了什么」；生成提示词存 assets/cover-prompt.md）
- **媒体文件**：assets/ep04-esc-abort-chain.mp4（1920×1080，**211.2s，已混音 + 烧录字幕 + 全片去停顿**）｜软字幕 assets/ep04.srt（24 cues，留底/可改用）
- **建议发布时段**：2026-06-19 20:00

> 发布时段：人审打回(重做1)明确指定「明天晚8点」= 2026-06-19 20:00（今天 2026-06-18）。已写为可解析 datetime（sau `--schedule` 格式 `%Y-%m-%d %H:%M`），`build_payload` 会作为定时时间传入；与 meta.yaml `schedule` 一致。

## 发布回填（人审通过后）
- **实际发布时间**：定时 2026-06-19 20:00（2026-06-18 20:54 提交成功，到点由抖音自动发布）
- **账号**：main
- **链接**：（sau 未返回作品链接，需到点发布后到抖音作品管理人工补）
- **发布方式**：飞书「确认发布」授权 → /douyin-publish --publish → sau upload-video --schedule
- **sau 结果**：`🥳 视频发布成功` / `Douyin video upload submitted`（定时提交成功，cookie 已更新）

> 铁律：发布走 douyin-publish dry-run，经 feishu 二次确认后才真发（过审 ≠ 发布授权）。
