# 封面生成提示词 · karpathy/autoresearch 源码速读

9:16 竖屏封面，画幅比例 1536×2752（或等效 9:16）。

## 整体气质
深色工程/终端夜跑风格：深藏青底色（接近 #0a1224），叠加极淡的青蓝网格线（工程制图纸质感）。主 accent 用「磷光绿」（接近 #4ee8a4），营造「AI 整夜在终端里自动跑实验」的科技夜战氛围。风格克制、精确、有代码/终端感，不是花哨渐变卡通风。三色主导：深藏青底 + 磷光绿 accent + 浅蓝白文字。

## 视觉主体（画面中上部）
一个「深夜自动跑实验」的终端窗口视觉：
- 一个深色终端/代码窗口卡片（圆角、细边框、左上角三个小圆点表示窗口），窗口里是磷光绿等宽字的「实验日志」感的若干行，像 git 提交记录一行行往下滚。
- ★终端里的日志文字只允许出现这类「状态词 + 实验名」的英文行，**绝对不能出现任何带数字的跑分行**（严禁 Loss= / F1= / acc= / val_bpb= 等任何数值分数）。可参考如下安全示意（英文，磷光绿/浅灰）：
  `$ git log --oneline`
  `keep     baseline`
  `keep     tune learning rate`
  `discard  new activation`
  `crash    wider model (OOM)`
  `↻ running overnight...`
  其中 keep 用磷光绿、discard/crash 用暗灰，传达「留好删差」。
- 窗口一角有一弯细细的月牙（弯月的抽象线条，不要 emoji 本体），暗示「整夜、无人值守」。
- 窗口下方或右侧一个磷光绿的循环箭头 ↻ 配极小字「整晚自动循环」，以及一个数字反差标记「≈100 实验 / 晚」（磷光绿高亮数字）。
- 整体传达：人不在，机器整夜自己改代码、跑实验、留好删差。

## 标题文字（画面下半部，占比最大，承担钩子）
- 顶部小字徽章（胶囊形状，磷光绿描边，等宽字体）：「karpathy 新仓 · 源码速读」
- 主标题大字（浅蓝白色，粗体无衬线 / 等宽）：
  第一行：「让 AI 整夜」（中大字号）
  第二行：「自己做 AI 研究」（超大字号，视觉焦点，其中「AI 研究」用磷光绿高亮）
- 副标题（磷光绿或浅蓝灰，中等字号）：「源码就 3 个文件」

## 底部
可选：一条磷光绿细横线分割，下方留白即可（不需要具体来源文字）。

## 禁止事项
- 不用紫粉渐变、彩虹渐变、卡通风。
- 不用 emoji 本体 / 拟人吉祥物（本片非系列内容，不延续任何系列吉祥物设定）。
- 不用真实品牌 logo（GitHub / NVIDIA 等图形 logo 一律不画；「karpathy」作为纯文字人名徽章可以）。
- 不用红色警示色，保持磷光绿 + 浅蓝白 + 深藏青的克制配色。
- ★★终端窗口的日志里严禁任何带数字的跑分行（Loss= / F1= / acc= / 0.34 之类一律不要），只能出现 keep / discard / crash 等状态词 + 实验名的英文行，避免被误读为真实 benchmark 成绩（红线）。
- 标题文字必须清晰可读，不被视觉元素遮挡；竖版 9:16，下半部标题区留足空间。
