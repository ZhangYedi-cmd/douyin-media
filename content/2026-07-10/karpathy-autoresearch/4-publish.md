# 发布物料（4-publish）

> 飞书「过审 → 确认发布」卡在人点过审后立即读本文件拼 sau 命令。
> 字段键名严格匹配解析器（tools/feishu-bot/meta.py `_FIELD_RE`：`- **键**：值`）。
> 不写可解析的「建议发布时段」——默认立即发布，要定时由人在确认时指定。

- **标题**：karpathy 新仓 autoresearch：让 AI 整夜自己做 AI 研究，源码就三个文件
- **正文/简介**：karpathy 上个月悄悄开了个仓 autoresearch，让 AI agent 整夜自己做 AI 研究：改代码、训练、看分、留好删差，一晚跑上百个实验，你醒来就多个更强的模型。我把源码扒了一遍——整个仓就三个文件：prepare.py 当只读裁判、train.py 是 agent 唯一能改的那个、program.md 是人写的说明书。三个关键设计让它真能跑：每个实验固定 5 分钟所以直接可比、只认一个越低越好且跟词表无关的分数、每次实验一个 git 提交好就留分支差就回退。最妙的是：你不是在调 Python，你是在编程那份说明书。这套「固定预算+单一指标+单文件可改+git留痕+简单优先」的骨架，其实任何「改一版跑个分留好删差」的优化都能照搬。你敢让 AI 整晚改你的代码跑到天亮吗？评论区聊聊。
- **话题标签**：#AI编程 #karpathy #源码解读 #AIAgent #机器学习
- **封面**：assets/cover.png
- **媒体文件**：assets/karpathy-autoresearch.mp4

## 给人审的备注（非发布字段）
- 成片：1920×1080，189.3s（≈3分09秒），约9.3MB；已合成配音 + 去停顿（12段全 depause，>0.45s 内部死气复扫为0）+ 烧字幕（分句闪现，非整段常驻）+ 4.1 抽帧 7 点验证声画同步（视频 189.24s vs 音频 189.28s，factor≈1.0002，无拉伸/滞后）。
- 选题：非 claude-code-source-series（该系列15集已 2026-06-29 收官），是双赛道选题池按 score 自动取的独立深度题（backlog 2026-07-08-006，score 4.00，能诚实自动做完里的最高分——池内 4.55/4.50/4.30 三条需人工上手实测，自动 run 会踩「不臆造 benchmark」红线）。视觉沿用工程蓝图主题 + 磷光绿 accent（「整夜终端自跑」身份，区别于 spec-driven 的青蓝）；封面为品牌一致原创视觉，非系列吉祥物、非 EPxx 徽章（本条非该系列，套集号会失实）。
- 内容红线自查：全部技术结论可溯源（口播稿 2-script.md 文末逐条溯源，源=karpathy/autoresearch 公开仓，2026-07-10 经 agent-reach 读取核对）；关键设计3的 commit 表（baseline/调LR/换激活/OOM）取自 program.md 的 results.tsv 示例，屏幕已标注「示例」，无本片自测臆造数据；val_bpb 只讲「越低越好、与词表无关」的性质、不念指标名不报具体分；封面终端日志只有 keep/discard/crash 状态词、无任何跑分数字（v1 曾误生成 Loss=0.34/F1=0.89 已重生成剔除）；无违禁词/绝对化；互动引导为「去 GitHub 搜」，非站外导流念链接。
- dubbing-reviewer 闸口：R1 PASS（四项客观检查真相源同步/完整性/语速/死气 + 音画一致全过）。
- 多音字处理（本 run 已 correct-by-construction，非留待人耳）：reviewer 点名的四类多音字已逐段注音重合成——train.py 段「六百多行/命令行」行→háng、「想调哪」调→tiáo；paradigm 段「在调 Python」调→tiáo；「只读/只认/只当」只→zhǐ；「差的/更差/好留差弃」差→chà（见 tts.config.json overrides，per-段注音，paradigm「执行」的行=xíng 未 pin，无跨段误伤）。人审若有耳，可最终抽听 train.py 段「六百多行」确认 háng。

## 发布回填
- **实际发布时间**：2026-07-10 20:55（立即发布，main 账号）
- **链接**：（sau 未返回作品链接，待去创作者中心人工补）
