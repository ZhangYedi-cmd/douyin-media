# 发布物料

- **标题**：Claude Code 凭什么敢让 AI 在你电脑上跑命令？我数了下源码，五道关卡
  - 备选：AI 在你电脑上敲 rm -rf，到执行隔着几道关？拆 Claude Code 的权限管线｜连看一眼 git 状态都要你先点「信任」——Claude Code 的安全设计有多狠
- **正文/简介**：拆 Claude Code 源码 EP05。一个能跑任意命令的 AI，凭什么敢放它在你电脑上跑？因为每条命令执行前都要过一条「安检流水线」，我从源码里拆出五层：①规则匹配——你配的 allow/deny/ask 规则毫秒级查表，结果就三态、还带一条可审计的「为什么」原因链；②工具自检——每个工具有自己的安全逻辑，Bash 工具真的懂 shell 语法，能把管道、分号后面藏的危险命令揪出来；③AI 判断——全自动模式下再叫另一个 AI 当监工，判断这操作像不像你会同意的；④Hook 拦截——你自己挂的钩子能在执行前 allow/deny/ask 改写请求；⑤弹窗问你——前四关拿不准，就把决定权交还给人。最妙的细节：连读一眼 git 状态，都要你先点过「信任此目录」才执行——因为 git 能通过钩子和配置偷偷执行任意代码，看一眼仓库都可能中招。敢放权的前提，是先把篱笆扎死。（系列每天更一期，源码逐文件可溯源）
- **话题标签**：#ClaudeCode #源码解读 #AI编程 #程序员 #大模型
- **封面**：assets/cover.png（竖屏 9:16，baoyu-image-gen 仿 EP01 系列风格：方块像素吉祥物本体当「守门人」抬手拦下 rm -rf 命令 + 背后五道关卡闸门 + 「凭什么敢让 AI / 在你电脑上跑命令」；生成提示词存 assets/cover-prompt.md）
- **媒体文件**：assets/ep05-permission-pipeline.mp4（1920×1080，**181.2s，已混音 + 烧录字幕 + 全片去停顿**）

> 发布时段：本集为系列日更，无人审指定时段。默认**立即发布**（不写可解析的「建议发布时段」字段——非 datetime 文本会被原样传给 sau `--schedule` 致真发崩，见 series-production-pitfalls #5）。如需定时，由人在飞书「确认发布」时指定。

## 发布回填（人审通过后）
- **实际发布时间**： 2026-06-19 22:17
- **账号**：main
- **链接**：（sau 未返回作品链接，到抖音作品管理人工补）
- **发布方式**：飞书人审通过 → publish.run_publish（与确认发布同一引擎）→ sau upload-video（立即发布）
- **sau 结果**：✅ 成功（2026-06-19 21:09）「🥳 视频发布成功」/「Douyin video upload submitted」，cookie 已更新。
- **首发失败 → 重试成功（根因纠正）**：21:01 首次发布在「填标题/描述/话题」步 `Locator.wait_for: Timeout 10000ms` 定位「作品描述」输入框失败。**自动回填一度判为「DOM 改版」，经核实是误判**——加诊断（截图+候选元素）重发，诊断**未触发**、同一选择器一次过。真因＝发布页**偶发慢渲染**（首发那次 进上传页用了 15s vs 正常 ~3s），10s 等待在慢加载那次太紧。视频停在发布页未提交、未真发，status 当时未动。
  - 已修底层引擎（非改选择器，选择器正确）：`tools/social-auto-upload/uploader/douyin_uploader/main.py:fill_title_and_description` 把 title_input/description_editor 的 `wait_for` 超时 10s→30s，给慢渲染留余量。
  - ⚠️ 待人工确认：首发那次失败前视频已搬进 version_2 发布页（未点发布），抖音**可能自动存了一条草稿**；请到抖音「作品管理/草稿箱」看有无 EP05 残留草稿，有则删除以免重复。

> 铁律：发布走 douyin-publish dry-run，经 feishu 二次确认后才真发（过审 ≠ 发布授权）。
