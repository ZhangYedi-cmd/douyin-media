# 发布物料（4-publish）

> 飞书「过审 → 确认发布」卡在人点过审后立即读本文件拼 sau 命令。
> 字段键名严格匹配解析器（tools/feishu-bot/meta.py `_FIELD_RE`：`- **键**：值`）。
> 不写可解析的「建议发布时段」——默认立即发布，要定时由人在确认时指定。

- **标题**：AI 写代码总爱过度设计？给它装个「最懒的高级工程师」，代码反而更干净
- **正文/简介**：你让 AI 加个日期选择器，它装库、写包装组件、加样式表、还跟你聊时区；老工程师一行 input 就完事。开源项目 ponytail 就是把这个「最懒高级工程师」塞进你的 AI agent。核心是一套七级阶梯：需要存在吗→库里已有吗→标准库→平台原生→已装依赖→一行→实在不行才写最少代码；但校验、错误处理、安全、可访问性四条底线一个都不许砍。它在真实 Claude Code 会话上自测：代码量-54%、token-22%、成本-20%、耗时-27%、安全100%不掉；而且早期宣传的80-94%被 issue 指出是裸模型基线灌水，项目方自己改回了公允的54%——这种敢砍自己数字的 benchmark 反而更可信。就算不装工具，把这七级阶梯抄进你给 AI 的规则里也管用。你被 AI「热心过度」坑过最离谱的一次是啥？
- **话题标签**：#AI编程 #ClaudeCode #AIagent #代码质量 #程序员日常
- **封面**：assets/cover.png
- **媒体文件**：assets/ponytail-lazy-senior-dev.mp4

## 给人审的备注（非发布字段）
- 成片：1920×1080，219.8s（≈3分40秒），约11MB；已合成配音 + 对 ladder/2·3 两段列表做定向去停顿 + 烧入分句字幕 + 4.1 抽帧 5 点验证声画同步（video 219.76s / audio 219.79s，无拉伸、无滞后）。
- 选题决策：backlog 顶部 next_up 为空，按 score 取。池内 top-3（Fable5 实测 4.55 / 三旗舰对打 4.50 / GLM-5 NIM 白嫖实测 4.30）全是「亲手跑一天/对打/测真实账单」的实证题，无人值守定时 agent 无法如实产出（硬做=臆造 benchmark，踩内容红线），按红线优先跳过，取最高分「可溯源解读题」ponytail（3.90, depth）。此现象建议治理线关注（评分引擎系统性高估无人线做不了的实测题），本轮不改 brain。
- 内容红线自查：全部数字来自 ponytail 项目自测并在片中明确标注「项目自己在真实 Claude Code 会话上测」，非我方实测、非臆造；对「80-94%→54%」的数字回撤如实呈现（正面案例）；边界项「某些模型上会反过来」如实保留；无违禁词/绝对化；互动为评论区提问，无站外导流；口播不念外链（结尾只说 GitHub 可查）。溯源清单见 2-script.md 文末（全一手：README 的 How it works / Numbers / Older single-shot 折叠段 / issue #126）。
- dubbing-reviewer 闸口：R1 直接 PASS（15 段「死气」经原始 silencedetect 波形对位证实全为句读/列表自然韵律，非句中死气；语速/音画/多音字均过）。采纳其非阻断建议，对最拖的 ladder/2·3 两段列表做定向 depause（省 3.07s / 1.67s）提抖音节奏。
- 未经核实项（无人耳/whisper 通道）：英文词 token/agent/ponytail/React/FastAPI/Claude Code 的中英连读、多音字「一行(háng)」「重写(chóng)」的读音，脚本层无法凭听感 100% 坐实，reviewer 判常规读对但建议人耳复核；FastAPI 已加 normalize 防被 API 规则拆坏。人审若听到读错，改法：tts.config.json 对应段加逐段 pronunciation override（不动全局字典）。
- 视觉：暖炭黑底 + 单热橙主题（贴 ponytail 项目气质 + 账号暗底橙 house style）；封面为 baoyu-image-gen 原创竖版 9:16（1536×2752），ponytail 扎马尾工程师角色 + 坏代码被划掉→一行橙光，标题「最好的代码是不写的代码」承担钩子，非视频截帧、非系列吉祥物（本片非 claude-code-source-series）。

## 发布回填
- **实际发布时间**：2026-07-13 21:04（立即发布；飞书确认发布卡授权后 sau 真发，输出「视频发布成功」+ submitted）
- **账号**：main
- **链接**：sau 未返回作品链接，待创作者中心人工补
