# 内容条目元数据 / 状态机
slug:                      claude # 短标识，同目录名
title:                     你让 Claude 写的每段字，都被悄悄打上了「暗号」——文本水印机制拆解 # 工作标题
type:                      kouban # kouban(口播) | tuwen(图文)
pillar:                    depth # depth(技术深度) | traffic(流量型)
status: drafting            # 合法状态与迁移见 `media flip --help`（唯一定义 = tools/console/packages/core/src/state.ts）
                           # 状态只经 media 命令翻转，不手编
source:                    2026-08-19-001 # 选题来源（哪个信息源/热点）

schedule:                  # 计划发布时间
publish_url:               # 发布后回填

timestamps:
  ideated: 2026-08-19
  drafting: 2026-08-26 11:44
  review:
  approved:
  published:
  retro_done:
