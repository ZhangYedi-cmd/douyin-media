Vertical 9:16 short-video cover image for a tech series. The FIRST reference image is EP01's cover — treat its mascot as the SAME established series character and reuse its identity. Match EP01's visual style EXACTLY: same warm dark near-black background, same SINGLE hot-orange accent color (no other bright colors), same faint low-opacity monospace source-code filling the background, same FLAT PIXEL-ART (blocky, low-res, square-pixel) mascot rendering, same hand-drawn orange spark/burst accents, same bold confident layout and typography weight.

CENTER (main visual anchor) — IDENTITY LOCK: reuse the EXACT SAME mascot from EP01's cover (the reference image): a SQUARE / BLOCKY flat pixel-art orange creature with a rectangular box-shaped body, two simple solid-black rectangular pixel eyes, and short stubby blocky legs. Do NOT redesign it, do NOT make it round/fluffy/animal-like, do NOT create a new similar-looking character (NO hamster, NO cat, NO robot). Keep the same blocky proportions, same eye style, same flat pixel rendering as EP01. ONLY change the scene/pose: this ONE mascot is now SPRINTING / DASHING at top speed to the right, leaning forward in a determined run, with flat blocky pixel-art SPEED LINES / motion streaks trailing behind it and a small hand-drawn orange lightning bolt ⚡ near it — it has chosen the FAST LANE.

CORE IDEA (must read instantly): "skip the loading, take the fast path." The mascot dashes along a short glowing HOT-ORANGE EXPRESS LANE / arrow track that shoots straight off to the right (the fast path). RIGHT BEHIND the running mascot, render a STACK / PILE of UNOPENED blocky pixel-art crates or boxes (the heavy full bundle of modules) sitting UNTOUCHED and slightly greyed-down — the mascot is leaving them behind, NOT carrying them. Near the sprinting mascot place a small flat pixel-art tag / chip reading "0ms" (or a tiny stopwatch icon) in the hot-orange accent. The visual should instantly read as: "one little AI zooming down an express lane while the big pile of stuff it could have loaded just sits there unused." Everything flat, blocky, charming pixel-art — NOT realistic, NOT 3D, NOT round.

TOP: a rounded pill-shaped badge with a thin hot-orange outline stroke (transparent fill), centered, containing the text:  Claude Code 源码解读 · EP11

BOTTOM: large BOLD sans-serif title, two lines, very high contrast, crisp and perfectly legible (render all characters accurately):
  Line 1 (pure white, include the literal command, monospace-ish but bold):  claude --version
  Line 2:  凭什么 0 毫秒出结果   ← render the whole line in the hot-orange accent color, extra bold

Composition: generous breathing room; the SPRINTING mascot centered in the upper-middle riding the orange express lane to the right; the pile of unopened module crates just behind/below-left of it, greyed and static; title anchored at the bottom; badge at top. Very dark warm-black canvas with faint blurred monospace source code (async function main / if args --version / return / await import(...) / startKeychainPrefetch / fast-path) behind everything at low opacity. NO realistic humans. Cinematic, editorial, developer aesthetic. All text (Latin command + Chinese title) must be spelled and rendered correctly and sharply. Series consistency with EP01 is the top priority: same character, same palette, same mood.
