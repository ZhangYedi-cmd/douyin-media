# 封面生成提示词 · Gemini CLI 源码速读（同一道题两家怎么答）

9:16 竖屏封面，画幅比例 1536×2752（或等效 9:16）。

## 整体气质
深色工程/制图夜战风：深藏青底色（接近 #0a1224），叠加极淡的蓝色网格线（工程制图纸质感）。这是一期「对比拆解」——同一道题（做终端里的 AI 编码 agent），一家闭源、一家开源。用**双色对照**讲这个故事：暖橙（接近 #ff9a5c）代表闭源的 Claude Code，靛紫（接近 #9a86ff，Gemini 品牌色）代表开源的 Gemini CLI。风格克制、精确、有代码/终端感，不是花哨渐变卡通风。三色主导：深藏青底 + 靛紫/暖橙双 accent + 浅蓝白文字。

## 视觉主体（画面中上部）
一个「一道题，两份答案」的左右对照视觉：
- 左侧一块**上了锁的黑盒**：深色卡片，表面是一坨压缩混淆、看不懂的代码（挤成一行行没有换行的乱字符，暖橙微光），卡片正中一个抽象的锁形图标（线条，不要 emoji 本体），顶上暖橙小字标签「Claude Code · 闭源」。传达"只能对着混淆包猜"。
- 右侧一块**敞开的源码窗口**：深色终端/代码窗口卡片（圆角、细边框、左上角三个小圆点），窗口里是靛紫等宽字、排布整齐、有缩进层次的源码行（像真正能读的代码，安全示意见下），顶上靛紫小字标签「Gemini CLI · Apache 开源」。传达"答案摊开能读能改"。
- 中间一个大大的靛紫「VS」或一道对照分割线。
- ★两块卡片里的代码只允许出现"结构性/示意性"的英文源码行，**绝对不能出现任何带数字的跑分行**（严禁 Loss= / F1= / acc= / benchmark 分数等任何数值成绩，避免被误读为真实评测——红线）。右侧敞开窗口可参考如下安全示意（英文，靛紫/浅灰等宽字）：
  `class Turn {`
  `  MAX_TURNS = 100`
  `  shouldConfirmExecute()`
  `  packages/core · packages/cli`
  `}`
  左侧黑盒是一行行挤在一起、无缩进、看不清的混淆字符（暖橙暗光），刻意"读不懂"。

## 标题文字（画面下半部，占比最大，承担钩子）
- 顶部小字徽章（胶囊形状，靛紫描边，等宽字体）：「源码速读 · 同一道题两家怎么答」
- 主标题大字（浅蓝白色，粗体无衬线 / 等宽）：
  第一行：「拆完 Claude Code」（中大字号，"Claude Code" 暖橙高亮）
  第二行：「再拆 Gemini CLI」（超大字号，视觉焦点，"Gemini CLI" 靛紫高亮）
- 副标题（浅蓝灰，中等字号）：「Google 的开源答案，抄了什么、改了什么」

## 底部
可选：一条靛紫细横线分割，下方留白即可（不需要具体来源文字）。

## 禁止事项
- 不用紫粉渐变、彩虹渐变、卡通风（靛紫仅作克制 accent，不做大面积粉紫渐变）。
- 不用 emoji 本体 / 拟人吉祥物（本片非 claude-code-source-series 系列内容，不延续任何系列吉祥物设定，也不套 EPxx 集号徽章——会假冒不存在的集号）。
- 不用真实品牌 logo（Google / Gemini / Anthropic / GitHub 图形 logo 一律不画；「Claude Code」「Gemini CLI」作为纯文字名可以）。
- 不用红色警示色，保持靛紫 + 暖橙 + 浅蓝白 + 深藏青的克制配色。
- ★★两块代码卡片里严禁任何带数字的跑分行（Loss= / F1= / 0.89 之类一律不要），只能出现类名/方法名/路径这类结构性源码示意，避免被误读为真实 benchmark（红线）。
- 标题文字必须清晰可读，不被视觉元素遮挡；竖版 9:16，下半部标题区留足空间。
