# 第 07 课读者诊断报告（diagnosis.md）

## 1. 硬禁令命中项排查
- 命中项：第 563-566 行存在 `**` 加粗语法（嵌套代码块导致外层截断并暴露给检查脚本）。
- 整改方案：在所有提示词模板和正文中彻底移除 `**` 加粗符号，使用纯文本层级结构，代码围栏采用 4 个反引号 ```` 包裹含三反引号的提示词块。

## 2. 篇幅与结构体检
- 篇幅：纯中文字数 7093 字，总字符数 17161，完美落在 6000 至 8000 字区间。
- 结构完整性：
  - 1.1 与 1.2：完整覆盖生成与审查解耦、YAML 契约、三大出口状态机。
  - 2.1、2.2、2.3：详尽拆解音频死气、多音字漏注、同段两读冲突三大实战套路与 L16/L17/L18 真实教训。
  - 3.1 与 3.2：通过边际收益表解释 3 轮熔断数学与工程依据，给出 drafting_blocked 降级与飞书工单设计。
  - 4.1 与 4.2：深入分析无头录屏时间膨胀与字幕漏烧（L15），提供 ffmpeg 抽帧与时长比率验证逻辑。
  - 5.1、5.2、5.3：提供完整、开箱即用的三大提示词模板（信封启动、历史迭代、熔断工单）。
  - 6.1 与 6.2：总结与包含两个真实冲突场景的课后实操。

## 3. 语言与温度检视
- 句法优化：有 8 处超过 80 字的单句，主要是逗号硬拉长句。在终稿中拆分为两到三句，提升阅读呼吸感。
- 术语白话化：将正文中残留的「自愈闭环」「闭环路径」替换为「自愈回路」「修复回路」「流转路径」。
- 卡点拦截验证：每节均包含至少 1 处真实条件式拦截（共 6 处），例如 depause 原地写入截断、删段后编号错位、同段两读文案重构、录屏 subs 参数丢失等。
- 难度承认与松话：在文案三源同步处承认操作繁琐并给降维路径，在总结处保留「反正能跑通的系统，骨子里都是这套克制的逻辑」松话。

## 4. 终审行动清单
1. 修复提示词代码块围栏层级，消除所有 `**` 标记。
2. 拆解 8 处超长单句，消除逗号硬拉。
3. 替换多余的行业术语，使行文更加平实自然。
4. 运行 check_style.py 达到硬禁令 0 处命中，并输出 final.md 与 polish-report.md。
