# 润色报告（CLI vs MCP 架构抉择）

## 1. 诊断问题逐条处理

| 诊断项 | 涉及位置 | 原文/问题表现 | 修改后 | 修改理由 |
|---|---|---|---|---|
| 硬禁令翻案句 | 4 节开头（原第 506 行） | 强调 CLI 的工程优势，并不意味着要全盘否定 MCP。技术选型从来不是非黑即白的站队，而是根据具体业务场景的特征寻找最优解。 | 选择工具形态需要根据具体业务场景的特征寻找最优解。 | 命中硬禁令「不是……而是……」翻案句与模板套话，直接给核心结论。 |
| 逻辑断裂 | 1.2 节 Prompt Caching 说明 | 提到 Prompt Caching 缓解费用但未说明多轮击穿原因 | 补充说明：多轮对话中 tool_result 频繁追加导致缓存前缀匹配不稳定，一旦前置上下文发生动态变化，缓存极易击穿，退化为全额计费。 | 补齐缓存击穿的真实工程原因。 |
| 逗号长句 | 1 节开头（原第 5 行） | 探索经历过多轮演进。从早期的函数调用（Function Calling），到后来的插件体系，再到由 Anthropic 发起并开源的 MCP…… | 早期采用简单的函数调用（Function Calling）。随后出现了各类应用专属的插件体系。后来 Anthropic 发起并开源了 MCP…… | 拆分成短句，去除长定语铺陈。 |
| 逗号长句 | 3.2 节（原第 446 行） | 如果你在这一步报错了，多半是 MCP Server 内部的异常没有被捕获，直接往 stdout 输出了非 JSON 文本，导致客户端的 JSON-RPC 解析器崩溃。 | 如果你在这一步报错了，多半是 MCP Server 内部抛了异常。异常直接写到了 stdout，把客户端的 JSON-RPC 解析器搞崩溃了。 | 拆成两句，保持平实话。 |
| 逗号长句 | 3.4 节（原第 495 行） | 如果一个 MCP 工具名为 execute_sql，每次调用弹出的确认框里只是一串复杂的 JSON 文本，人类审批者很难在一秒钟内肉眼分辨出该 SQL 是只读查询还是删库操作。 | 如果一个 MCP 工具名叫 execute_sql，弹出的确认框里往往只是一串深层嵌套的 JSON。审批人在几秒钟内很难肉眼看清这是只读查询还是删库命令。 | 拆句并口语化表达。 |
| 黑话替换 | 3.2 节、4.2 节、2.3 节 | 排错链路、闭环、复用的基础设施能力 | 排查步骤、确认、在现有自动化流水线里直接跑的系统工具 | 替换为日常工程师白话。 |
| 总结模板感 | 5.4 节 | 回顾 MCP 与 CLI 的技术演进路径，我们可以得出三个确定的结论：第一……第二……第三…… | 在实际构建系统时，不必把所有接口都往最新的协议上生搬硬套。让 MCP 留在只读、跨生态的数据接入层，让 CLI 统领业务执行与状态流转…… | 去除“三大结论”说教排比，落到平实收束。 |

## 2. 终检结果

- 检查脚本：`python3 scripts/check_style.py writing/cli-vs-mcp-architecture/final.md`
- 硬禁令命中：0 处（清零）
- 警告项：0 处（清零）
- 纯汉字字数：8369 字
- 全文总字符数：21996 字符
