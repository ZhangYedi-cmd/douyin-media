# 治理执行记录 · account-audit · 2026-09-02

## 任务 / 触发 / 目标
- 任务：account-audit（元层：治理线自审）
- 触发：harness-dispatcher 周期派活（periodic:30d，距上次 2026-07-08 已 56 天）
- 目标对象：`harness/logs/index.jsonl` 全量运行账本（41 条历史记录）+ 全仓 `meta.yaml` 状态

## 现状（查了什么、用什么查）
- 数据来源：读取 `harness/logs/index.jsonl`，按任务分组统计执行次数、成功率、产出提议数与采纳率（口径：落盘 ≥7 天且有提议的报告）；扫描 `content/*/meta.yaml` 统计发布超 7 天且非 `retro_done` 的复盘积压数。

## 发现（逐条，每条带证据）

### 各任务运行与采纳指标汇总

| 任务名称 | 执行总次数 | 成功率 | 提议总数 (findings) | 有效报告数 (≥7d) | 采纳次数 (applied) | 采纳率 | 样本状态（熄火线 ≥5 条） |
|---|---|---|---|---|---|---|---|
| `retro` | 27 | 100.0% | 96 | 27 | 12 | 44.4% | ✅ 样本充足 (≥5) |
| `ideate` | 7 | 100.0% | 44 | 6 | 0 | 0.0% | ✅ 样本充足 (参数锁定) |
| `benchmark-refresher` | 3 | 100.0% | 19 | 2 | 1 | 50.0% | ⚠ 样本不足 (<5) |
| `backlog-gardener` | 3 | 100.0% | 5 | 2 | 0 | 0.0% | ⚠ 样本不足 (<5) |
| `account-audit` | 1 | 100.0% | 1 | 1 | 0 | 0.0% | ⚠ 样本不足 (<5) |

### 核心诊断项
1. **复盘严重逾期积压报警（超限）**：
   - 现状：`content/*/meta.yaml` 中状态为 `published` 且发布已超过 7 天但未标记 `retro_done` 的条目共有 **17 条**（含 EP02~EP15 历史系列 13 条、ponytail/cc-safety-net/kimi-k3/grok 4 条）。
   - 判定：越过 `tasks.md` 钉死的「逾期积压 >3 条」报警线，大环存在严重欠账。
2. **调参判定（熄火线生效）**：
   - `benchmark-refresher`（3 次）、`backlog-gardener`（3 次）有效样本均 <5 条，触碰熄火线，**本轮保持原参数（30d / 7d），不执行自动调参**。
   - `ideate`（2d）与 `retro` 窗口按任务卡硬性锁定。

## 变更提议（给人审勾选；不自动改）
- [ ] **提议 1（紧急接入/人工补收）**：复盘逾期积压达 17 条，严重阻断「发布→复盘→大脑反哺」闭环，建议优先接入 `retro-debt-collector` 任务，或人工执行 `/douyin-retro` 进行 debt 模式快照补收 —— 依据：积压 17 条 > 3 条报警线 —— 可逆性：高。
- [ ] **提议 2（账本维护）**：EP02~EP15 早期系列已完结且归档，建议人审确认是否将已无有效分析价值的早期系列条目批量标记为 `retro_done`，避免噪音淹没近期未复盘的 4 条真内容 —— 依据：17 条积压中 13 条为 6 月份早期系列 —— 可逆性：高。

## 盲区 / 未决（机器查不了，需人工）
- 创作者中心后台数据采集（sau cookie 有效性与免扫码登录）需在实际拉取时验证。

## 落地记录（人审后回填）
- 批准：待人审 · 审核人：<> · 时间：<> · 已应用到：无
