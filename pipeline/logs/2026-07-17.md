# 2026-07-17 工作日 run

**结果**：✅ 当日 1 条做到出审并已过审 —— `content/2026-07-17/kimi-k3-benchmark-check/`（流量题，成片 3:44）。
20:58 推审核卡 → **21:01 人点「通过」**，status=`approved`，server 已发「确认发布」卡。**本 run 停在出审，未发布**（发布铁律：通过≠真发，需人二次授权）。

## 1. 取题
- `content/_research/research-2026-07-17.md` 当日已有报告（治理线 ideate 已跑）→ **过渡期兜底未触发**，不重复跑 ideate。
- backlog `next_up` 为 null → 按 score 取 `status:idea` 最高 = **`2026-07-17-001`（4.15，唯一最高，无并列）**。
- 旁证同向：近 7 条取题（07-08~07-16）**全是 depth**，traffic 已欠账，与 positioning 6:4 滚动配比一致；本条亦是本批唯一 `urgency: today`（旗舰热度窗 2–3 天）。
- promote：idea→picked，回填 `content_path`，建目录 + `meta.yaml`(source 指针) + `1-brief.md`。

## 2. 创作
- **素材亲验（没只信选题报告）**：抓 `kimi.com/blog/kimi-k3` 与 `simonwillison.net/2026/Jul/16/kimi-k3/`。**关键发现：官方 Full Benchmark Table 是文本表（不是图）**，35 行可逐行解析 → 「逐行读给你听」这个选题角度**真的能做实**，不是噱头。
- **自算对账**：写脚本按官方表逐行计数 —— K3 第一或并列第一 **8/35**（含 ZeroBench 平手，严格第一 7）；对 Fable 5 赢或平 **13/35**；对 Sol **20/34**。最狠一行：`Kimi Code Bench 2.0`（Moonshot 自家内部榜）K3 72.9 vs Fable 5 76.9 —— **自家主场输给对手还照印**。
- **★ 揪出选题报告一处错数**：报告写「AA-Briefcase 第二 1527」，但官方表 1548、AA 自述 1547 —— **三方打架 → 全片弃用该数**，未传播下去。
- 四件套：新建 `ledger`(对账台) 主题（琥珀=审计荧光笔身份色，与 cc-safety-net 的 blueprint 区分）→ 6 章 17 段 → tts-dub 合成（**先单段探余额，无 1008**）→ dubbing-reviewer → 录屏。
- 封面：baoyu-image-gen 竖版 9:16（1536×2752），`--ref` 复用 EP01 吉祥物本体。

## 3. 配音质检（dubbing-reviewer，2 轮未触 3 轮上限）
- **R1 FAIL**：arena/2(0.93s)、footnote/3(0.73s) 破折号 `——` 死气。
- 处置：① 全片扫出 **16 段都有 >0.45s 死气** → 全段 depause；② 补 R1 点名的 normalize 缺口（`Bench 2.0→二点零`、`3T→三 T`）**删 mp3 重合成**；③ 补 `Footnote.tsx` 少的「也」。
- **R2 PASS**：三源 15 段逐字一致、tally 前移一位无错位、13 条注音无死键无同段冲突、死气 0。

## 4. 出审
- 终检闸 A–G 全过（A/B/F 在最终 mp4 上验；抽帧 5 处音画对齐；factor≈1.000 无拉伸）。
- `4-publish.md` 5 字段**用 `tools/feishu-bot/meta.py` 实跑解析验证**通过（不靠肉眼比对）。
- 20:58 推卡（server pid 31712 单进程）→ 21:01 人点通过 → `approved`。

## 5. 本轮踩坑与判断（★ 值得沉淀）
1. **`rec/depause.mjs` 不能原地写**（`in == out`）：ffmpeg 打开输出即截断输入，**静默失败**——批量跑完 17 段全部「无输出」、时长一点没变，复扫才发现死气仍在。**改走「写临时文件再 move」**。教训类型同 L2（真相源/产物覆盖），建议登记 `pipeline/lessons.md`。
   - **反面教材价值**：若当时只信「命令 exit 0」不复扫，会带着 64 处死气出审。**复扫救了这一条**。
2. **控时长（主动判断，非被要求）**：初剪配音 **4:20**，超 `brain/style-guide.md`「深度题 2–3min」上限，且 `benchmarks.md` 连续 8 轮同向「完播率是账号系统短板、漏在中后段」。→ 删两段（K3 铺垫 / 鹈鹕 SVG）+ 收紧 4 段，**4:20 → 3:44**。删段导致 tally 章整体前移一位，**`tts.config.json` 注音 override 键随之重排**（不重排就会注错段）；另「自行车(xíng)」正好随鹈鹕段删除，全片「行」自此均为 háng。
3. **多音字同段冲突**：`ending/2` 原稿同时有「就得打折(děi)」与「诚实得多(轻声 de)」→ 逐段注音无解。**改文案「诚实得多→诚实很多」解冲突**，三处真相源（2-script.md / narrations.ts / Ending.tsx）同步。
4. **`npm run synthesize-audio` 是雷**（R1 指出）：它走 `scripts/synthesize-audio.sh` → 坏掉的 mmx CLI，**完全不读 `tts.config.json`**（丢全部注音 + 换错音色）。本 run 全程只走 `tts-dub/scripts/synthesize.mjs`，**一次都没碰 npm 脚本**。与 `2-create.md` 步骤 3 已有的告诫一致。

## 6. 如实报的未尽项（已随卡与 4-publish.md 明示人审）
- **时长 3:44 仍超 3min 上限约 44s**：内容为 35 行逐行对账，密度所限；参照 cc-safety-net(3:49) 已过审先例交人裁。人审在知情下点的通过。
- **封面装饰表格 ✕/✓ 语义矛盾**（高亮「赢」行同时有红 ✕ 绿 ✓）：抽象纹理非真实数据，缩略图不可辨，未重生成。
- **normalize 口径未统一**：`K2.6`/`Bench 2.0` 已中文化，`GPT 5.6`/`Opus 4.8` 未做（R2 判非阻断）。**AI 无法耳听验证**，标给人。

## 7. 阻塞
无。TTS 余额正常（单段探测无 1008），飞书 server 在线，无停产事件 → 未触发「阻塞即上报」。
