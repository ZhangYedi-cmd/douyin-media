# 工程重构与流水线恢复待办（2026-07-08 审计结论）

> 来源：2026-07-08 全仓架构审计对话。原则：先地基（git）、再止血（恢复生产）、后重组（目录）、最后瘦身（文档）。
> 每项标注 owner（人/AI）与验证方式。执行时逐项勾销。

## 阶段 0 · 人要亲手做的（不阻塞其他阶段，但尽快）

- [ ] **EP04 发布确认**：去创作者中心确认 EP04 是否真已发布（meta.yaml 停在 scheduled，2026-06-19 20:00 定时提交后无人回填）。确认后由 AI 统一回填 meta.yaml + backlog(2026-06-10-004) + dashboard 三处。
- [ ] **EP13/14/15 作品链接**：从创作者中心拿链接，交 AI 回填各自 4-publish.md 与 dashboard。

## 阶段 1 · 地基：版本控制（AI，半小时）

- [x] 写 `.gitignore`：`node_modules/`、`content/*/*/build/`、`content/*/*/demo/`、`*.mp4`、`*.mp3`、`tools/social-auto-upload/`、venv/缓存类。（2026-07-08）
- [x] `git init` + 首次提交（brain / pipeline / automation / content 文档层 / .claude）。（2026-07-08，commit 2bfd831）
- 验证：`git status` 干净；`.git` 体积在几十 MB 内（重资产未入库）。✓ `.git` 30M。
- 附加：为 Spec Coding 选题建了内容分支 `content/spec-driven-development`（首个"选题分支"实践，后续内容条目是否都开分支待复盘后定规则）。

## 阶段 2 · 止血：恢复生产 + 选题池消化（AI 改，人审 expired 清单）

> 背景：daily-run 自 06-30 起连续空跑（清系列模式已收官无题可取）；选题池 9 条 idea 全是 06-14 入池、24 天无人消化、时效题已过期；ideate 自 06-14 后再没跑过。模式切换已获人口头授权（2026-07-08）。

- [x] **daily-run 切回双赛道**：`automation/daily-run.md`「当前模式」+ 步骤 2 + 参数节已改（选题驱动、6:4、调研间隔≥2 天先 ideate）；定时任务 prompt `~/.claude/scheduled-tasks/douyin-ai/SKILL.md` 已同步。（2026-07-08）
- [x] **backlog 加 `next_up` 指针**：`backlog.yaml` 顶部已加 `next_up: null` + 语义注释；daily-run/douyin-ai 取题逻辑同步为"next_up 优先 → 按 score 取最高（同分先 depth，expired 不取）"。（2026-07-08）
- [x] **expiry sweep 机制**：已钉进 douyin-ideate Step 2.0（timeliness≥4 超 7 天 / today 超 2 天 → expired；timeliness≤2 长留；expired 留池做去重；清扫结果写报告）。（2026-07-08）
- [x] **现存 9 条 idea 出「留/杀」清单**：提案在 `content/_backlog/expire-proposal-2026-07-08.md`——建议杀 4（001/002/005/009）留 5，其中 006 是规则例外需人拍板。**待人审后翻状态**。（2026-07-08）
- [x] **跑一次 douyin-ideate 补新水**：清单人审（杀4留5）落定后已跑——9 条新候选入池（id 2026-07-08-002~010），队首 Fable 5 实测 4.55S，报告 `content/_research/research-2026-07-08.md`。★ 7/9 GPT-5.6 发布日两条已备。（2026-07-08）
- [x] **停产/异常事件飞书通知**：daily-run 加「阻塞即上报」铁律节（feishu-notify 能力三），douyin-ai prompt 同步；已实发一条测试消息验证通道 OK。（2026-07-08）

## 阶段 3 · 目录重组（AI，纯搬运 + 全仓改引用，需在阶段 1 之后）

- [x] **automation/ 解体**：`daily-run.md` + `logs/` 已迁入 `pipeline/`；`harness/` 已提顶层；`schedule.md` 残值（触发方式、治理线独立调度指针）并入 daily-run 参数节后删除。automation/ 目录已消灭。（2026-07-08）
- [x] **tts.config.json → brain/**：已迁；`pipeline/2-create.md` 口径同步修正为实际用法——build 内 config 从账号级模板 `brain/tts.config.json` 复制，沉淀性读法规则回写模板。（2026-07-08）
- [x] **`content/_research/作品列表.xlsx` → `harness/data/`**。（2026-07-08）
- [x] **tools/ 分家**：阶段 1 已顺手完成——`social-auto-upload` 整目录 gitignore（留在磁盘供 sau CLI 用，不做 submodule：路径被 douyin-publish 依赖，submodule 徒增脆性）；`feishu-bot` 源码已入库、venv/config.yaml(含 secret) 被其自带 .gitignore 排除。（2026-07-08）
- [x] **全仓改引用**：README/dashboard/brain/模板/harness 文档/4 个 skill/PRD/定时任务 prompt/Claude memory 全改；历史快照（pipeline/logs 旧日志、docs/2026-06-14 纪要）按约定不改。顺带：`.claude/scheduled_tasks.lock` 加 gitignore 并移出跟踪。（2026-07-08）
- 验证：✓ 活文档 grep 旧路径零命中；✓ 取题逻辑干跑通过（next_up 合法、9 条可取、按分命中 4.25/depth）；✓ 飞书出站通道实测。

## 阶段 4 · 文档瘦身与防漂移（AI 改写，内容变化大，单独人审）

- [x] **dashboard.md 降级为派生视图**：重写完成——快照式状态表 + 待人确认区（EP04/open-weight-5/链接）+ 池子计数 + 数据汇总；系列叙事删除（过程在 pipeline/logs/）。（2026-07-08）
- [x] **pipeline/*.md 瘦身**：1-ideate 重写为纯阶段契约；教训收编 `pipeline/lessons.md`（L1~L14，SOP 内改 Lx 指针）；4-publish/daily-run 与终检闸 G 的三处重复收敛为单一出处+引用；L9 配音余额探测从 memory 转正为仓内规则；「SOP=契约、细节住 skill」等约定写进 CLAUDE.md 新增「文档架构约定」节。**注**：2-create 的操作命令未强行下沉——账号级创作胶水（封面 ref、build 落位）没有对应账号级 skill 可承接，硬拆需新造 skill 属过度设计，此决定留档；若未来创作链路提为 skill 再下沉。（2026-07-08）
- [x] **`content/_template/` 改成真实 schema**：新增 `_template/README.md`（每文件谁产/何时/必产否 + build/demo 工作区约定）；1-brief 里过时的旧版 5 维打分行改为指向 backlog 6 维分。文件未砍（1-brief/3-review/5-retro 各有 4-5 处实际使用）。（2026-07-08）
- [x] **docs/ 定位只读 ADR 存档**：新增 `docs/README.md` 存档声明；harness README 的 TODO 表说明从规划文档收编回本体（含 backlog-gardener 与 ideate 清扫的职责切分注）。（2026-07-08）
- [x] **状态记账单一 owner**：douyin-publish Step 5 明确为唯一记账点（meta/backlog/dashboard 一次翻齐），`pipeline/4-publish.md` 新增「发布收尾」节，CLAUDE.md 约定钉死；dashboard/README 的记账描述均指向 backlog 头注释单一出处。（2026-07-08）

## 依赖与顺序

阶段 1 → 阶段 2（业务最急，文件改动小）→ 阶段 3（搬运，需 git 兜底）→ 阶段 4（改写，逐项人审）。
阶段 0 与其余并行，越早越好（EP04 是系列唯一未闭环）。
