# 合并与重排报告（33 课 → 29 课，2026-09-04）

本报告记录 `plan/` 下卡片、台账、产物清单、大纲摘要从 33 课合并重排为 29 课的全部改动。范围：`plan/cards/*.md`、`plan/handoff-ledger.md`、`plan/inventory.md`、`plan/spec-lesson-standard.md`、新增 `plan/outline-digest.md`（替换原六份 `outline-digest-*.md`）。未改动：`00-写作背景包.md`、`00-ADR.md`、`01-本机执行计划.md`、`plan/outline-format.md`、`plan/cards-review.md`、`plan/progress.md`、`plan/old-courses/`、`courses/` 下已过审成稿（`courses/01-一条内容的端到端走查.md` 经核查未引用 25 以上课号，无需改动）。

## 一、合并做了什么

按 `00-ADR.md` §4 和 `00-写作背景包.md` §7 已定的方案，五处合并/平移：

| 新课号 | 来源 | 新课名 | 处理方式 |
|---|---|---|---|
| 25 | 旧 25 + 旧 26 | 无头作业：进程流归一与终局裁决 | 合并，「本课要讲什么」「学员交付」「材料清单」「承接」合一；大纲 5+5 节压缩为 7 节（第 5 节合并旧 25/26 各自的裁决/并发内容，第 6 节合并收尾策略/恢复规则，第 7 节为唯一收尾节，结尾问题取旧 26） |
| 26 | 旧 27 | 读设计稿，定契约 | 未合并，仅重排课号；正文改「上一课」「材料清单」「承接」三处的课号引用 |
| 27 | 旧 28 + 旧 29 | 后端：读写两条路径 | 合并，大纲 4+4 节压缩为 7 节（合并收尾节，结尾问题取旧 29） |
| 28 | 旧 30 + 旧 31 | 前端：六页整站与联调 | 合并，大纲 5+5 节压缩为 7 节（P1/P3 合一节，单端口+验证合一节，结尾问题取旧 31） |
| 29 | 旧 32 + 旧 33 | 收官：形态坐标与 1:1 验收 | 合并，旧 33 的验收清单挂在旧 32 心法内容之后作为最后两节（第 5 节「验收怎么用+结构项」、第 6 节「内容项」），第 7 节为唯一收尾节，结尾问题取旧 33 |

合并原则：两课「本课要讲什么」合成一份；「2.1 大纲」合成一份且一级标题 ≤7 节，同类节合并（收尾节只留一个，取后一课的「结尾抛出的问题」；同主题的开篇契约合一）；「学员交付」「材料清单」（去重）「承接」按新的前后课全部重排；验收脚本参数：25、26、27、28 均为 `实操`，29 为 `方法论`；旧 30、31、32、33 四个卡文件已删除，新文件按 `25.md`～`29.md` 命名落盘。所有卡定位块「全书 33 课 / 01→33」统一改为「全书 29 课 / 01→29」（`共同约束（33 门课一致，不许改写）`页脚按其自身声明原样保留，未改）。

## 二、课号映射表

| 旧课号 | 新课号 |
|---|---|
| 01–24 | 不变 |
| 25 | 25（并入） |
| 26 | 25（并入） |
| 27 | 26 |
| 28 | 27（并入） |
| 29 | 27（并入） |
| 30 | 28（并入） |
| 31 | 28（并入） |
| 32 | 29（并入） |
| 33 | 29（并入） |

全书课号重排已按此映射改完 `plan/cards/01.md`～`29.md`、`plan/handoff-ledger.md`、`plan/inventory.md`、`plan/spec-lesson-standard.md`、`plan/outline-digest.md` 里所有引用；范围写法同步改（如「27 到 31」改「26 到 28」）。改完后 grep `第 3[0-3] 课` 与 `3[0-3] 课` 在上述五类文件里零命中（已验证）；`01 到 24` 内部小范围引用（如「12 到 16」「18 到 19」）未受影响，原样保留。

## 三、六个补大纲子代理核出的十二条，各改在哪

1. **`docs/00-walkthrough.md` 补进 inventory.md 第 01 行**：`plan/inventory.md` 第 01 课结束时小节「仓库文件与目录」行，`（无）` 改为 `` `docs/00-walkthrough.md` ``。
2. **「沙盒代」改「护栏代」+ 与治理线 `harness/` 撞名的括注；第 29 课回收用同一词；四位置模型第三格去掉「推断待核」**：`plan/cards/06.md` 大纲第 1、2 节两处；`plan/cards/29.md` 「本课要讲什么」第 2 条与大纲第 2 节，明确区分「四代演进表（护栏代）」与「四位置模型表（文档）」是两张不同的表，回收时不再混用；`plan/outline-digest.md` 第 06、29 课条目同步。
3. **`overrides` 口径统一为 `tts.config.json`，并注明仓库两处不一致**：`plan/cards/07.md`、`08.md`、`10.md` 的「本课要讲什么」末尾各加一条；`07.md` 大纲第 3 节原有的 `overrides` 落点描述保持不变（已是 `tts.config.json` 口径）。
4. **`depause.mjs` 不断言具体命令行格式，只写三条约束**：`plan/cards/08.md` 「本课要讲什么」新增第 6 条，指明仓库真实实现（`--dir --cap --minact`，按目录批处理）与文档写法不一致，正文只给不切词/不原地写/跑完复扫三条约束，参数契约留给学员自定。
5. **`pipeline/5-retro.md` 改 `content/_template/5-retro.md`**：`plan/inventory.md` 第 22 课结束时小节「文档」行。
6. **第 20 课卡内容确认已按「第 02 课已装 social-auto-upload、20 课只做登录」写（未改卡）；inventory.md 补登录态**：`plan/inventory.md` 第 02 课结束时小节确认已列 `social-auto-upload`；第 20 课结束时小节「仓库文件与目录」行加一句「登录态（cookies 文件）」。
7. **第 18 课卡片结构按 `cards.py` 真实实现改（三张卡各两按钮，无查看全文，封面单独发）；inventory.md 四决策点措辞改**：`plan/cards/18.md` 「本课必须定死的契约骨架」第 2 条重写；大纲第 3 节原文已是三卡两按钮结构（未改）；`plan/inventory.md` 第 18 课结束时小节「概念与判据」行的「四个决策点」措辞改为「长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离」（该措辞随后在 19～29 课累计小节里一并生效，因为是同一份累计事实）。
8. **第 19 课加 `REWORK_LIMIT=2` 与第 10 课三轮熔断是两个独立数字、锁定 `console_api:false` 旧 spawn 路径**：`plan/cards/19.md` 「本课要讲什么」新增第 7、8 条；大纲第 2、3 节同步补一句区分与锁定说明。
9. **`media check` 规则数七条改八条**：`plan/inventory.md`（原 01–24 段落全量替换，25–29 段落新写时直接写「八条」）；`plan/cards/15.md`（本课要讲什么第 1 条、大纲第 3 节、材料清单，三处）；`plan/cards/16.md`（核查无「七条/八条」字样，未改）；`plan/cards/29.md`（材料清单 `check.ts` 一行）。
10. **`nextUp.ts` 是写事务，归 M2；14 课材料清单挪走，inventory.md 第 14 行改精确读命令口径**：`plan/cards/14.md` 材料清单去掉 `nextUp.ts` 并注明留到第 15 课；`plan/cards/15.md` 材料清单写命令列表补回 `nextUp.ts`（大纲第 2 节原文已提到 `next-up`，未改）；`plan/inventory.md` 第 14 课结束时小节「命令」行改为「`media` 读命令（st、next、backlog ls；check 和 next-up 留到第 15 课）」。
11. **`tools/feishu-bot/meta.py` 改指 `server.py` 的 `_flip` 函数**：`plan/cards/15.md` 材料清单一行，并注明 `meta.py` 现在只剩读函数（`find_slug_dir`/`load_meta`/`read_publish_material` 等）。
12. **新 27 课加一条：`routes/jobs.ts` 实际五个作业端点，课程只讲三条慢作业**：`plan/cards/27.md` 「本课要讲什么」新增第 8 条、大纲第 5 节「慢作业 3 条」核心判断与交付物、材料清单 `jobs/runner.ts 与 defs.ts` 一行都加了这句说明。

## 四、跨模块核对：发现与改动

**L3 课一致性核对**（`spec-lesson-standard.md` vs 各卡大纲）：映射后 L3 课为 `12、13、14、15、16、18、19、26、27、28` 共十张（任务原文写「十一张卡」，按映射表实际只落在十个新课号上，可能是把旧编号 28/29、30/31 分别计成两张——记入下方未解决问题）。逐张核对六步节名、对比表列（方案/适用前提/代价/已知失败模式）、ADR 四项（决策/理由/被否方案/反悔成本）、执行方案四节模板、出口条件写法：

- **发现并改**：`plan/cards/18.md` 大纲第 6 节「落进同一份执行方案的四节模板（实施步骤 / 功能与详细设计 / 对外契约 / 风险与待定项）」——第二节名漏了模板要求的 `<单位> × 功能 × 详细设计` 结构，改成「实施步骤 / 决策点 × 功能 × 详细设计 / 对外契约 / 风险与待定项」。
- **发现并改**：新 `plan/cards/26.md`（原第 27 课同一处）同样漏了单位，写作时一并改成「实施步骤 / 接口 × 功能 × 详细设计 / 对外契约 / 风险与待定项」。
- 其余核对项（12、13、14、15、16、19、27、28）六步节名、对比表列、ADR 四项、出口条件写法均已符合标准，未发现需要改的地方。
- `spec-lesson-standard.md` 标题行与「使用方式」节的「18、19、27-31 课」已按映射改为「18、19、26-28 课」（对应第二节课号重排）。

**承接链核对**（`handoff-ledger.md` 五组新链）：24→25、25→26、26→27、27→28、28→29 五组逐一核对「第 N 课结尾抛出的问题」与「第 N+1 课开头接的点」是否同一件事，五组全部一致（合并规则本身保证了这一点：合并课的结尾统一取被合并两课里靠后一课的原文），无新增分歧，已写进 `handoff-ledger.md` 「链条核对」节新增小节；原有「11→12」「20→21」两处历史分歧记录未改动。

`inventory.md` 已重排成 29 节，合并课（25、27、28、29）的小节内容取自旧编号里较后一课的累计小节（原本就是累计写法，天然等于两课累计），未额外拼接。

## 五、还没解决的问题

1. 「十一张卡」与实际映射出的十张 L3 课号不一致，未能判断是任务描述笔误还是另有一张卡应算 L3（见上文第四节说明），未改动、留待人裁决。
2. `plan/cards/29.md` 大纲第 1 节「回收表」仍是占位说明（「其余课暂按各自任务卡『记忆锚点』占位，等对应课成稿写出后回填原句」）——这是旧 32 课卡本身的设计（只有第 01 课有成稿可摘录原句），合并时原样保留，不是新引入的缺口，但依然待第 02～28 课成稿写出后回填。
3. `plan/inventory.md` 里「`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）」这条历史标注未处理——不在本次任务范围（十二条consistency清单未列它），原样保留。
