# 全链承接核对报告（第 01～28 课）

核对时间：2026-09-05。范围：`V3/courses/` 下第 01 到 28 课成稿（第 29 课在写，未核对）。只读核对，未改任何文件。

核对依据：`V3/plan/00-写作背景包.md` 第 2、3、6 节，`V3/plan/handoff-ledger.md` 主表全文，`V3/plan/inventory.md`。

方法：四路并行只读子代理分别核对第 01-07、08-14、15-21、22-28 课的「承接 / 结尾 / 跨课引用 / 读者视角」四项，主对话独立完成全书「术语口径」一致性核对（用 grep 覆盖全部 28 课全文），并对四路报告里的关键发现逐条用 grep 和实际仓库文件核实。

---

## 第 01 课（一条内容的端到端走查）

- 承接：无上一课，跳过。
- 结尾：通过，与台账逐字一致。
- 跨课引用：**不通过**。
  - 文件:行号 / `01-一条内容的端到端走查.md:81`
  - 问题：终检闸表格断言"音画同步"这一判据"08 造抽帧脚本"，即声称第 08 课会造一个基于抽帧、能机器判真假的音画同步检测脚本。但第 07 课定的六项检查点里没有"音画同步/抽帧"这一项，第 08 课实际造出的 `check-av-consistency.mjs` 是纯文本比对（网页组件里的大字文案 vs 口播文案逐字对照），候选类，人工判断，恒 `exit 0`，不是基于抽帧的时间轴同步检查，也不产生机器判定的真假结果。这个承诺在第 07、08 课全文均未兑现，`抽帧`二字在 08 课全文查不到。
  - 建议改法：要么在第 08 课补一个真正基于抽帧的音画同步检测脚本（改变第 08 课范围），要么把第 01 课这一行判据和归属课号改成如实反映现状（改成"组件大字与口播不一致"，判定方式是人工，不是脚本）。二选一，需要人裁决影响面。
- 读者视角：通过。第 84 行"仓库里有一条内容出审前留下的注释……"明确框定为参考仓库材料。

## 第 02 课（装备：开源件与自检基线）

- 承接：通过，与第 01 课实际结尾语义一致。
- 结尾：通过，与台账（已改版本）逐字一致。
- 跨课引用：**不通过**。
  - 文件:行号 / `02-装备-开源件与自检基线.md:15`、`02-装备-开源件与自检基线.md:45`
  - 问题：两处重复第 01 课同一处漂移——"第 07、08 课要造的配音质检脚本……抽帧核对音画"。与第 01 课问题同源，第 07、08 课从未实现基于抽帧的音画核对脚本。
  - 建议改法：与第 01 课那条一并处理，改法定下来后本课这两处措辞同步改。
- 读者视角：通过。

## 第 03 课（立骨架：仓库、目录、契约）

- 承接：通过，与第 02 课实际结尾语义一致（标点从冒号变逗号，语义未变）。
- 结尾：轻微问题。
  - 文件:行号 / `03-立骨架-仓库、目录、契约.md:230`
  - 问题：正文用逗号连接"骨架有了，AI 能读你的契约复述流程，可它不知道……"，台账"结尾抛出的问题"列用句号断成两句。纯标点差异，语义未变。
  - 建议改法：优先级低，可选择性同步标点，不影响判断。
- 跨课引用：通过（"12 到 16 课""第 21 课"等均为前瞻性提法，不需现在核实）。
- 读者视角：**不通过**。
  - 文件:行号 / `03-立骨架-仓库、目录、契约.md:113`
  - 问题：本课第 32 行刚说学员自己的 `pipeline/lessons.md` "这一课先建成空文件"，紧接着第 113 行又说"这条真实教训现在记在这套流水线的 `pipeline/lessons.md` 里，编号 L5"，没有用"参考仓库/参考流水线"这类框定语，容易让读者以为自己刚建的空文件里已经有编号 L5 这条记录，并且与本课自己刚说的"空文件"自相矛盾。全书其他处理同一件事的地方（第 05 课 L15、第 08 课 L16、第 20 课 L5/L6）都明确写"参考流水线/仓库里……这份记录不在你的项目里"，本课是全书唯一一处缺了这层框定的地方，直接撞上背景包第 2 节点名的雷区（把参考仓库的踩坑编号当成读者自己文件里已有的东西）。
  - 建议改法：改成"参考流水线的 `pipeline/lessons.md` 里记着这个事故，编号 L5；你自己这份 `lessons.md` 现在还是空的，等真踩到同类坑再补"这类说法，与第 20 课的处理方式对齐。

## 第 04 课（账号大脑：五份文件怎么写）

- 承接：通过，与第 03 课实际结尾语义一致。
- 结尾：轻微问题。
  - 文件:行号 / `04-账号大脑-五份文件怎么写.md:170`
  - 问题：正文"账号大脑写完了，AI 知道你要什么内容了……"，台账写的是"大脑写完了，AI 知道你要什么内容了……"，少两个字，逐字不一致（逐义一致）。
  - 建议改法：优先级低，任选一边改成另一边的原文统一措辞。
- 跨课引用：通过。
- 读者视角：通过。

## 第 05 课（用开源 Skill 跑通一条内容）

- 承接：通过，与第 04 课实际交付内容一致（开头比台账摘要更具体，属正常复述）。
- 结尾：中等问题。
  - 文件:行号 / `05-用开源Skill跑通一条内容.md:223`
  - 问题：正文是"第一条成片出来了，你眼睛看着觉得还行，这个判断是你一步步盯着走完全程才给出来的。可下次你不在场的时候，谁来看这个还行？"，台账（已改版本）是"第一条成片出来了，你眼睛看着觉得还行。可下次你不在场的时候，谁来看这个还行？"——正文在中间多插入了一整句"这个判断是你一步步盯着走完全程才给出来的"，不是标点级差异。
  - 建议改法：把这句插入内容同步进台账"结尾抛出的问题"列，或从正文删掉以对齐台账，二选一，当场改台账并打勾。
- 跨课引用：中等问题。
  - 文件:行号 / `05-用开源Skill跑通一条内容.md:18`
  - 问题："开工前顺手确认两个开源 skill 还在位，第 02 课装的时候一个进了项目仓库、一个进了家目录"，把 web-video-presentation 和 baoyu-image-gen 的安装位置差异归因于第 02 课。查第 02 课原文，第 02 课只提到 baoyu-image-gen 的**凭据**放在 `~/.baoyu-skills/.env`，没有明确说这两个 skill 本体分别装到项目级和家目录级。这句话本身在真实仓库里是对的，但"第 02 课装的时候"这个归因在第 02 课成稿里找不到出处。
  - 建议改法：改成不挂"第 02 课"前缀的中性说法，或回第 02 课补一句说明装机位置。
- 读者视角：通过。

## 第 06 课（验证信号流向谁）

通过。承接、结尾、跨课引用、读者视角四项均无问题。四位置模型、四代演进表两组术语在本课首次定义，口径清楚（第三格「文档」、第三代「护栏代」）。

## 第 07 课（反馈信号从哪来）

- 承接：通过。
- 结尾：通过，与台账逐字一致。
- 跨课引用：**不通过**。
  - 文件:行号 / `07-反馈信号从哪来.md:102`
  - 问题：本课写"学员第 04 课交付 `brain/tts.config.json` 时已经拿到这个字段的结构：逐段覆盖 speed、pitch、pronunciation、voice_id，键是 segment id"，把 overrides 字段结构归因于第 04 课。核对第 04 课原文（第 151 行），表格只写"pronunciation / overrides：全局多音字（先留空）/ 按段覆盖语速与注音"，只提了 speed 和 pronunciation 两项，没有 pitch，更没有把 voice_id 列为 overrides 子字段（voice_id 在第 04 课是 tts.config.json 的顶层字段）。pitch 是第 05 课 tts-dub skill（第 123 行）才加进 overrides 描述的。本课凭空往 overrides 结构里加了 voice_id，并把整套结构（含第 04 课没有的 pitch）都错误归因给第 04 课，是一处实质性跨课引用失实。
  - 建议改法：把归因改准确——overrides 的 speed/pitch/pronunciation 三项结构是第 05 课 tts-dub skill 定的，不是第 04 课；同时删掉 voice_id，因为 04/05/07 三课从未把 voice_id 放进 overrides 子字段。
- 读者视角：通过。

## 第 08 课（造出六个检测脚本）

通过。承接、结尾、跨课引用、读者视角四项均无问题。参考流水线的 L16、L18 等踩坑编号均正确框定为"参考流水线自己的历史"。

## 第 09 课（裁判与选手分离）

通过。承接、结尾、跨课引用、读者视角四项均无问题。

## 第 10 课（自愈回路与三轮熔断）

通过。承接、结尾、跨课引用、读者视角四项均无问题。overrides 口径打架（`tts.config.json` 的 overrides vs `segment-overrides.json`）在本课正确统一并说明了历史原因。

## 第 11 课（SPEC 方法：从调研到验证的六步）

- 承接：通过。
- 结尾：通过，与台账逐字一致。
- 跨课引用：**不通过**（编造数字，违反背景包"零编造"铁律）。
  - 文件:行号 / `11-SPEC方法-从调研到验证的六步.md:11`
  - 问题："第 8 课那六个检测脚本加起来 278 行；后面要造的控制台里，负责解析进程输出的那个模块有 537 行。"经在仓库里实测：六个检测脚本（`check-completeness/pace/silence/source-sync/pronunciation/av-consistency.mjs`）实际总行数为 **246 行**（278 是把不属于"六个检测脚本"的 `list-voices.mjs` 也算进去之后的 7 个文件总和）；控制台里负责解析进程输出的模块最接近的候选是 `tools/console/packages/cc-stream/src/normalize.ts`，实测 **202 行**，全仓库找不到任何 537 行的对应模块。两个数字都无出处，且把它们说成"第 8 课那六个检测脚本"的真实行数，容易让读者误以为这是自己文件的真实数据。
  - 建议改法：删除这两个具体数字，改成不依赖精确行数的定性描述（如"脚本量级远小于后面要造的解析模块"）；如果一定要保留数字，须重新核实仓库真实值（246 行 / 202 行）并就近给出处。
- 读者视角：通过。

## 第 12 课（media 调研与选型讨论）

- 承接：与第 11 课实际结尾之间的差异，台账"第 11→12 课"一节已明确记录并裁决（09-04 已裁：11 到 12 改第 11 课结尾为前三步），**非本次新发现，不计入问题数**。
- 结尾：**不通过**。
  - 文件:行号 / `12-media调研与选型讨论.md`（全文最后一段）
  - 问题：台账"结尾抛出的问题"列原文是"四组路线拍完板，ADR 写好了。决策变不成代码，中间还差一份能让 AI 照着施工的执行方案。"但成稿实际最后一段是"就这么回事，四组路线选完了，剩下的活留给下一课。"两者完全对不上，不是措辞级差异。即便退一步看倒数第二段（"决策落了纸，AI 还照着施工不了，中间差一份能让 AI 照着施工的执行方案"），措辞也和台账原句不同。
  - 建议改法：需要人裁决二选一——把台账该行改成成稿实际收尾句并在"改过没"列打勾，或把成稿最后一段改回台账原句，当前两边确实对不上，不能自行选定。
- 跨课引用：通过（四组互斥路线名称、第 10 课熔断细节引用均属实）。
- 读者视角：通过。

## 第 13 课（media 写 SPEC）

通过。承接、结尾、跨课引用、读者视角四项均无问题。参考流水线 `flip` 命令示例正确标注为示意，未冒充学员自己的文件。

## 第 14 课（执行 M1：core 与首批命令）

通过。承接、结尾、跨课引用、读者视角四项均无问题。未发现把 `tools/console`、`packages/*` 等仓库真实路径当学员自有文件的问题（本课正确地没有钉死 core 包的目录路径）。

## 第 15 课（执行 M2：全量命令与存量迁移）

通过。承接、结尾、跨课引用四项均无问题。读者视角本课自身没有问题，但本课只实现了 `backlog sweep` 的通用机制（预演/落盘），没有替读者定过期天数的具体阈值——这个"未定值"状态在第 23 课被误当成"已定"引用，问题记在第 23 课。

## 第 16 课（验证与收敛）

- 承接：通过。
- 结尾：通过，与台账逐字一致。
- 跨课引用：通过（CHK-01 到 CHK-08 八条规则、第 13 课出口条件原句均核实无误）。
- 读者视角：**不通过**（与第 19 课同类问题，且已用第 27 课原文交叉印证）。
  - 文件:行号 / `16-验证与收敛.md:23`
  - 问题："真跑一遍之后，这次改动会在 `tools/console/logs/audit.jsonl` 里追加一行。"——把参考仓库的真实文件路径 `tools/console/logs/audit.jsonl` 写成学员自己此刻就有的路径。但控制台（`tools/console/` 这个目录结构）要到第 26 课的 ADR 才第一次被决策出来（"决策：单仓多包，`tools/console/` 下五个包……"），第 16 课属于模块 3（media CLI），此时控制台还不存在。全书其余 14 处提到 `audit.jsonl` 的地方（第 11、15、17、19、20 课）都只写裸文件名 `audit.jsonl`，不带任何目录前缀，唯独这一处突然带上了 `tools/console/logs/` 前缀，是一处孤立的路径泄漏。经核实，`tools/console/logs/audit.jsonl` 正是参考仓库里的真实路径。
  - 建议改法：把这一处改成裸文件名 `audit.jsonl`，与全书其余 14 处保持一致，不提前泄漏第 26 课才决策的目录结构。

## 第 17 课（人审闸口：闸设在哪）

通过。承接、结尾、跨课引用、读者视角四项均无问题。本课对 `state.ts` 的引用是全书的正确示范——"输入是你第 14 课造的 core 包里 state 模块的 review 状态三条出边（**路径按你自己 SPEC 里定的来**）"，没有钉死具体目录。

## 第 18 课（飞书通道 SPEC）

通过。承接、结尾、跨课引用、读者视角四项均无问题。"第 17 课卡片字段草案原文"逐句核对与 17 课实际字段表完全吻合。

## 第 19 课（飞书实现与按钮回流）

- 承接：通过，开篇准确复述 18 课四个决策点。
- 结尾：通过，与台账逐字一致。
- 跨课引用：通过（"第 18 课每张卡最多两个按钮""第 15 课 4.3 节的例子""第 10 课三轮 vs REWORK_LIMIT=2 是两个数"均核实无误）。
- 读者视角：**不通过**（本次核对里最明确的一处，且能被第 27 课原文直接证伪）。
  - 文件:行号 / `19-飞书实现与按钮回流.md:7`（同一路径认领延续到第 39、91 行的叙述语境）
  - 问题：把参考仓库的真实文件路径 `tools/console/packages/core/src/state.ts` 写成"本课的输入"，即当作学员自己已有的文件路径来引用。但学员的 media CLI core 包是第 14 课造的，那时候控制台（`tools/console`，第 26～28 课才建）根本不存在，学员自己的 core 包不可能嵌在 `tools/console/` 目录下。第 17 课对同一个对象（state 模块的 review 三条出边）的引用是正确示范——"路径按你自己 SPEC 里定的来"，第 19 课在这里明显退步。更直接的反证是第 27 课自己的原文："你自己第 19 课的服务……因为**那时候控制台还没造出来**，照默认配置实现，等于给一个当时还不存在的东西发请求，永远接不上。"——第 27 课自己承认第 19 课那个时间点控制台不存在，但第 19 课第 7 行却写着控制台路径下的文件是"本课输入"，两处自相矛盾。
  - 附带说明：本课正文里真正下发给 AI 的 Prompt（第 55～68 行）只写了裸 `state.ts`，没有带 `tools/console/` 前缀，问题只出在叙述文字里，不影响实际操作指令，但仍会误导读者以为自己仓库现在就该有这个目录结构。
  - 建议改法：把第 7 行和正文里出现的 `tools/console/packages/core/src/state.ts` 一律改成"你自己 SPEC 里定的 core 包 state 模块路径"，或直接删去具体路径前缀只留 `state.ts` 模块名，呼应第 17 课的写法。

## 第 20 课（发布 Skill 与 dry-run）

通过。承接、结尾、跨课引用、读者视角四项均无问题。"参考流水线 lessons.md L5/L6"正确标注为参考仓库经验。

## 第 21 课（双线闭环与资产读写表）

- 承接：与第 20 课结尾之间的差异，台账"第 20→21 课"一节已明确记录并裁决，**非本次新发现，不计入问题数**。
- 结尾：通过，与台账逐字一致。
- 跨课引用：通过。反哺大环三课地图表（21/22/24）在第 21 课与第 24 课经逐字比对完全一致（第 24 课原文也明确写"这张表跟第 21 课第 5 节的那张一字不差"）。
- 读者视角：通过。

## 第 22 课（复盘归因反哺大脑）

- 承接：通过，逐字引用第 21 课实际收尾。
- 结尾：通过，与台账逐字一致。
- 跨课引用：轻微问题。
  - 文件:行号 / `22-复盘归因反哺大脑.md:47`
  - 问题：Prompt 里写"我第20课已经建好的 douyin-publish skill 和 pipeline/4-publish.md 是怎么处理抖音登录的"。第 20 课的登录处理主要在该课第 1 节（依托 social-auto-upload 的 cookie 机制），`pipeline/4-publish.md` 管的是发布物料六字段和闸口，并不是登录处理的规格来源。表述把两件事并成一句，不算错但不够精确。
  - 建议改法：删掉"和 pipeline/4-publish.md"，或改成"和 pipeline/4-publish.md 里定的字段规格"分开说清楚哪部分管登录、哪部分管字段。
- 读者视角：通过。

## 第 23 课（选题池与外网情报）

- 承接：通过，逐字引用第 22 课结尾。
- 结尾：通过，逐字对齐台账。
- 跨课引用：**不通过**。
  - 文件:行号 / `23-选题池与外网情报.md:138`
  - 问题："过期清扫 | `media backlog sweep --apply` | 能 | 参考流水线的两条窗口是 urgency 为 today 的候选超 2 天、时效分大于等于 4 的候选超 7 天，自动翻成 expired；**你自己第 15 课 sweep 定的窗口按你的来**"——暗示第 15 课已经替读者的 `backlog sweep` 命令定过具体的过期天数窗口。核对第 12、13、15、16 课全文，第 15 课只实现了 `sweep` 命令的预演/落盘通用机制，从未让读者自己拍板过具体的天数阈值；CHK-07 这一判据在第 13、16 两课写的是模糊的"选题快要被清扫规则命中"，唯独第 15 课自己写成了具体的"2 天"，三课关于同一判据的表述本身就不完全一致，"2 天"更像是示例默认值，不是明确要求读者钦定的决策。本课这句话把一个读者没有走过的决策步骤当成了已完成的既定事实。
  - 建议改法：把"按你自己第 15 课定的窗口按你的来"改成"这一课自己先定一个窗口"；如果确实希望这个决策定在第 12～16 课，需要回头检查是不是本该在第 12 课 ADR 或第 13 课执行方案里补一条"过期窗口天数"的决策项，目前查无此项。
- 读者视角：通过。

## 第 24 课（治理总调度与自审元层）

通过。承接、结尾、跨课引用、读者视角四项均无问题。24/25/26/27/28 五组"合并改版"边界经台账记录及本次核对均一致。

## 第 25 课（无头作业：进程流归一与终局裁决）

通过。承接、结尾、跨课引用、读者视角四项均无问题。

## 第 26 课（读设计稿，定契约）

通过。承接、结尾、跨课引用、读者视角四项均无问题。设计稿路径全部写作 `V3/appendix/design/`，未见 `docs/design/` 误写。五包一仓 ADR 明确标注 core/cli/cc-stream 三包"早就按这条边界在跑了，只是没有正式收口"，交代了历史沿革，本身没有问题（问题出在第 16、19 两课提前把 `tools/console/` 当成既成路径来写，而不是第 26 课本身）。

## 第 27 课（后端：读写两条路径）

通过。承接、结尾、跨课引用、读者视角四项均无问题。watcher 监听目录明确标注"都是你自己第 03、21 课已经交付过的真实目录和文件，仓库里没有的目录不要写进去"，主动规避了读者视角雷区。

## 第 28 课（前端：六页整站与联调）

通过。承接、结尾、跨课引用、读者视角四项均无问题。`theme.css` 606 行一说经核实与随课发放的 `V3/appendix/design/css/theme.css` 实际行数完全一致，属于学员自己材料，不是编造。`usePageData` 等命名明确标注为参考流水线实现，不是学员已有文件。

---

## 全书术语口径核对（第 5 项要求，全书统一核对，逐项通过）

以下八组口径经全文 grep 覆盖 28 课，均一致，未见冲突：

1. 四个治理 skill = retro（`douyin-retro`）/ ideate（`douyin-ideate`）/ backlog-gardener / benchmark-refresher；`harness-dispatcher` 全书统一称"调度器"；`account-audit` 全书统一称"自审元层"。
2. 第 06 课四位置模型第三格全书统一叫「文档」（未见"记录""日志"等混用）；四代演进表第三代全书唯一命名点在第 06 课，统一叫「护栏代」。
3. `overrides` 落点全书统一收敛为 `tts.config.json` 的 `overrides` 字段；参考流水线自身存在的 `segment-overrides.json` 口径冲突在第 07、09、10、11 课均被明确指出并统一处理，无遗留冲突。
4. `media check` 的 CHK 规则全书统一为 CHK-01 到 CHK-08 共八条，第 13、15、16 课三处表格逐字一致。
5. 飞书重做上限（`REWORK_LIMIT = 2`，第 19 课）与配音质检三轮熔断（第 10 课）全书明确区分为两个独立数字，第 19 课原文专门写明"这个二和第 10 课的三没有换算关系"。
6. 反哺大环三课地图（21/22/24）：第 21 课第 5 节与第 24 课第 4 节的三行表格逐字一致，经比对确认。
7. SPEC 目录口径 `docs/spec/media/`、`docs/spec/feishu-gate/`、`docs/spec/console/` 全书统一，未见拼写变体。
8. 设计稿路径全书统一为 `V3/appendix/design/`（含第 26、28 课两处引用），未见 `docs/design/` 误写。

---

## 问题总数与严重度排序汇总

本次核对（第 01～28 课）共发现 **14 处问题**，另有 **2 处台账已裁决的已知承接差异**（第 11→12 课、第 20→21 课，均非本次新发现，不计入总数）。术语口径核对全部通过，无新发现。

按背景包要求的严重度分层（承接断裂 > 跨课引用失实 > 术语不一致 > 措辞）排列：

### 一、跨课引用失实 / 事实矛盾（8 处，含 2 处等同于"读者视角+事实矛盾"的复合问题，严重度最高）

1. **第 16 课 `16-验证与收敛.md:23`**：把参考仓库真实路径 `tools/console/logs/audit.jsonl` 当成学员此刻已有的路径，此时控制台（第 26 课才决策）尚不存在，与全书其余 14 处裸文件名 `audit.jsonl` 的写法矛盾。
2. **第 19 课 `19-飞书实现与按钮回流.md:7`**：同类问题，`tools/console/packages/core/src/state.ts` 被当作"本课输入"，与第 17 课对同一对象的正确处理（路径不钉死）矛盾，且被第 27 课自己的原文直接证伪（"那时候控制台还没造出来"）。
3. **第 11 课 `11-SPEC方法-从调研到验证的六步.md:11`**：编造未经核实的行数（278 行、537 行），实测应为 246 行和 202 行，违反"零编造"铁律。
4. **第 12 课结尾段**：成稿实际最后一段与台账"结尾抛出的问题"完全对不上，需要人裁决更新台账或改正文。
5. **第 01 课 `01-一条内容的端到端走查.md:81`** 与 **第 02 课 `02-装备-开源件与自检基线.md:15、45`**：承诺"08 课造抽帧脚本"做音画同步机器判定，第 08 课从未兑现，实际造的是人工判断的文本比对脚本。
6. **第 23 课 `23-选题池与外网情报.md:138`**：声称"第 15 课已定"过期清扫窗口天数，但第 15 课只造了通用机制，从未替读者定过具体天数。
7. **第 07 课 `07-反馈信号从哪来.md:102`**：overrides 字段结构（含凭空多出的 voice_id）错误归因于第 04 课，实际分散不一致地定义在 04/05 两课。

### 二、读者视角违规（1 处，与上面第 1、2 条同类但单独立项）

8. **第 03 课 `03-立骨架-仓库、目录、契约.md:113`**：`pipeline/lessons.md` 编号 L5 未框定为"参考流水线"，与本课自己刚说"lessons.md 是空文件"矛盾，直接撞背景包点名的雷区，是全书唯一缺失框定语的一处。

### 三、结尾/措辞类（5 处，优先级较低）

9. 第 05 课结尾多出一整句判断性插入语，需同步台账或改正文（中）。
10. 第 05 课把两个 skill 安装位置的差异归因于第 02 课，第 02 课原文无此表述（中）。
11. 第 22 课把抖音登录处理归到 `pipeline/4-publish.md`，措辞不够精确（低）。
12. 第 04 课结尾"账号大脑"vs 台账"大脑"，少两字（低）。
13. 第 03 课结尾标点差异（句号/逗号），语义未变（低）。

---

## 已知台账差异（非本次新发现，供参照）

- 第 11→12 课：台账已裁决记录第 11 课结尾只预告六步的第 1、2 步，但第 12 课交付物包含第 3 步 ADR，属已知差异。
- 第 20→21 课：台账已裁决记录第 20 课结尾问的是"数据谁看/怎么改"，第 21 课开头讲的是治理线整体骨架，属已知差异。
