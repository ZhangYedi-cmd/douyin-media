# 第 24 课任务卡

> 来源：课程任务卡系统自动转换生成，2026-09-04。先读 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/00-写作背景包.md`，再读本卡。
> 本卡里凡是引用 `00-ADR.md` 章节号的地方，按 ADR 实际章节找；找不到就在回复的未核实项里说明。

## 1. 定位

- **模块**：模块 5 治理线
- **课号**：第 24 课（全书 29 课，**严格串行 01 → 29**，阅读顺序即写作顺序）
- **教法档**：L2
- **验收脚本参数**：`实操`

## 2. 本课要讲什么

1. 旧 13 课 + `harness-dispatcher` 封装：读注册表、评估触发条件（事件窗口 / 周期 / 加权池）、到点才派活、收回报告 append 进账本。
2. 另外三个治理 skill：`backlog-gardener`、`benchmark-refresher`、`account-audit`，各给一条 L1 prompt。
3. 调参限幅表：自审能自动调哪些数值、哪些必须提议人审。
4. **这一课是反哺大环的第三段，必须显式点破**：自审元层是反哺作用在治理线自己身上——大环合上之后，连「怎么治理」这件事本身也进了回路。别让学员以为自审是个孤立功能。

## 2.1 大纲

记忆锚点：自审是反哺作用在治理线自己身上——大环合上之后，连「怎么治理」这件事本身也进了回路。

开篇契约：上一课交付了 `douyin-ideate` skill 和一批入池候选，选题池能自己养起来了；本课输入是学员的 `harness/tasks.md`（第 21 课的空壳）、`brain/benchmarks.md`、`content/_backlog/backlog.yaml`；本课产出一份任务注册表 + 四个治理 skill + 一轮巡检记账。

### 1. harness-dispatcher：读注册表，到点才跑
- 核心判断：调度和执行要解耦——加一个治理任务只改 `tasks.md` 的 yaml，不碰 dispatcher 本身；三种触发类型（`post-publish-window` 事件触发、`periodic:<N>d` 周期触发、`weighted-pool` 加权池）各自的到点判据不一样，不能用同一套逻辑判断。
- 二级标题：1.1 三种触发类型怎么判到点 / 1.2 派活与记账（收报告，`applied:false` 待人审应用）
- 交付物：prompt 生成 `harness-dispatcher` skill + `harness/tasks.md` 的 yaml 注册表骨架（含 retro/ideate 两个已实现任务的 trigger 定义）
- 学员此时手上有：第 21 课的 `harness/tasks.md` 空壳、第 22/23 课的 douyin-retro/douyin-ideate skill
- 交接：调度器会喊人干活了，但真正干活的还差两个任务

### 2. 三个治理 skill 起步：backlog-gardener / benchmark-refresher / account-audit
- 核心判断：三个技能各管一类「没人催但放着会烂」的资产，量不大、路线不互斥，用一条 L1 prompt 各自钉死判据加验收就够，不需要走 SPEC 六步。
- 二级标题：不分
- 交付物：三条 L1 prompt（各自：目标怎么选 / 干什么 / 产物与记账）——backlog-gardener 只产提议不翻状态、benchmark-refresher 逐条判成立 / 存疑 / 失效、account-audit 定熄火线（单任务有效记录 <5 条只出心跳报告）
- 学员此时手上有：第 1 节的注册表结构、第 21 课的资产读写表、`brain/benchmarks.md`
- 交接：三个技能都能跑了，但谁能碰治理线自己的配置，得先划边界

### 3. 调参限幅表：自审能自动调什么
- 核心判断：可逆的数值（间隔、权重）划进自动档，启停和锁定参数一律走提议人审——这条边界防的是自审自己把自己的配置改跑偏，改错了却没人拦住。
- 二级标题：不分
- 交付物：调参限幅表（参数 / 当前值 / 可自调范围 / 单次步长 / 调整规则），照 `harness/tasks.md` account-audit 任务卡的边界表结构（如 benchmark-refresher 间隔 14~60d、backlog-gardener 间隔 7~30d、ideate 间隔锁定、retro 窗口锁定）
- 学员此时手上有：第 1 节的注册表、第 2 节的三个治理 skill
- 交接：边界画完，得点破这一层到底是什么

### 4. 反哺大环第三段：自审作用在治理线自己身上
- 核心判断：大环不是只喂给生产内容用的——account-audit 不审内容、不审 brain，只审治理线自己跑得好不好，这一层是反哺的自反闭合：第 21 课回答「治理线为什么只提议」，第 22 课回答「一条内容怎么单次归因」，这一课回答「连治理线的调度参数本身，也要拿运行数据反过来校准」。
- 二级标题：不分
- 交付物：反哺大环三段完整地图（课号 / 回答什么问题 / 交付什么）+ account-audit 的读写边界（只动 `harness/tasks.md` 的 yaml 数值字段，任务卡文字与其它文件一概不碰）
- 学员此时手上有：第 21 课骨架、第 22 课单次归因、第 3 节调参限幅表
- 交接：一轮真的巡检该跑起来了

### 5. 治理线能自己巡检了
- 本课做到了什么：一份能被 dispatcher 读到的任务注册表，四个治理 skill（retro、ideate、backlog-gardener、benchmark-refresher）加自审元层 account-audit，一轮巡检记账（每个任务写清楚差几天到点、跑没跑）。
- 还看不到什么：backlog-gardener 和 benchmark-refresher 这一课才第一次跑通；而且不管哪条线，现在都得学员自己敲一次命令才会动。
- 下一课补什么：怎么让它在学员睡觉的时候自己跑。
- 结尾抛出的问题：治理线能自己巡检记账了。可这些都要你敲一次命令才动。怎么让它在你睡觉的时候自己跑？

## 3. 学员交付

这一课结束时，学员手上要多出来的可落盘的东西：

一份任务注册表 + 四个治理 skill + 一轮巡检记账

## 4. 材料清单

写作 skill 的 Step 1 有硬门槛：**材料不足 5 条不进下一步**。以下路径都相对仓库根目录 `/Users/yedizhang/yedi-study/douyin-media`，逐条实际打开读，不要写「参考仓库」：

- **上一课（第 23 课 · 选题池与外网情报）的成稿全文**：`docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/23-选题池与外网情报.md`（等价物 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/23-*/final.md`）。**这是承接的第一真相源**，动笔前先读它的开篇与结尾两段。
- `.claude/skills/harness-dispatcher/SKILL.md`
- `harness/tasks.md` 全文（含各任务卡的调参限幅表）
- `harness/logs/index.jsonl`
- `.claude/skills/backlog-gardener/SKILL.md`、`benchmark-refresher/SKILL.md`、`account-audit/SKILL.md`
- 旧稿 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses/v1-15-治理总调度harness-dispatcher.md`（本机没有 `v2-13-治理总调度.md`，已按主题改指向 18 课旧稿里同主题的一篇，只当素材看口径，不能照搬其结论）
- 第 21、22、23 三课成稿的治理任务口径

## 5. 承接

**承接的第一真相源是上一课的成稿，不是下面这三行。**下面三行是计划里的预期，用来校验，不用来代替原文。

动笔前先读第 23 课的 `final.md`，开篇那句「上一课把 X 做完了」要按它实际写了什么复述，具体到产物名、数量、判据这一级，不要写成摘要。

```
上一课（第 23 课 · 选题池与外网情报）的产出：第 23 课交付了 `douyin-ideate` skill 和一批入池候选，治理线三个任务都有了。
本课开头要接的点：任务多了就得有人排班。这一课造调度器，顺带把大环最后一段合上。
本课结尾要抛出的问题：治理线能自己巡检记账了。可这些都要你敲一次命令才动。怎么让它在你睡觉的时候自己跑？
```

写完拿成稿的结尾对一遍这三行。**对不上不一定是错**——写着写着找到更好的收口是常事——但对不上就当场改 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/handoff-ledger.md` 里第 24 课那一行，让下一课拿到真实的交接点，并在「改过没」列打个勾。

## 6. 流程

跑 `/ai-arch-tutorial-style`（工程教程体写作 skill），六步流水线全走，只改一处：

- **Step 3（大纲）改为 AI 自选**：直接选定一个大纲方案继续往下写，不停下来等人选。选定理由写进 `outline.md` 一段话。
- 其余步骤照原样：Step 1 材料盘点存 `analyzed.md`（材料不足 5 条不进下一步，材料清单在上面第 4 块）、Step 2、Step 4 写 `draft.md`、Step 5 派 subagent 做读者诊断存 `diagnosis.md`、Step 6 按清单修改 + 五遍终检出 `final.md` 和 `polish-report.md`。
- 中间产物落盘到 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/24-24/`，保证断点可续。

## 7. 验收与交付

三关自己跑完才算交稿：

1. **硬禁令清零**：`python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py <final.md>` 零命中（装腔词、过度自信词、标题模板前缀）。这是写作 skill Step 6 的 ⛔，不放宽。
2. **验收脚本跑绿**：`LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh <final.md 路径> 实操`。**汉字数已改成只报数不 FAIL**（用户口径：字数按内容动态平衡，不做卡控），看到 `[INFO] 汉字 ... 超出参考值` 是正常的，**不要为了这一行去砍字**。剩下仍是硬 FAIL 的项：代码行上限、实操篇围栏块 ≥3、排障句 ≥2、状态值核对。
3. **材料可溯**：文中出现的每个文件路径、命令、脚本名，能在材料清单给的文件里 grep 到。脚本的「命令与脚本名核对」那一节会报 `[WARN] 未在仓库找到`，逐条确认掉。

交付动作，两件都要做：

- 落盘 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/24-治理总调度与自审元层.md`
- **`final.md` 全文写进最终回复，不要只给路径**，回复里要能直接读。`polish-report.md` 一并附上（它记着五遍终检各改了什么，是后面全链承接核对的线索）。

## 8. 交付后

交稿后不做接力。把三样东西写进你的最终回复：成稿路径、check-lesson.sh 最后 12 行输出、未核实项清单（仓库里 grep 不到、已按规则删掉或改写的名词与数字）。主回话验收后决定下一课。

失败怎么办：硬禁令没清零、材料不足被 Step 1 拦下这类能自愈的，原地重试一次。重试仍失败就停下，在回复里写清卡在哪一步、报错原文，不要自己降低标准硬交。

---

## 共同约束（33 门课一致，不许改写）

> 本课程的目标不是教人做自媒体，是让学员通过这条流水线学会怎么跟 AI 干活：掌握和 AI 协作的流程、懂得如何引导 AI、掌握 Loop 思维与架构设计。抖音只是载体。
>
> 因此：**视频制作本身（怎么剪、怎么配音、怎么调多音字和语速、封面怎么画）一律从简，走一遍流程即可，不做深入讲解。** SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环这四块是学员要重点掌握的，往厚里写。
>
> 判断标准：这一段讲的是「这个工具怎么用」还是「这类问题该怎么设计」。前者压，后者留。
>
> 注意别误伤 —— 模块 2 那四课（07-10）表面在讲配音质检，实际讲的是 Loop 架构，配音只是素材，不属于可精简范围。

还有一条铁律贯穿全书：**学员不写一行实现代码。**学员写的只有三类东西——中文的需求与判据、SPEC 文档、验收结论。代码、配置、脚本一律由 AI 产出。课文里出现大段代码就说明写偏了。
字数不设限，心法类内容（SPEC、Loop、状态收敛、反哺）讲透为准，可以多讲。
