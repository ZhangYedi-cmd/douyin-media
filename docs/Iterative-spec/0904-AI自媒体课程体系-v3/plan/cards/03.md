# 第 03 课任务卡

> 来源：课程任务卡系统自动转换生成，2026-09-04。先读 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/00-写作背景包.md`，再读本卡。
> 本卡里凡是引用 `00-ADR.md` 章节号的地方，按 ADR 实际章节找；找不到就在回复的未核实项里说明。

## 1. 定位

- **模块**：模块 0 起步
- **课号**：第 03 课（全书 29 课，**严格串行 01 → 29**，阅读顺序即写作顺序）
- **教法档**：L2
- **验收脚本参数**：`实操`

## 2. 本课要讲什么

1. **全新课**。目录树设计：`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/` 各管什么。
2. `CLAUDE.md` 怎么写：系统一句话、干活前必读、内容红线、流程纪律、文档架构约定。给规格表让学员填，不给成品抄。
3. `pipeline/` 五份阶段契约的写法：每份只写输入/输出/状态翻转/闸口，操作细节住 skill，同一规则只写一处。
4. `content/` 目录约定与六份文件模板、`backlog.yaml` 头部。
5. **验收动作直接复用第 01 课那个 Prompt**：让 AI 只读你的契约复述流程，复述结果和你的意图对得上才算过。

## 2.1 大纲

记忆锚点：骨架仓不是把文件堆起来，是把「谁能看懂谁」这件事从人脑挪到契约文件上——AI 只读契约就能复述流程，才算骨架立住了。

开篇契约：上一课交付了 `docs/00-toolchain.md` 和全绿自检输出，工具都装好了；本课输入是学员口述的目录切法和已装好的工具清单；本课产出一个能 `git init` 的骨架仓，含四件：目录树、`CLAUDE.md`、`pipeline/` 五份契约骨架、`content/` 模板与 `backlog.yaml` 头部。

### 1. 目录树设计：六个目录各管什么
- 核心判断：`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/` 六个目录各自只装一类东西，装混了后面每一课都要返工去分。
- 二级标题：不分
- 交付物：六目录职责对照表（目录 / 装什么 / 谁来写 / 谁来读）
- 学员此时手上有：`docs/00-toolchain.md`（上一课产出的工具清单，用来判断 `tools/` 目录该指向哪些已装工具）
- 交接：目录骨架定了，下一节把最外层的规则文件 `CLAUDE.md` 填出骨架。

### 2. CLAUDE.md：规格表而不是成品
- 核心判断：`CLAUDE.md` 的五节（系统一句话、干活前必读、内容红线、流程纪律、文档架构约定）要学员自己填空对着表回答，不是照抄一份现成范例，抄来的东西 AI 读不出你账号的判断。
- 二级标题：不分
- 交付物：规格表（五节各一行：这节回答什么问题 / 判据是什么，不给示例内容）
- 学员此时手上有：第 1 节的六目录职责对照表
- 交接：全局规则填完了，下一节把流程细节拆到 `pipeline/` 五份契约里。

### 3. pipeline 五份阶段契约：骨架与审查点
- 核心判断：每份契约只写输入 / 输出 / 状态翻转 / 闸口四项，操作细节住 skill 不住契约，同一条规则只许出现一次。
- 二级标题：分（五份契约，按数量分）。3.1 选题与取题：`1-ideate.md`；3.2 创作：`2-create.md`（含出审前终检闸）；3.3 出审：`3-review.md`；3.4 发布：`4-publish.md`；3.5 编排入口：`daily-run.md`
- 交付物：五份契约骨架各一份（骨架 = 固定小节标题 + 每节该填什么的审查点，不给全文内容）
- 学员此时手上有：第 2 节填完的 `CLAUDE.md`
- 交接：五份契约骨架立好了，下一节把 `content/` 目录的模板和选题池头部也搭起来。

### 4. content 模板与 backlog.yaml 头部：骨架与审查点
- 核心判断：`content/_template/` 的模板文件和 `backlog.yaml` 头部要按 `pipeline/` 契约里定的产物名对齐字段，不能各写各的。
- 二级标题：不分
- 交付物：`content/_template/` 六份文件骨架清单（含 `meta.yaml`）+ `backlog.yaml` 头部骨架（审查点：字段名是否在 pipeline 契约里出现过）
- 学员此时手上有：第 3 节的五份契约骨架（尤其 `1-ideate.md` 的状态翻转小节、`2-create.md` 的产物清单）
- 交接：骨架仓的四件东西都搭完了，最后验证 AI 能不能只读它们复述出你的流程。

### 5. 验证：让 AI 只读契约复述流程
- 核心判断：复述结果和你的意图对不上，说明契约本身没写清楚，不是 AI 理解错了；这条 Prompt 就是骨架搭没搭对的验收动作。
- 二级标题：不分
- 交付物：Prompt 块（复用第 01 课那条 Prompt 的变体：只读 `CLAUDE.md` + `pipeline/` 五份 + `content/_template/`，复述五个动作和人出现点，不许读别的文件）
- 学员此时手上有：骨架仓全部四件产物（目录树、CLAUDE.md、五份契约骨架、content 模板）
- 本课做到了什么、还看不到什么、下一课补什么：骨架有了，AI 能读契约复述流程；但它不知道账号该做什么内容、用什么口气说话，这些判断依据还没地方写；下一课把账号大脑写下来。
- 结尾抛出的问题：骨架有了，AI 能读你的契约复述流程。可它不知道你的账号该做什么内容、用什么口气说话。这些判断依据写在哪？

## 3. 学员交付

这一课结束时，学员手上要多出来的可落盘的东西：

一个能 `git init` 的骨架仓：目录树 + `CLAUDE.md` + `pipeline/` 五份契约 + `content/` 模板 + `backlog.yaml` 头部

## 4. 材料清单

写作 skill 的 Step 1 有硬门槛：**材料不足 5 条不进下一步**。以下路径都相对仓库根目录 `/Users/yedizhang/yedi-study/douyin-media`，逐条实际打开读，不要写「参考仓库」：

- **上一课（第 02 课 · 装备：开源件与自检基线）的成稿全文**：`docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/02-装备-开源件与自检基线.md`（等价物 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/02-*/final.md`）。**这是承接的第一真相源**，动笔前先读它的开篇与结尾两段。
- `pipeline/` 五份全文（`1-ideate.md`、`2-create.md`、`3-review.md`、`4-publish.md`、`daily-run.md`）
- `CLAUDE.md` 全文
- `content/_backlog/backlog.yaml` 头部与 `content/_backlog/README.md`
- `content/_template/` 全部模板文件
- 仓库根目录树（`git ls-files | head -100` 的形状）
- `00-ADR.md` §4 课程大纲九模块 29 课表，模块 0 第 03 课行（原引用的「§5」是借开源与自建清单，没有课程表，已改指到实际有大纲表的 §4；表本身已按 09-04 合并方案改成 29 课）
- `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses/mini-v3-03.md`（另一台机器写过的同课成稿，只当素材看口径，不能抄）

## 5. 承接

**承接的第一真相源是上一课的成稿，不是下面这三行。**下面三行是计划里的预期，用来校验，不用来代替原文。

动笔前先读第 02 课的 `final.md`，开篇那句「上一课把 X 做完了」要按它实际写了什么复述，具体到产物名、数量、判据这一级，不要写成摘要。

```
上一课（第 02 课 · 装备：开源件与自检基线）的产出：第 02 课交付了 `docs/00-toolchain.md` 和全绿自检输出，工具都装好了。
本课开头要接的点：工具是散的，得有一个仓库把它们串成流水线。这一课立骨架和契约。
本课结尾要抛出的问题：骨架有了，AI 能读你的契约复述流程。可它不知道你的账号该做什么内容、用什么口气说话。这些判断依据写在哪？
```

写完拿成稿的结尾对一遍这三行。**对不上不一定是错**——写着写着找到更好的收口是常事——但对不上就当场改 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/handoff-ledger.md` 里第 03 课那一行，让下一课拿到真实的交接点，并在「改过没」列打个勾。

## 6. 流程

跑 `/ai-arch-tutorial-style`（工程教程体写作 skill），六步流水线全走，只改一处：

- **Step 2、3 不出方案、不问人**：大纲照本卡「2.1 大纲」块展开成 outline.md，frontmatter 写 `plan: fixed-from-card`，局部调整在 outline.md 开头说明，节的数目和顺序不动。
- 其余步骤照原样：Step 1 材料盘点存 `analyzed.md`（材料不足 5 条不进下一步，材料清单在上面第 4 块）、Step 2、Step 4 写 `draft.md`、Step 5 派 subagent 做读者诊断存 `diagnosis.md`、Step 6 按清单修改 + 五遍终检出 `final.md` 和 `polish-report.md`。
- 中间产物落盘到 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/03-03/`，保证断点可续。

## 7. 验收与交付

三关自己跑完才算交稿：

1. **硬禁令清零**：`python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py <final.md>` 零命中（装腔词、过度自信词、标题模板前缀）。这是写作 skill Step 6 的 ⛔，不放宽。
2. **验收脚本跑绿**：`LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh <final.md 路径> 实操`。**汉字数已改成只报数不 FAIL**（用户口径：字数按内容动态平衡，不做卡控），看到 `[INFO] 汉字 ... 超出参考值` 是正常的，**不要为了这一行去砍字**。剩下仍是硬 FAIL 的项：代码行上限、实操篇围栏块 ≥3、排障句 ≥2、状态值核对。
3. **材料可溯**：文中出现的每个文件路径、命令、脚本名，能在材料清单给的文件里 grep 到。脚本的「命令与脚本名核对」那一节会报 `[WARN] 未在仓库找到`，逐条确认掉。

交付动作，两件都要做：

- 落盘 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/03-立骨架-仓库、目录、契约.md`
- **`final.md` 全文写进最终回复，不要只给路径**，回复里要能直接读。`polish-report.md` 一并附上（它记着五遍终检各改了什么，是后面全链承接核对的线索）。

## 8. 交付后

交稿后不做接力。把三样东西写进你的最终回复：成稿路径、check-lesson.sh 最后 12 行输出、未核实项清单（仓库里 grep 不到、已按规则删掉或改写的名词与数字）。主回话验收后决定下一课。

失败怎么办：硬禁令没清零、材料不足被 Step 1 拦下这类能自愈的，原地重试一次。重试仍失败就停下，在回复里写清卡在哪一步、报错原文，不要自己降低标准硬交。

---

## 共同约束（33 门课一致，不许改写）

> 本课程的目标不是教人做自媒体，是让学员通过这条流水线学会怎么跟 AI 干活：掌握和 AI 协作的流程、懂得如何引导 AI、掌握 Loop 思维与架构设计。抖音只是载体。
>
> 因此：**视频制作本身（怎么剪、怎么配音、怎么调多音字和语速、封面怎么画）一律从简，走一遍流程即可，不做深入讲解。** SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环这四块是学员要重点掌握的，往厚里写。
>
> 判断标准：这一段讲的是「这个工具怎么用」还是「这类问题该怎么设计」。前者压，后者留。
>
> 注意别误伤 —— 模块 2 那四课（07-10）表面在讲配音质检，实际讲的是 Loop 架构，配音只是素材，不属于可精简范围。

还有一条铁律贯穿全书：**学员不写一行实现代码。**学员写的只有三类东西——中文的需求与判据、SPEC 文档、验收结论。代码、配置、脚本一律由 AI 产出。课文里出现大段代码就说明写偏了。
字数不设限，心法类内容（SPEC、Loop、状态收敛、反哺）讲透为准，可以多讲。
