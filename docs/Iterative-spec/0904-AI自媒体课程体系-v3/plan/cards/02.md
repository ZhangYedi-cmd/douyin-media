# 第 02 课任务卡

> 来源：课程任务卡系统自动转换生成，2026-09-04。先读 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/00-写作背景包.md`，再读本卡。
> 本卡里凡是引用 `00-ADR.md` 章节号的地方，按 ADR 实际章节找；找不到就在回复的未核实项里说明。

## 1. 定位

- **模块**：模块 0 起步
- **课号**：第 02 课（全书 29 课，**严格串行 01 → 29**，阅读顺序即写作顺序）
- **教法档**：L1
- **验收脚本参数**：`实操`

## 2. 本课要讲什么

1. **全新课**。`00-ADR.md` §5「借开源与自建清单」那十项（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）的来源、安装命令、装好的判据。（原引用的 `04-补全计划` 本机不存在，这份清单已并入 ADR §5，改指到那里）
2. 每一项配一条自检命令和期望输出，凑成一份能一次跑完的自检基线。
3. **这一课的装机命令要从各项目官方 README 取**，仓库里只有 skill 目录和配置样例，不要凭印象写版本号和参数。

## 2.1 大纲

记忆锚点：十项工具装的不是十件事，是给后面每一课的动手环节先把地基铺平，装错一步后面全部对不上。

开篇契约：上一课交付了端到端流程图和人出现点清单，学员知道这条线有哪几站；本课输入是 `00-ADR.md` §5 借开源与自建清单那十项工具的名字和来源；本课产出两件：`docs/00-toolchain.md`（工具清单 + 安装命令 + 自检命令）、一份全绿的自检输出。

### 1. 基础运行环境：Claude Code、Node、uv、ffmpeg/ffprobe
- 核心判断：这四项是其余六项工具能跑起来的前提，版本装错，后面的自检会全数报错，所以第一个装、第一个验。
- 二级标题：分（讲三块）。1.1 Claude Code：装上并确认能对话；1.2 Node 20+ 与 npm、uv：脚本和包管理跑不动就卡在这一步；1.3 ffmpeg/ffprobe：处理音视频的地基
- 交付物：Prompt 块（让 AI 按官方文档装好并跑自检：Claude Code 官方安装文档、nodejs.org 官方文档、uv 用 WebFetch 读 `astral-sh/uv` 的 README、ffmpeg 用 WebFetch 读 ffmpeg.org 官方文档）+ 四项安装命令/自检命令对照表
- 学员此时手上有：（无仓库产物，`docs/00-walkthrough.md` 仅作流程背景，不作本节 Prompt 输入）
- 交接：地基装好了，下一节开始装三个真正要跑内容生产的开源 skill。

### 2. 内容生产开源件：web-video-presentation、baoyu-image-gen、agent-reach
- 核心判断：这三个 skill 各自独立，装完各自能跑出一次自检结果，不依赖彼此，装错了只影响它自己那一段。
- 二级标题：分（讲三块）。2.1 web-video-presentation：网页转成片的引擎；2.2 baoyu-image-gen：封面生成；2.3 agent-reach：选题情报抓取
- 交付物：Prompt 块（分别装三项并自检，命令来源各自 GitHub README，用 WebFetch 分别读 `ConardLi/garden-skills`、`JimLiu/baoyu-skills`、`Panniantong/agent-reach` 三份 README，来源见 `00-ADR.md` §5）+ 三项安装命令/自检命令对照表
- 学员此时手上有：第 1 节装好的 Claude Code / Node / uv 环境（这三个 skill 都要靠它们跑）
- 交接：三个开源 skill 装完了，下一节看涉及外部账号和余额的那几项——它们装的同时还要准备申请材料。

### 3. 发布与配音：social-auto-upload、飞书自建应用、TTS 引擎
- 核心判断：这三项不是装完就完事，还要准备账号或余额，装的动作和申请材料要一起走，不然自检会卡在服务不可用而不是没装对。
- 二级标题：分（讲三块）。3.1 social-auto-upload：发布引擎；3.2 飞书自建应用：申请配置；3.3 TTS 引擎：MiniMax 或 OpenAI，脚手架自带 edge-tts 免费配方
- 交付物：Prompt 块（装 social-auto-upload 并自检，用 WebFetch 读 `https://github.com/dreammis/social-auto-upload` 的 README；飞书自建应用按 `tools/feishu-bot/config.example.yaml` 的六步申请说明走，不额外 WebFetch；TTS 引擎按 `.claude/skills/tts-dub/tts.config.example.json` 的字段形状配好，走一次最小合成自检）+ 三项对照表
- 学员此时手上有：第 1 节的基础环境（social-auto-upload 底层是 uv+py3.12 环境，需要 uv 已装好）
- 交接：十项工具和账号都装齐了，最后一步是把前面三节的自检命令拼成一份能一次跑完的基线。

### 4. 自检基线：一次跑完，全绿
- 核心判断：单项自检都过不等于流水线能跑，得把十项自检命令拼成一份能一次跑完的清单，落成 `docs/00-toolchain.md`，全绿才算装完。
- 二级标题：不分
- 交付物：Prompt 块（让 AI 汇总前三节的安装命令 + 自检命令，生成 `docs/00-toolchain.md`，并跑一遍自检拿到全绿输出）
- 学员此时手上有：前三节各自产出的安装记录和自检命令
- 本课做到了什么、还看不到什么、下一课补什么：十项工具装齐验证过了，但它们各自为政，没有一个仓库把它们串起来；下一课立骨架仓，把它们串成流水线。
- 结尾抛出的问题：工具装齐了，但它们各自为政，没有一个仓库把它们串起来。这套流水线的骨架长什么样？

## 3. 学员交付

这一课结束时，学员手上要多出来的可落盘的东西：

`docs/00-toolchain.md`（工具清单 + 安装命令 + 自检命令）+ 一份全绿的自检输出

## 4. 材料清单

写作 skill 的 Step 1 有硬门槛：**材料不足 5 条不进下一步**。以下路径都相对仓库根目录 `/Users/yedizhang/yedi-study/douyin-media`，逐条实际打开读，不要写「参考仓库」：

- **上一课（第 01 课 · 一条内容的端到端走查）的成稿全文**：`docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/01-一条内容的端到端走查.md`（等价物 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/01-*/final.md`）。**这是承接的第一真相源**，动笔前先读它的开篇与结尾两段。
- `00-ADR.md` §5 借开源与自建清单（十项工具来源表；原引用的「§2.1」在 ADR 里不存在，已改指到实际存在的 §5）
- `.claude/skills/web-video-presentation/SKILL.md`、`.claude/skills/tts-dub/SKILL.md`、`.claude/skills/douyin-ideate/SKILL.md`、`.claude/skills/douyin-publish/SKILL.md` 四个开源件目录
- GitHub `dreammis/social-auto-upload` 的 README（用 WebFetch 读 https://github.com/dreammis/social-auto-upload ；本机 `tools/social-auto-upload/README.md` 不存在）
- `tools/feishu-bot/config.example.yaml`（六步申请配置说明）与 `requirements.txt`
- `brain/tts.config.json`（MiniMax 配置形状）
- `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses/mini-v3-02.md`（另一台机器写过的同课成稿，只当素材看口径，不能抄）

## 5. 承接

**承接的第一真相源是上一课的成稿，不是下面这三行。**下面三行是计划里的预期，用来校验，不用来代替原文。

动笔前先读第 01 课的 `final.md`，开篇那句「上一课把 X 做完了」要按它实际写了什么复述，具体到产物名、数量、判据这一级，不要写成摘要。

```
上一课（第 01 课 · 一条内容的端到端走查）的产出：第 01 课交付了端到端流程图和人出现点清单，学员知道这条线有哪几站。
本课开头要接的点：流程图上每一站都要一个工具支撑，这一课把工具装齐并验证装对了。
本课结尾要抛出的问题：工具装齐了，但它们各自为政，没有一个仓库把它们串起来。这套流水线的骨架长什么样？
```

写完拿成稿的结尾对一遍这三行。**对不上不一定是错**——写着写着找到更好的收口是常事——但对不上就当场改 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/handoff-ledger.md` 里第 02 课那一行，让下一课拿到真实的交接点，并在「改过没」列打个勾。

## 6. 流程

跑 `/ai-arch-tutorial-style`（工程教程体写作 skill），六步流水线全走，只改一处：

- **Step 2、3 不出方案、不问人**：大纲照本卡「2.1 大纲」块展开成 outline.md，frontmatter 写 `plan: fixed-from-card`，局部调整在 outline.md 开头说明，节的数目和顺序不动。
- 其余步骤照原样：Step 1 材料盘点存 `analyzed.md`（材料不足 5 条不进下一步，材料清单在上面第 4 块）、Step 2、Step 4 写 `draft.md`、Step 5 派 subagent 做读者诊断存 `diagnosis.md`、Step 6 按清单修改 + 五遍终检出 `final.md` 和 `polish-report.md`。
- 中间产物落盘到 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/02-02/`，保证断点可续。

## 7. 验收与交付

三关自己跑完才算交稿：

1. **硬禁令清零**：`python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py <final.md>` 零命中（装腔词、过度自信词、标题模板前缀）。这是写作 skill Step 6 的 ⛔，不放宽。
2. **验收脚本跑绿**：`LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh <final.md 路径> 实操`。**汉字数已改成只报数不 FAIL**（用户口径：字数按内容动态平衡，不做卡控），看到 `[INFO] 汉字 ... 超出参考值` 是正常的，**不要为了这一行去砍字**。剩下仍是硬 FAIL 的项：代码行上限、实操篇围栏块 ≥3、排障句 ≥2、状态值核对。
3. **材料可溯**：文中出现的每个文件路径、命令、脚本名，能在材料清单给的文件里 grep 到。脚本的「命令与脚本名核对」那一节会报 `[WARN] 未在仓库找到`，逐条确认掉。

交付动作，两件都要做：

- 落盘 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/02-装备-开源件与自检基线.md`
- **`final.md` 全文写进最终回复，不要只给路径**，回复里要能直接读。`polish-report.md` 一并附上（它记着五遍终检各改了什么，是后面全链承接核对的线索）。

## 8. 交付后

交稿后不做接力。把三样东西写进你的最终回复：成稿路径、check-lesson.sh 最后 12 行输出、未核实项清单（仓库里 grep 不到、已按规则删掉或改写的名词与数字）。主回话验收后决定下一课。

失败怎么办：硬禁令没清零、材料不足被 Step 1 拦下这类能自愈的，原地重试一次。重试仍失败就停下，在回复里写清卡在哪一步、报错原文，不要自己降低标准硬交。

---

## 共同约束（33 门课一致，不许改写）

> 本课程的目标不是教人做自媒体，是让学员通过这条流水线学会怎么跟 AI 干活：掌握和 AI 协作的流程、懂得如何引导 AI、掌握 Loop 思维与架构设计。抖音只是载体。
>
> 因此：**视频制作本身（怎么剪、怎么配音、怎么调多音字和语速、封面怎么画）一律从简，走一遍流程即可，不做深入讲解。** SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环这四块是学员要重点掌握的，往厚里写。
>
> 判断标准：这一段讲的是「这个工具怎么用」还是「这类问题该怎么设计」。前者压，后者留。
>
> 注意别误伤 —— 模块 2 那四课（07-10）表面在讲配音质检，实际讲的是 Loop 架构，配音只是素材，不属于可精简范围。

还有一条铁律贯穿全书：**学员不写一行实现代码。**学员写的只有三类东西——中文的需求与判据、SPEC 文档、验收结论。代码、配置、脚本一律由 AI 产出。课文里出现大段代码就说明写偏了。
字数不设限，心法类内容（SPEC、Loop、状态收敛、反哺）讲透为准，可以多讲。
