# 第 20 课任务卡

> 来源：课程任务卡系统自动转换生成，2026-09-04。先读 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/00-写作背景包.md`，再读本卡。
> 本卡里凡是引用 `00-ADR.md` 章节号的地方，按 ADR 实际章节找；找不到就在回复的未核实项里说明。

## 1. 定位

- **模块**：模块 4 交付闸口
- **课号**：第 20 课（全书 29 课，**严格串行 01 → 29**，阅读顺序即写作顺序）
- **教法档**：L2
- **验收脚本参数**：`实操`

## 2. 本课要讲什么

1. 旧 08 课 + **补 social-auto-upload 的安装与抖音扫码登录**（uv 建环境、cookie 存哪、登录态怎么验）。
2. `douyin-publish` 按 L2 封装：一张规格表（学员填）+ 两三条 prompt。
3. **dry-run 铁律**：先打出最终命令和物料给人确认，人授权后才真发。发布不可逆。
4. `4-publish.md` 模板全文，发布收尾是唯一记账点。

## 2.1 大纲

记忆锚点：发布前最后一次确认，不是走流程，是把要发的东西原样摆给人看一遍。

开篇契约：上一课交付了能推卡能收按钮的飞书服务，人审闸口通了；本课输入是 `.claude/skills/douyin-publish/SKILL.md` 与 `references/`（`field-mapping.md`、`sau-setup.md`）、`dreammis/social-auto-upload` 的 README、`pipeline/4-publish.md` 全文、`pipeline/lessons.md` L5 到 L8、`tools/console/packages/cli/src/commands/publishDone.ts`；本课产出一个 `douyin-publish` skill + 一次真发或 dry-run 到底。

### 1. 登录抖音：环境已经装好，这一课只做账号绑定
- 核心判断：social-auto-upload 在第 2 课已经进了工具清单、装好了环境（`uv` + Python 虚拟环境 + 浏览器内核）；这一课不重装，只做账号相关的事——cookie 存哪、扫码登录怎么做、登录态怎么验，这些是每个账号各自的动作，不属于通用工具安装。
- 二级标题：不分。
- 交付物：登录与验证命令清单（`sau douyin login --account main --headed` 扫码登录 → `sau douyin check --account main` 验证 `valid`）。
- 学员此时手上有：第 2 课交付的 `docs/00-toolchain.md` 里 social-auto-upload 那一条安装记录和全绿自检输出。
- 交接：账号登录验证过了，接下来把发布这件事封装成一个能被飞书服务调用的 skill。

### 2. douyin-publish 封装：一张规格表定映射
- 核心判断：这是 L2 的活，不是一条 Prompt 钉死，是先让学员填一张规格表（content 条目字段怎么映射成 sau 参数），再用两三条 Prompt 让 AI 按表实现 skill。
- 二级标题：不分。
- 交付物：字段映射规格表（sau 参数 / 来源字段 / 说明）+ 两条 Prompt（照 `4-publish.md` 的物料字段实现读取与校验逻辑；把校验结果拼成完整 sau 命令与物料摘要）。
- 学员此时手上有：上一节验证过的登录态；`pipeline/4-publish.md` 定义的发布物料字段（标题 / 正文 / 话题标签 / 封面 / 建议发布时段 / 媒体文件）。
- 交接：skill 能拼命令了，命令拼完之后能不能直接执行，是这一课真正的红线。

### 3. dry-run 铁律：先摆出来，人点头才发
- 核心判断：发布不可逆，所以默认就是 dry-run——不带发布信号只拼出完整命令和物料摘要给人看、绝不执行；只有带上发布信号（等于人已经在飞书确认发布卡点过头）才真发。不是"先 dry-run 再问一遍"，是这一个信号本身就代表已经拿到授权，真发时不再重复确认。
- 二级标题：不分。
- 交付物：dry-run / 真发两条路径的判据表 + 一条 Prompt（验证 dry-run 路径只拼命令不执行）。
- 学员此时手上有：第 19 课交付的确认发布卡（人点下去带的就是这个授权信号）。
- 交接：命令能安全拼出来、安全发出去了，最后要把这次发布记进真相源。

### 4. 记账收口：发布成功之后喊 media publish-done
- 核心判断：发布收尾是全流水线状态记账的唯一位置，不再手改 `meta.yaml` 或 `backlog.yaml`——发布成功后直接喊 `media publish-done <slug>`，第 12 到 16 课造的这个入口在这里第一次真正派上用场；失败则原样重试一次（`pipeline/lessons.md` L7、L8 记的两次真实失败都是重跑一次就过），仍失败才转人工，且不调用 `publish-done`。
- 二级标题：不分。
- 交付物：`pipeline/4-publish.md` 模板字段全文 + `media publish-done` 用法（含 `--scheduled`、`--url`）。
- 学员此时手上有：第 14、15 课交付的全量 `media` 命令（含 `publish-done`）。
- 交接：安全发布的技术条件都齐了，剩下是真的按下去。

### 5. 真发或 dry-run 到底
- 本课做到了什么、还看不到什么、下一课补什么：有真实抖音号的学员，这里可以走完一次真发，看着 `media publish-done` 把状态和链接一次记完；没有号的，dry-run 走到底、停在完整命令和物料摘要这一步，同样算完成这一课，不因为没账号卡住。生产线从取题到发布整条通了，但它现在只会消费资产。
- 结尾抛出的问题：生产线从取题到发布整条通了，可它只会消费资产：选题池会空，大脑不会自己长。谁来保养这些资产，按什么纪律改？

## 3. 学员交付

这一课结束时，学员手上要多出来的可落盘的东西：

一个 `douyin-publish` skill + 一次真发或 dry-run 到底

## 4. 材料清单

写作 skill 的 Step 1 有硬门槛：**材料不足 5 条不进下一步**。以下路径都相对仓库根目录 `/Users/yedizhang/yedi-study/douyin-media`，逐条实际打开读，不要写「参考仓库」：

- **上一课（第 19 课 · 飞书实现与按钮回流）的成稿全文**：`docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/19-飞书实现与按钮回流.md`（等价物 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/19-*/final.md`）。**这是承接的第一真相源**，动笔前先读它的开篇与结尾两段。
- `.claude/skills/douyin-publish/SKILL.md` 与 `references/`
- GitHub `dreammis/social-auto-upload` 的 README（用 WebFetch 读 https://github.com/dreammis/social-auto-upload ；本机 `tools/social-auto-upload/README.md` 不存在）
- `pipeline/4-publish.md` 全文
- `pipeline/lessons.md` L5 到 L8
- 旧稿 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses/v1-09-包装social-auto-upload发布Skill.md`（本机没有 `v2-08-发布Skill与dry-run.md`，已按主题改指向 18 课旧稿里同主题的一篇，只当素材看口径，不能照搬其结论）
- `tools/console/packages/cli/src/commands/publishDone.ts`（发布收尾唯一记账）

## 5. 承接

**承接的第一真相源是上一课的成稿，不是下面这三行。**下面三行是计划里的预期，用来校验，不用来代替原文。

动笔前先读第 19 课的 `final.md`，开篇那句「上一课把 X 做完了」要按它实际写了什么复述，具体到产物名、数量、判据这一级，不要写成摘要。

```
上一课（第 19 课 · 飞书实现与按钮回流）的产出：第 19 课交付了能推卡能收按钮的飞书服务，人审闸口通了。
本课开头要接的点：人点了头，接下来是全流水线唯一一个真正不可逆的动作。这一课把它做安全。
本课结尾要抛出的问题：生产线从取题到发布整条通了，可它只会消费资产：选题池会空，大脑不会自己长。谁来保养这些资产，按什么纪律改？
```

写完拿成稿的结尾对一遍这三行。**对不上不一定是错**——写着写着找到更好的收口是常事——但对不上就当场改 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/handoff-ledger.md` 里第 20 课那一行，让下一课拿到真实的交接点，并在「改过没」列打个勾。

## 6. 流程

跑 `/ai-arch-tutorial-style`（工程教程体写作 skill），六步流水线全走，只改一处：

- **Step 3（大纲）改为 AI 自选**：直接选定一个大纲方案继续往下写，不停下来等人选。选定理由写进 `outline.md` 一段话。
- 其余步骤照原样：Step 1 材料盘点存 `analyzed.md`（材料不足 5 条不进下一步，材料清单在上面第 4 块）、Step 2、Step 4 写 `draft.md`、Step 5 派 subagent 做读者诊断存 `diagnosis.md`、Step 6 按清单修改 + 五遍终检出 `final.md` 和 `polish-report.md`。
- 中间产物落盘到 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/20-20/`，保证断点可续。

## 7. 验收与交付

三关自己跑完才算交稿：

1. **硬禁令清零**：`python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py <final.md>` 零命中（装腔词、过度自信词、标题模板前缀）。这是写作 skill Step 6 的 ⛔，不放宽。
2. **验收脚本跑绿**：`LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh <final.md 路径> 实操`。**汉字数已改成只报数不 FAIL**（用户口径：字数按内容动态平衡，不做卡控），看到 `[INFO] 汉字 ... 超出参考值` 是正常的，**不要为了这一行去砍字**。剩下仍是硬 FAIL 的项：代码行上限、实操篇围栏块 ≥3、排障句 ≥2、状态值核对。
3. **材料可溯**：文中出现的每个文件路径、命令、脚本名，能在材料清单给的文件里 grep 到。脚本的「命令与脚本名核对」那一节会报 `[WARN] 未在仓库找到`，逐条确认掉。

交付动作，两件都要做：

- 落盘 `docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/20-发布Skill与dry-run.md`
- **`final.md` 全文写进最终回复，不要只给路径**，回复里要能直接读。`polish-report.md` 一并附上（它记着五遍终检各改了什么，是后面全链承接核对的线索）。

## 8. 交付后

交稿后不做接力。把三样东西写进你的最终回复：成稿路径、check-lesson.sh 最后 12 行输出、未核实项清单（仓库里 grep 不到、已按规则删掉或改写的名词与数字）。主回话验收后决定下一课。

失败怎么办：硬禁令没清零、材料不足被 Step 1 拦下这类能自愈的，原地重试一次。重试仍失败就停下，在回复里写清卡在哪一步、报错原文，不要自己降低标准硬交。

---

## 共同约束（33 门课一致，不许改写）

> 本课程的目标不是教人做自媒体，是让学员通过这条流水线学会怎么跟 AI 干活：掌握和 AI 协作的流程、懂得如何引导 AI、掌握 Loop 思维与架构设计。抖音只是载体。
>
> 因此：**视频制作本身（怎么剪、怎么配音、怎么调多音字和语速、封面怎么画）一律从简，走一遍流程即可，不做深入讲解。** SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环这四块是学员要重点掌握的，往厚里写。
>
> 判断标准：这一段讲的是「这个工具怎么用」还是「这类问题该怎么设计」。前者压，后者留。
>
> 注意别误伤 —— 模块 2 那四课（07-10）表面在讲配音质检，实际讲的是 Loop 架构，配音只是素材，不属于可精简范围。

还有一条铁律贯穿全书：**学员不写一行实现代码。**学员写的只有三类东西——中文的需求与判据、SPEC 文档、验收结论。代码、配置、脚本一律由 AI 产出。课文里出现大段代码就说明写偏了。
字数不设限，心法类内容（SPEC、Loop、状态收敛、反哺）讲透为准，可以多讲。
