# 学员产物累计清单

本表第 N 节是第 1 到第 N 课交付物的**累计**，不是本课新增。每节按五类列出学员手上具体有什么，名字具体到文件名、命令名、skill 名。写作代理据此判断某课某条 Prompt 的输入，学员手上是否已经有。

## 第 01 课结束时（一条内容的端到端走查）

- **仓库文件与目录**：`docs/00-walkthrough.md`
- **命令**：（无）
- **skill与子代理**：（无）
- **文档（SPEC、契约、brain）**：（无）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）

## 第 02 课结束时（装备：开源件与自检基线）

- **仓库文件与目录**：`docs/00-toolchain.md`
- **命令**：（无）
- **skill与子代理**：（无）
- **文档（SPEC、契约、brain）**：（无）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出

## 第 03 课结束时（立骨架：仓库、目录、契约）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部
- **命令**：（无）
- **skill与子代理**：（无）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）

## 第 04 课结束时（账号大脑：五份文件怎么写）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部
- **命令**：（无）
- **skill与子代理**：（无）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准

## 第 05 课结束时（用开源Skill跑通一条内容）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准

## 第 06 课结束时（验证信号流向谁）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完

## 第 07 课结束时（反馈信号从哪来）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定

## 第 08 课结束时（造出六个检测脚本）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定

## 第 09 课结束时（裁判与选手分离）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）

## 第 10 课结束时（自愈回路与三轮熔断）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）

## 第 11 课结束时（SPEC方法：从调研到验证的六步）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）

## 第 12 课结束时（media调研与选型讨论）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）

## 第 13 课结束时（media写SPEC）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定
- **命令**：（无）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）

## 第 14 课结束时（执行M1：core与首批命令）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）
- **命令**：`media` 读命令（st、next、backlog ls；check 和 next-up 留到第 15 课）、`media publish-done`、`media promote`
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律

## 第 15 课结束时（执行M2：全量命令与存量迁移）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛

## 第 16 课结束时（验证与收敛）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）

## 第 17 课结束时（人审闸口：闸设在哪）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者

## 第 18 课结束时（飞书通道SPEC）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）

## 第 19 课结束时（飞书实现与按钮回流）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`

## 第 20 课结束时（发布Skill与dry-run）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；登录态（cookies 文件）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）

## 第 21 课结束时（双线闭环与资产读写表）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段

## 第 22 课结束时（复盘归因反哺大脑）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；`docs/Iterative-spec/0904-AI自媒体课程体系-v3/appendix/22-复盘脱敏样例数据.md`（第 22 课附录，四条脱敏样例数据 + 账号历史均值基线，供没有真实播放数据的学员练归因）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（已核实：`tools/console/packages/cli/src/commands/metricsRecord.ts` 真实存在，`--window 24h|72h|7d` + `--json-data`，每次 append `harness/logs/metrics.jsonl` 一行，仅 `7d` 窗口回填 `backlog.yaml` 对应条目的 `metrics` 字段）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（采集/记账/产出三段 `SKILL.md`，登录鉴权复用 `douyin-publish` 的方式；不含具体脚本文件名，内部实现由 AI 决定）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告（真实数据或第 22 课附录脱敏样例）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注四道转化，各对应不同打法问题）；小样本不归因（播放量三位数漏斗百分比是噪音）、改基线需≥2条同向证据（均引自参考流水线任务卡）；反哺大环第二段（单次归因）；记账收口（`media flip <slug> retro_done`）与改大脑判断分属两条不同判据的边界

## 第 23 课结束时（选题池与外网情报）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`pipeline/5-retro.md` 模板全文、一份真实复盘报告
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界

## 第 24 课结束时（治理总调度与自审元层）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`pipeline/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（自建应用配置、卡片结构、3 秒回调窗口、命名空间过滤）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）

## 第 25 课结束时（无头作业：进程流归一与终局裁决）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表；`cc-stream` 包（六个文件）；裁决函数 + 收尾策略实现
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）；三层解析（原始 JSON 行→归一事件→里程碑）；七种归一事件、三档信任分级；`claude -p` 无头跑的坑（认证/超时/输出流）；只认文件的裁决（不信退出码、不信 AI 自述）；单并发与锁；SIGTERM 先给收尾机会，不退再 SIGKILL；人工取消/服务重启后的恢复规则

## 第 26 课结束时（读设计稿，定契约）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表；`cc-stream` 包（六个文件）；裁决函数 + 收尾策略实现；`docs/design/` 九页原型 + `theme.css`（课程附带材料）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表；`docs/spec/console/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）；三层解析（原始 JSON 行→归一事件→里程碑）；七种归一事件、三档信任分级；`claude -p` 无头跑的坑（认证/超时/输出流）；只认文件的裁决（不信退出码、不信 AI 自述）；单并发与锁；SIGTERM 先给收尾机会，不退再 SIGKILL；人工取消/服务重启后的恢复规则；「原型降级为设计稿，抄规格不抄代码」；五包一仓边界与 API 契约；`review.html`、`harness-task-detail.html` 明确不在 1:1 范围（下一期内容）

## 第 27 课结束时（后端：读写两条路径）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表；`cc-stream` 包（六个文件）；裁决函数 + 收尾策略实现；`docs/design/` 九页原型 + `theme.css`（课程附带材料）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）；六个页面快照读接口、`file`/`asset`/`health` 接口、SSE 推送；快写 4 条（`execFile media <子命令>`）、慢作业 3 条（job runner，202 + jobId）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表；`docs/spec/console/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）；三层解析（原始 JSON 行→归一事件→里程碑）；七种归一事件、三档信任分级；`claude -p` 无头跑的坑（认证/超时/输出流）；只认文件的裁决（不信退出码、不信 AI 自述）；单并发与锁；SIGTERM 先给收尾机会，不退再 SIGKILL；人工取消/服务重启后的恢复规则；「原型降级为设计稿，抄规格不抄代码」；五包一仓边界与 API 契约；`review.html`、`harness-task-detail.html` 明确不在 1:1 范围（下一期内容）；SSE 推变更三件配合（store/watcher/revision）；读接口一律走 core 的 `parsers`；`server` 禁止直接写文件，一律经 `media`；飞书回调改走这条写路径（第 19 课的 `server.py` 不再自己写）

## 第 28 课结束时（前端：六页整站与联调）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表；`cc-stream` 包（六个文件）；裁决函数 + 收尾策略实现；`docs/design/` 九页原型 + `theme.css`（课程附带材料）；AppShell、快照 hook、SSE 订阅 hook、P1 总览页、P3 详情页；P4 选题池页、P5 治理页、P2 看板页、P6 数据页、单端口整站产物（前端构建产物由 server 托管）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）；六个页面快照读接口、`file`/`asset`/`health` 接口、SSE 推送；快写 4 条（`execFile media <子命令>`）、慢作业 3 条（job runner，202 + jobId）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表；`docs/spec/console/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；一份验收输出
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）；三层解析（原始 JSON 行→归一事件→里程碑）；七种归一事件、三档信任分级；`claude -p` 无头跑的坑（认证/超时/输出流）；只认文件的裁决（不信退出码、不信 AI 自述）；单并发与锁；SIGTERM 先给收尾机会，不退再 SIGKILL；人工取消/服务重启后的恢复规则；「原型降级为设计稿，抄规格不抄代码」；五包一仓边界与 API 契约；`review.html`、`harness-task-detail.html` 明确不在 1:1 范围（下一期内容）；SSE 推变更三件配合（store/watcher/revision）；读接口一律走 core 的 `parsers`；`server` 禁止直接写文件，一律经 `media`；飞书回调改走这条写路径（第 19 课的 `server.py` 不再自己写）；theme token 从 `docs/design/css/theme.css` 映射，不重新配色；全站验收判据（六页能看、两条写路径都通，模块 7 出口条件）

## 第 29 课结束时（收官：形态坐标与 1:1 验收）

- **仓库文件与目录**：`docs/00-toolchain.md`；目录树骨架（`pipeline/`、`content/`、`brain/`、`harness/`、`tools/`、`docs/`）；`CLAUDE.md`；`content/_template/` 六份模板文件（含 `meta.yaml`）；`content/_backlog/backlog.yaml` 头部；一条内容的 `content/<slug>/build/` 工程目录；`final.mp4`；竖版 `cover.png`；字幕层（`?subs=1`）；`docs/spec/` 目录约定；core 包三件：`parsers`、`state`、`writer`（含往返测试）；飞书自建应用配置六步产物（App ID/Secret、机器人拉群、回调订阅长连接、Verification Token、config）；`notify.py`（出站发卡）、`cards.py`（卡片结构）、`server.py`（长连接收按钮）；`harness/README.md`、`harness/tasks.md`、`harness/report-template.md`（`harness/` 骨架）；一批入池候选（写入 `backlog.yaml`）；一份任务注册表；`cc-stream` 包（六个文件）；裁决函数 + 收尾策略实现；`docs/design/` 九页原型 + `theme.css`（课程附带材料）；AppShell、快照 hook、SSE 订阅 hook、P1 总览页、P3 详情页；P4 选题池页、P5 治理页、P2 看板页、P6 数据页、单端口整站产物（前端构建产物由 server 托管）
- **命令**：`media`（全部读命令）、`media publish-done`、`media promote`；全量 `media` 命令（其余写命令 + `media check`）；`media metrics record`（待核：仅见于第 22 课卡「转换时发现的问题」附注，非正文材料清单）；六个页面快照读接口、`file`/`asset`/`health` 接口、SSE 推送；快写 4 条（`execFile media <子命令>`）、慢作业 3 条（job runner，202 + jobId）
- **skill与子代理**：`tts-dub` skill（配置契约 + 合成脚本）；`dubbing-check` skill 全量（六个检测脚本，含 `check-silence.mjs`；`depause.mjs`；`SKILL.md`）；`dubbing-reviewer.md` 子代理定义；`feishu-notify` skill（出站发卡 + 长连接收按钮闭环）；`douyin-publish` skill（薄封装、含 dry-run 铁律）；`douyin-retro` skill（含 `fetch.py`）；`douyin-ideate` skill（含 scoring、report 两份 references）；`harness-dispatcher`、`backlog-gardener`、`benchmark-refresher`、`account-audit`（四个治理 skill）
- **文档（SPEC、契约、brain）**：`pipeline/1-ideate.md`、`pipeline/2-create.md`、`pipeline/3-review.md`、`pipeline/4-publish.md`、`pipeline/daily-run.md`（五份阶段契约）；`brain/positioning.md`、`brain/persona.md`、`brain/style-guide.md`、`brain/sources.md`（四份填满）、`brain/benchmarks.md`（留空，待第 22 课复盘后写）；`brain/tts.config.json`；配音批次检查点表（六项：量什么/怎么量/阈值/谁来判）；一份真实判决输出（PASS/FAIL 裁决口径表）；一次真实 FAIL → 修 → 重合成 → PASS 全过程记录；六步模板四件套：调研模板、ADR 模板（决策/理由/被否方案/反悔成本四项固定）、执行方案模板（四节）、施工记录模板；`docs/spec/media/00-调研.md`、`docs/spec/media/01-ADR.md`；`docs/spec/media/02-执行方案.md`（14 条命令逐条穷举、状态迁移表、M1/M2 里程碑切分）；存量迁移记录；`docs/spec/media/03-施工记录.md`、一份验收输出；一张不可逆动作表（发出去收不回/花钱/对外可见/覆盖真相源）、一份卡片字段草案；`docs/spec/feishu-gate/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；`pipeline/4-publish.md` 模板全文（发布收尾为唯一记账点）；一次真发或 dry-run 到底的执行记录；一张资产读写表、`line` 字段（生产线/治理线）分账约定；`content/_template/5-retro.md` 模板全文、一份真实复盘报告；一轮巡检记账、调参限幅表；`docs/spec/console/00-调研.md`、`01-ADR.md`、`02-执行方案.md`（三份）；一份验收输出；一张形态坐标表、一份撤档判据；一份 1:1 验收报告
- **概念与判据**：端到端流程图（一张）；「人出现在哪几个点」清单（一份）；`slug`、`dry-run` 两词的就地解释；终检闸 A-G 索引表（七项判据对应课号）；十项工具清单及各自安装命令/自检命令/判据（Claude Code、Node 20+/npm、uv、ffmpeg/ffprobe、web-video-presentation、baoyu-image-gen、agent-reach、social-auto-upload、飞书自建应用、MiniMax TTS）；一份全绿自检输出；状态记账方式：手改 `meta.yaml` 记账（临时手段，第 15 课收敛）；「不许出现判不了的词」审校标准；四位置模型表（验证信号流向人/机器/文档/没人管），第 4 列填完；三条判据（能写成断言/只能靠人看/现在写不了但以后能写）；六项出错点归属；`overrides`（逐段覆盖）落点约定；判改分离约束（裁判只读：Bash/Read/Grep/Glob，无 Edit/Write；改由主回话做，最多三轮）；自愈回路 + 三轮熔断规则（三轮不过即停、上报）；SPEC 六步方法（调研→讨论选型→决策→写 SPEC→执行→验证）；「讨论选型」步的辩护式提问法；里程碑出口条件写法（一句能跑的验证动作）；四组互斥路线的决策（入口形态/YAML 写法/并发保护/派生视图）；学员验收出口条件、不自己改代码的验收纪律；`check` 八条规则（跨文件漂移怎么问出来、退出码怎么返）；状态记账唯一入口收敛为 `media`，手改 `meta.yaml` 正式收敛；`--dry-run` 先出计划再执行；`audit.jsonl` 当证据；写路径唯一性自证方法；`media check` 在自己仓库跑通并返退出码（模块 3 出口条件）；闸口出边落到状态机：`review` 状态三条出边及各自触发者；四个决策点（长连接 vs webhook、卡片结构、3 秒回调窗口、命名空间隔离）；按钮落到 `state.ts` 三条出边（通过/打回重做/打回挂待办）；自动重做熔断上限；同步路径只做内存操作，3 秒回调窗口内返回；`server` 不直接改文件，状态写入必须经 `media`；dry-run 铁律（先出最终命令和物料给人确认，人授权才真发）；治理线「只提议不落笔」铁律；反哺大环第一段；传播漏斗归因（曝光/点击/完播/互动/关注）；反哺大环第二段（单次归因）；选题池数据契约（`backlog.yaml` 写法、`next_up` 指针、过期条目清扫规则）；情报只从外网来、不爬抖音的边界；触发条件评估（事件窗口/周期/加权池）；反哺大环第三段（自审作用在治理线自己身上）；三层解析（原始 JSON 行→归一事件→里程碑）；七种归一事件、三档信任分级；`claude -p` 无头跑的坑（认证/超时/输出流）；只认文件的裁决（不信退出码、不信 AI 自述）；单并发与锁；SIGTERM 先给收尾机会，不退再 SIGKILL；人工取消/服务重启后的恢复规则；「原型降级为设计稿，抄规格不抄代码」；五包一仓边界与 API 契约；`review.html`、`harness-task-detail.html` 明确不在 1:1 范围（下一期内容）；SSE 推变更三件配合（store/watcher/revision）；读接口一律走 core 的 `parsers`；`server` 禁止直接写文件，一律经 `media`；飞书回调改走这条写路径（第 19 课的 `server.py` 不再自己写）；theme token 从 `docs/design/css/theme.css` 映射，不重新配色；全站验收判据（六页能看、两条写路径都通，模块 7 出口条件）；形态坐标（脚本集合/工作流/agent 系统）；哪些出口关了会退化；五条判据：①生产线跑得通；②治理线四个任务能被同一个调度器派活；③状态写入口唯一，`media check` 能问出漂移并返退出码；④控制台单端口起站六页可看两条写路径都通；⑤学员从头到尾没有手写过一行实现代码
