# 润色报告：第 21 课《双线分工：生产线消费资产，治理线保养资产》

## 一、diagnosis.md 逐条处理

诊断由 sonnet 子代理对 draft.md 通读后返回，六个维度逐条处理如下。

### 1. 读不懂
- **daily-run.md 范围指代不清**（Prompt 1）：原句「读我自己的 CLAUDE.md、pipeline/ 五份阶段契约和 daily-run.md」把 daily-run.md 单独点出，读起来像五份契约之外的第六份文件。已改成「读我自己的 CLAUDE.md 和 pipeline/ 五份阶段契约（含 daily-run.md）」，明确 daily-run.md 就是五份里的一份。
- **「已发布线上资产」未界定**：第 2 节首次出现处补了括注「也就是抖音平台上那条已经发出去的视频本体，不是本课资产读写表要管的仓库文件」，说明它和第 4 节读写表覆盖的仓库文件是两类东西，不指望表里能找到对应行。

### 2. 太啰唆
- 「让系统自己改自己判断对错的标准」六行内重复出现两次，已删掉第一处的重复表述，只在给出完整推理的那句保留。
- 结尾「骨架立住了 + 下一课补复盘 + 台账第 21 行原句」三次提复盘，已删掉中间「下一课补的是第一个治理任务，复盘」这句过渡，直接从「还没有一个真正跑起来的治理任务」接到台账原句，复盘只提两次。

### 3. 逻辑断裂
- **「harness 边只有两条例外」与第 4 节自相矛盾**：第 3 节原来只举了 `retro_done`、`expired` 两条 `line: harness` 的边，但第 4 节读写表里已经在用第三条边（`idea` 到 `archived`，经 `media backlog apply`）。已改成「一共三条」，把 `archived` 这条边补进第 3 节的叙述和小表，和第 4 节的举例对上。
- **backlog-gardener 例子自相矛盾**：原句「判定是疑似撞题、建议人审复核」和「报告结尾写的是全池干净，无需处理」两句面上矛盾。已补上真实报告里省略掉的推理过程——两组重叠分别是「已自述错峰理由的有意差异化」和「标签体系不够细导致的假重叠」，逐组查完才给出「全池干净」的结论，矛盾消除。
- **读写表 meta.yaml 行混淆「要不要理由」和「要不要人审」**：原句用「除打回 rejected 外不需要理由」回答「是否需要人审」，两个概念不是一回事。已重写成分别说清楚：生产线翻转走的是第 17 课定的审核闸口（另一层门槛），不是这一课说的人审；治理线那条迁移本身不需要人审，是记账动作。

### 4. AI 味儿
- 六节结尾里有七处用了同一个句式骨架（「X 立完/画完，剩下……」）收束，诊断指出这是模板感的来源。已把其中五处改写成不同的句式和措辞，只保留第 1 节结尾作为该模式的唯一一次出现，避免重复。

### 5. 哪里在装
- 「提前把这张地图摆出来，是为了避免……很容易觉得治理线立起来了，反哺这件事就算做完了」——命中导游腔（交代摆图的用意）和猜心句（预设读者的心理状态）两条禁令。已删掉这层自我说明，直接陈述事实：「这一课只解决了……反哺这件事只走完了三段里的第一段」。
- 「值得多说一句的是 media backlog apply 这条命令」——导游腔的轻度变体（预告自己要多说一句）。已删掉这句预告，内容直接说。

### 6. 本课专项检查（Prompt 输入合法性 + 参考流水线细节混用）
- 四条 Prompt 逐条核对，输入都成立，唯一的疵点是上面第 1 项已修的 daily-run.md 措辞。
- 「多半是它读到了参考流水线现成的 tasks.md，照着抄了一份」——这句话字面上暗示 AI 能访问作者私仓，因果机制不成立。已改写成「多半是它凭训练数据里见过的类似项目，编了一份看起来真实的具体任务」，把归因换成幻觉而不是越权读取。
- 诊断确认全文出现「参考流水线」的 8 处都挂了限定语，没有把参考仓库细节直接当成读者自己文件的内容来叙述；但指出第 3 节「只有两条例外」这句紧跟在「你自己的 core 包」之后，容易让人把参考仓库的简化计数当成读者自己文件的完整描述。这条已随第 3 节的逻辑断裂修复一并解决。

## 二、终检阶段的主要修改

- 五遍法过了一遍句子层：拆掉 5 处超 80 字的逗号硬拉长句（分号并列结构保留了 1 处，判断为正常的两个并列分句，不算硬拉长）。
- 符号层：跑 `check_style.py` 全程零命中硬禁令；显式 grep 确认全文没有出现「闭环」「全链路」「链路」「抓手」「颗粒度」「赋能」「心智」这七个本课专属禁用词。
- 开篇逐字核对：把开篇复述第 20 课结尾问题那句，从早期改写过标点的版本改回和 `handoff-ledger.md` 第 20 行「结尾抛出的问题」逐字一致的原文（含冒号和问号）。
- 结尾逐字核对：结尾段落与 `handoff-ledger.md` 第 21 行「结尾抛出的问题」逐字一致，未改动台账。
- 材料可溯：`check-lesson.sh` 的状态值核对（8 个状态值全部合法）、命令核对（`media backlog`、`media flip`、`media promote`、`media publish-done` 全部在仓库里找到出处）均通过；正文引用的 14 条候选、17 条复盘积压两个数字分别核对自 `harness/logs/2026-07-08-backlog-gardener.md` 和 `harness/logs/index.jsonl`（2026-09-02 的 `account-audit` 记账）。

## 三、最终状态

- `check_style.py`：硬禁令清零，剩 1 处分号并列长句的软警告，判断为正常结构不改。
- `check-lesson.sh <final.md> 实操`：`RESULT: PASS`。汉字数约 4980（上限 1 万，仅报数）；代码行 48（上限 200）；围栏块 4（要求 ≥3）；排障句「多半是」3 处（要求 ≥2）。
