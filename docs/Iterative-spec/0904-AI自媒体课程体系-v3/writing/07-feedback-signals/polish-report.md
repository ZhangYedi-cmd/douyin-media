# 润色报告（Step 6）

## 一、诊断清单逐条处理

诊断由 sonnet 子代理完成，读者画像为"会用 Claude Code、没做过自动化流水线、手上没有参考仓库、只有前几课自己做出来的东西"，并额外附了第六维：本课每条 Prompt 的输入是不是读者手上真实拥有的东西。

### 维度六（本课额外维度）：Prompt 输入合法性，全部处理，是本轮改动的主线

诊断发现草稿系统性地把三份读者没有的文件（`.claude/skills/dubbing-check/SKILL.md`、`pipeline/lessons.md`、`.claude/skills/dubbing-check/scripts/check-pace.mjs`）当作候选清单和判据的既定依据，还引用了一份第 09 课才会造的子代理定义文件（`.claude/agents/dubbing-reviewer.md`）来给本课的判据背书。逐条核对第 06 课那份"学员产物累计清单"，确认读者到本课为止只有五份阶段契约（含 `pipeline/2-create.md`）、`brain/` 四份 md 加 `tts.config.json`、`tts-dub` skill，没有 `dubbing-check`、没有 `pipeline/lessons.md`、没有任何子代理定义。

处理方式：重新核对 `pipeline/2-create.md` 全文，发现它的步骤 3（真相源铁律、死气检查、多音字同段两读）加步骤 4（点名完整性/语速离群/多音字/音画一致四类检查）已经足够倒推出六项候选，不需要引用读者没有的文件。全文改为只依赖这一份契约：

- 第 1 节的候选清单来源，从"两份材料（SKILL.md + lessons.md）"改为"一份材料（`pipeline/2-create.md` 步骤 3 和步骤 4）"，Prompt 块同步改成读步骤 3 和步骤 4，不再要求 AI 去读 SKILL.md 或 lessons.md。
- 排障句"清单太短"的修法，从"把 lessons.md 的 L2/L3/L10/L16/L17/L18 点给它"改成"把步骤 4 那四个检查类别点给它"。
- 语速离群项的"英文词语速假象"说明，从直接引用 `check-pace.mjs` 源码里的 1.3 音节折算系数，改成不依赖任何文件的通用推理：英文词念出来的时间比它对应的字数暗示的要长，按字数折算会显得偏慢。这条既满足任务里"要讲"的要求，又不需要读者能核实一份他们没有的脚本。
- 多音字读错项的"为什么归人判"，从跨段全局字典误伤的案例（这个案例只在 `pipeline/lessons.md` 的 L10 条，读者拿不到）改成 `pipeline/2-create.md` 步骤 3 本身就写的"同段两读无解"案例（L17，这个 L 编号本身就印在读者的 `pipeline/2-create.md` 里）。
- 六项定案表里的 L2、L3、L17 引用，核实这三个编号确实以 `（L2）`、`（L3）`、`（L17）` 的形式原样出现在读者拥有的 `pipeline/2-create.md` 终检闸和步骤 3 段落里，保留；L10、L16、L18 的具体故事因为只在 lessons.md 里，从正文引用中去掉，只在 3.2 节 overrides 键错位那处保留一句不带编号的通用描述（"删段或加段会让键错位"，这是从 `pipeline/2-create.md` 步骤 3 原文直接转译的，不依赖 lessons.md）。
- 第 2 节里用第 09 课那份 `dubbing-reviewer.md` 裁决口径表给判据背书的整段（原第 49 行），是用读者拿不到、甚至这门课还没造出来的材料去证明当下的结论，整段删除。判据本身的说服力改成靠两个读者能核实的例子（语速的英文词假象、多音字的同段两读）自己站住，不再借助外部权威。
- `brain/tts.config.json` 的引用里去掉了"dubbing-check 发现多音字或语速离群时就填这里"这半句注释转述，因为 dubbing-check 这个词读者还没见过，容易造成"这是什么"的疑问；只保留读者用得上的字段结构本身。
- "第 05 课起，这份账号级模板被复制进 build/"这句凭空补充的课号归因删掉，改成直接陈述机制（模板会被复制进 build/），不再挂靠一个读者没法验证的具体课号。

### 维度一 读不懂

1. Prompt 块一读第 06 课那张表却不给路径：改成"把内容贴给 AI"，不要求 AI 去读一个不知道存在哪的文件。
2. check-pace.mjs 具体细节不可信：随维度六一并删除。
3. tts.config.json 注释提前点名 dubbing-check：随维度六一并删除该半句。
4. "组件"术语解释挂在句尾：改成候选清单结果那段里单独一句先定义再往下用。

### 维度二 太啰唆

1. "阈值要有出处"讲了四遍：只在 3.1 节开头交代一次规则，具体展开只在合成不全那一段落地，其余表格行不再重复这句话。
2. 结尾"表还只是表"重复两次：合并成一句陈述加一句问题，不再重复。
3. 三档分类定义讲三遍：保留三次出现，但确认这三次分别承担不同功能（第一次下定义、Prompt 块是可执行内容、收尾是应用到本批候选的结论），不是纯重复，未改。

### 维度三 逻辑断裂

1. "倒推靠两份材料"与实际 Prompt 只读一份材料自相矛盾：随维度六统一改成一份材料，正文与 Prompt 现在一致。
2. 用第 09 课材料给第 07 课的判据背书：随维度六删除整段。
3. "第 05 课起"课号归因凭空：随维度六删除该归因。

### 维度四 AI 味儿

"X 不是 Y，是 Z"句式全文出现 6 处。核对后保留 3 处（开篇 Loop 定义、第 2 节核心判断、第 3 节核心判断，这三处是章节的主论点句，结构上正好是 structure.md 鼓励的职责分配句式，删掉会削弱判断力度），改写 3 处非核心句（开篇"配音只是素材"、表格前"排第一行"那句、overrides 键说明那句），避免同一个句式模具反复出现。

### 维度五 装

1. 破折号两处（第 39 行"1.3 个音节折算——但"、第 26 行"音画一致——把这四个词"）：随内容删改和标点修正一并清零，`check_style.py` 复核硬禁令为 0。
2. 全文正文直角引号误用（本该用弯引号却写了 ASCII 直引号）：逐处改成弯引号，只保留真正在引用具体短语或候选表达的地方用引号，复核 `check_style.py` 通过。
3. "值得留意"（好评口头禅变体）：改成"合成不全这一行的阈值分两层"，直接给结构，不再用"值得留意"这种自我标注。
4. "L17 和 L18 记的是修法……这两条留到第 3 节用"这种交代写作安排的导游腔：随维度六删除整句。
5. "第 09 课那份……只借它的裁决口径表说明道理站得住"这种交代取材策略的导游腔：随维度六删除整段。
6. "不需要为了凑够三类硬造一个第三档的例子"这种自证式表白：改成直接陈述"这一批候选里没有对应的项"，不再解释自己为什么没有硬凑。
7. 结尾反问句预告下一课：保留了"下一课把这六项逐条造成脚本"这句平实陈述加结尾问题，去掉了原本重复"检查点表定了六项要量什么"的那半句，问题本身是任务要求对齐 `handoff-ledger.md` 第 07 行的结尾，不算悬念钓鱼，予以保留。

## 二、终检阶段的主要修改

- 结构遍：第 2 节把原本挤在一段里的语速/多音字/组件大字三个例子拆成三个独立段落，每段一个例子，避免单段塞三件事。
- 论证遍：六项定案表逐行核对阈值来源，能标 L 编号的（L2、L3、L17）核对这些编号确实出现在读者拥有的 `pipeline/2-create.md` 里才保留；语速离群一行如实写"没有一次真实事故定过这条线"，不编造统计方法。
- 句子遍：拆掉 8 处超过 80 字的逗号长句（第 1 节材料介绍句、审两件事那句、第 2 节分类定义句、语速/多音字/组件大字三段、合成不全阈值句、overrides 落点两段），`check_style.py` 警告从初稿的 10 处降到 0。
- 温度遍：保留候选清单太短、overrides 键错位两处"多半是"卡点拦截（都基于读者能自己观察到的状态：清单条数少、改完读音没变），第三处死气行的"复扫不能只看退出码"也是同类型拦截，全文合计 3 处，过 check-lesson.sh 的排障句门槛。
- 符号遍：`check_style.py` 硬禁令清零，警告 0 处；确认全文无直角引号、无破折号、无星号加粗。

## 三、验收

- `check_style.py`：硬禁令清零，警告 0 处。
- `check-lesson.sh <final.md> 实操`：RESULT PASS。汉字 3496（远低于 1 万上限）；代码行 10 / 200；围栏块 3 / 3；排障句 3 / 2；命令与脚本名核对无 WARN（全文引用的文件路径 `pipeline/2-create.md`、`brain/tts.config.json`、`build/`、`overrides`、`rec/segment-overrides.json` 均在仓库可 grep 到，且都是读者已拥有或用于一句话说明命名差异的对照项）；状态值核对为空（本课未涉及 `meta.yaml` 状态值）。

## 四、主回话验收后的修改

主回话验收指出一处贯穿第 1、3 节的读者帽问题：全篇把参考仓库真实的 `pipeline/2-create.md`（含步骤 3、步骤 4、L2/L3/L17 等编号、0.45 秒阈值）当成了学员自己第 03 课写的那份契约在读。核对学员自己的 `courses/03-立骨架-仓库、目录、契约.md` 第 3.2 节确认：学员那份 `2-create.md` 只是四件事的骨架（输入/输出/状态翻转/闸口），闸口一句"闸口有两处，一处是配音审批"，没有步骤编号、没有细节、没有任何 L 编号，`courses/05-用开源Skill跑通一条内容.md` 第 3 节也没有"步骤 3/步骤 4"这种说法。这是把写作材料（我读参考仓库用的）当成了读者前提，背景包第 2 节明令禁止的错误，在这一课的第 1 节候选来源和第 3 节阈值出处两处都踩了。

处理方式：

1. 第 1 节候选来源改为学员真实拥有的三样：第 05 课自己配音合成、抽帧核对时碰到的具体问题（0 字节 mp3、读错的字、特别快或特别慢的段、字幕和时长核对时的画面切断，逐条核对 `courses/05-用开源Skill跑通一条内容.md` 第 3 节原文确认真实存在）；第 06 课那张验证信号去向表标机器的格子；学员自己 `pipeline/2-create.md` 里"配音审批"这一句话闸口。第一条 Prompt 改成把这三样交给 AI，不再要求 AI 去读一份学员没写过内容的步骤 3、步骤 4。"完整性、语速离群、多音字、音画一致"四类改成由课文明确点出是"这门课参考的那条真实流水线，作者自己跑的时候验证过"的分类，直说不是学员文件里已有的东西，供学员对照候选清单查漏；真相源不同步、段内死气这两类学员本批内容大概率还没撞上，说明是作者提前给出的经验，不是学员自己发现的。
2. 六项定案表的阈值出处全部改口径：0.45 秒改成"参考流水线一次真实返工里定下的起步值，学员的账号跑过几条内容之后可以按自己的返工经验往上或往下调"；原来三处"`pipeline/2-create.md` 标成 L2/L3/L17"的写法全部删除，改成"参考流水线上有过真实事故""参考流水线的踩坑记录里有真实案例"这类不点具体编号的表述，不再暗示学员翻自己的文件能找到这些编号。合成不全和真相源不同步两行补上与学员第 05 课真实经历的对应关系（0 字节 mp3），加强这两行阈值不是凭空来的。
3. 3.2 节的 overrides 落点，改成让学员把这条规则写进自己的 `pipeline/2-create.md`：第三条 Prompt（原表格 Prompt）末尾补一句，让 AI 顺便把"配音质检发现的问题改进 build 内 `tts.config.json` 的 `overrides`，沉淀性规则回写 `brain/` 模板"写进学员自己 `pipeline/2-create.md` 的配音审批那一段，没有新增 Prompt 块。3.2 节正文相应把"`pipeline/2-create.md` 步骤 3 写得很清楚"这句改成"这条规则现在要补进学员自己的 `pipeline/2-create.md`"，说明这是这一课要往读者契约里新写的内容，不是契约里原本就有的。`rec/segment-overrides.json` 那句命名差异说明保留，只是不再挂靠一个不存在的"步骤 3"出处。
4. 篇幅顺势放开：第 2 节给三档判据各配一个具体反例（能断言的硬塞给人看会漏检、只能人看的硬判成机器会误杀真的合格段、缺数据源的硬造阈值会拖垮整套检查的可信度），不是为了凑字数，是诊断第六维那种"读者拿不到材料"问题解决之后，判据本身需要更扎实的支撑才站得住。全文从 3496 字长到 4476 字，仍远低于 1 万字上限。

修改后复核：`check_style.py` 硬禁令清零，警告 5 处（均为枚举类长句或表格单元格信息密度高，非逗号硬拉长句，结合语境判断不改）；`check-lesson.sh <final.md> 实操` RESULT PASS，汉字 4476、代码行 13/200、围栏块 3、排障句 3；`pipeline/2-create.md`、`brain/tts.config.json`、`build/`、`overrides`、`rec/segment-overrides.json`、`2-create.md` 六个反引号引用逐一复核，均可在仓库 grep 到，命令与脚本名核对无 WARN。
