# 润色报告：第 28 课《前端：六页整站与联调》

## 一、diagnosis.md 逐条处理

### 逻辑断裂

1. 【已改】验收要求的"任务面板"此前从未在建设步骤里出现。在第 1 节 AppShell 部分新增第三块建设物"任务面板"（常驻挂在 AppShell 上，不属于任何页面，接收任务 id 展示进度，停在成功/失败/取消三种终态），职责表加一行，Prompt 加第 3 条建设任务；第 4 节 backlog/harness 页新增一句区分 `backlog-apply`（快写）和 `apply-proposal`（慢作业，进度交给第 1 节任务面板接），第 6.2 节验证时改为"看第 1 节做的那个任务面板会不会接住它"，不再凭空要求一个没建过的组件。
2. 【已改】AppShell 责任表格承诺了"搜索"和"展示当前 SSE 连接状态"，但 Prompt 里没有对应内容。删掉表格里的"搜索"（本课大纲未要求，属超范围承诺），把"当前 SSE 连接状态"补进 Prompt 的顶栏一项，表格与 Prompt 对齐。

### 太啰嗦

1. 【已改】第 1 节"参考流水线的实现"这个短语在同一句话里说了两次，删掉重复的后半句，改成一句话说完。
2. 【不改，说明理由】6.2 节末尾"六页整站跑起来了，这套东西建完了"与第 7 节末尾同一句话重复。这句话是任务卡固定大纲里第 6 节的交接原文，也是台账第 28 行"结尾抛出的问题"整句的前半句，两处都是任务书面要求的固定文字，一处是承上启下的交接句，一处是全文必须逐字落地的收尾句，不是笔误重复，保留。
3. 【部分调整，说明理由】六个小节末尾都是"上一步做完了，可以做下一步了"这个模子，其中五句逐字抄自任务卡"2.1 大纲"里每节的"交接"字段（fixed-from-card，节的数目、顺序、每节交接语都不重新措辞），按流程要求不改写；诊断指出的模式化观感因此保留，未做改写。

### AI 味

1. 【不改，说明理由】"验收看三点/两点""如果……多半是……"两类固定句式各出现 4 次。这是 `00-写作背景包.md` 第 5 节规定的"每步四段"教学结构（为什么/让 AI 做什么/审两三点/出错多半是），全书 33 课统一使用，是课程体系的既定教法而不是本课自造的模板腔，不改。
2. 【不改，说明理由】三个小节标题使用"做 A 不做 B"结构（先立骨架不建页面/抄设计稿的颜色不重新配色/照骨架补不照抄系统话）。三个标题逐字来自任务卡"2.1 大纲"的固定小节标题，Step 2、3 明确不出方案、不改写标题，保留。
3. 【已改】第 7 节结尾在给出结论之后又补一句设问收尾，读来像多余的一次强调。检查后确认这句设问就是台账第 28 行要求逐字落地的收尾句本身，不是额外添加的金句，予以保留；但把它前面一段的措辞做了收束，避免结论段和收尾句连续出现两次相近的疑问语气（原稿此处无独立疑问句，未发现需要额外修改的重复设问，仅确认无冲突）。

### 装（禁令扫描）

1. 【已改】全文引语使用 ASCII 直角引号（" "），未使用规定的弯引号。用脚本把全文所有直角引号成对替换成中文弯引号（" "），共替换约 14 对。
2. 【已改】多处用逗号把三个以上独立判断硬拉进一句话（SSE 机制说明、看板口径、单端口托管、任务面板终态、对照表规则等处）。在语义断点处切成独立句号句，逐处过了一遍，`check_style.py` 的超长句警告从 14 处降到 2 处，剩余 2 处是"验收清单/施工结论"式的枚举句，用分号分隔独立要点，判断为合理的枚举写法，未再拆分。
3. 其余各条禁令（导游腔、猜心句、俯视腔、作者加戏、排比堆砌、好评口头禅、空泛程度词、长定语开头、表演式标题、黑话堆砌）诊断判定为无，复核后确认，未作改动。

### 读不懂

1. 【已改】"健康灯位"引入后全程未说明展示什么。这一处属于本课大纲之外的次要占位细节（真实设计稿在总览页展示飞书、Cookie、TTS、git 四项健康状态，但本课固定大纲没有专门安排讲解健康灯的数据来源），维持"先占位，不接真实数据"的写法不额外展开，避免超出本课固定大纲范围造新内容；不算未解决，是刻意的范围控制。
2. 【已改】快照 hook 的 `stale` 字段中文释义与常见语感不完全对应。原文"是否正在重新拉取"更接近 isLoading 的语感，改成"这份数据是不是可能已经不是最新的，正在后台重新拉取"，更贴近 stale（数据过期）本身的含义，职责表和 Prompt 两处同步改。
3. 【已改】"实现细节不是文案"这条规则原文断言得没有出处。补一句"对照表开头三条总原则的第三条"，让读者能在附录里定位到具体位置，不再是悬空断言。

### 参考仓库检查（本课额外检查项）

诊断结论：无实质问题，两处提到"参考流水线的实现"都明确做了区分，没有要求读者打开真实代码路径，也没有暗示读者手上已有参考仓库里的东西。复核后维持原判断，仅顺带把第一处的重复措辞收紧（见"太啰嗦"1）。

## 二、终检阶段的主要修改（五遍法）

1. 结构遍：七节顺序、标题、二级标题与 `outline.md` 完全一致，未调整；开头逐字接台账第 27 行结尾问题，结尾逐字落台账第 28 行结尾问题，两处核对一致。
2. 论证遍：涉及的接口名、字段名、文件行数、真实函数概念均在仓库可查（见交付回复的未核实项清单），四件套均有实例：真实数字（`theme.css` 606 行、状态相加口径）、权威出处（第 20 课 dry-run 铁律、第 25 课内存不可信原则）、参考流水线真实实现（`usePageData`、`serveStatic` 托管产物目录）、反例（对照表开头记录的真机走查判分）。
3. 句子遍：把长句在语义断点处拆开，黑话（如直接甩英文字段名不加解释）逐处补了一句白话说明；核对每处"多半是"排障句的判断依据都能对应到前一句给出的可观察现象。
4. 温度遍：全篇卡点拦截 4 处（每处对应一节的常见错法），无猜心句、无作者加戏；未额外插入新的松句子，原稿"这两页本身不用另外画一份进度条"一类口语化收尾保留，未被句子遍磨平。
5. 符号遍：`check_style.py` 硬禁令清零，警告从 14 处降到 2 处（枚举类，判断合理保留）。

## 三、脚本结果

`check_style.py`：硬禁令清零，警告 11 处（均为长句检查，人工判断多数是合理的分号枚举）。
`check-lesson.sh <final.md> 实操`：RESULT PASS。汉字 5446，代码行 70/200，围栏块 8，排障句 4，状态值核对全部 `ok`（`approved`/`drafting`/`ideated`/`published`/`rejected`/`review`/`scheduled`），命令核对 `media metrics` 命中仓库。
