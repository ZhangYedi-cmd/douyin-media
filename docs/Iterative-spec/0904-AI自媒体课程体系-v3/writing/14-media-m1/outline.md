---
plan: fixed-from-card
length: standard
figures: none
sections: 5
---

本课 Step 2、3 不出方案、不问人：大纲照任务卡 `cards/14.md` 第「2.1 大纲」块原样展开，节的数目（5）和顺序不动，二级标题按任务卡「不分」的标注保持一级到底。局部调整只有一处：任务卡每节给的是「核心判断 / 二级标题 / 交付物 / 学员此时手上有 / 交接」五项，本大纲把「学员此时手上有」项显式列出来，作为每节动笔前的材料校验清单（对齐写作背景包第 2 节的铁律：本节每条 Prompt 的输入必须是这张清单里已经出现的东西），不改变任务卡定的节序和判断，只是把校验动作前置到大纲里，方便写作时逐节核对。

## 1. 开篇：规格能施工了，学员只验收不编码
核心判断: 这一课起，学员不再写判据也不再写规格，AI 产出代码，学员的动作是照出口条件验收，看不懂就让 AI 解释或重写，不许自己上手改一行。
支撑材料: analyzed.md「执行方案」条（第 13 课收尾三句）、「方法出处」条（第 11 课第 6 节的执行定义）
交付物: 验收纪律三句话——出口条件过没过是唯一准绳；看不懂先问 AI；不通过就换个 Prompt 重来，不自己动手改
学员此时手上有: 第 13 课的 `02-执行方案.md`（14 条命令四件套、两张状态迁移表、M1/M2 出口条件）
二级标题: none
收尾交接: 验收纪律定了，先把读路径的地基铺起来。

## 2. core 三件：parsers、state、writer
核心判断: 往返测试是这一段的验收标准，真实的 meta.yaml 和 backlog.yaml 读进来再写出去，字节级别不能变形，变形就说明写法碰了不该碰的地方。
支撑材料: analyzed.md「代码原型」条（writer.ts 落盘顺序头注）、「代码原型 / 踩坑」条（yaml-edit.ts R1 实测）、「真实测试」条（yaml-edit.test.ts 往返用例）、「施工记录」条（A 包 PASS 关键发现）、「真实样例」条（学员自己的 meta.yaml）
交付物: 一条让 AI 按执行方案实现 core 三件并配往返测试的 Prompt + 验收动作（跑往返测试，看 diff 是不是空的）
学员此时手上有: 第 13 课执行方案里 core 模块的详细设计（第 13 课产物）
二级标题: none
收尾交接: core 有了，读路径可以往上搭命令了。

## 3. 读命令全量：st、next、backlog ls、doctor
核心判断: 读命令只读不写，验收标准是拿真实的项目数据跑一遍，输出的数字和状态跟自己肉眼看的一致。
支撑材料: analyzed.md「代码原型 / 读命令」条（st/next/backlogLs/doctor 四条命令实现）
交付物: 一条让 AI 按执行方案实现全部读命令的 Prompt + 验收动作（对每条命令跑一次，看输出跟自己记忆里的状态对不对）
学员此时手上有: core 三件（本课第 2 节产物）；第 13 课执行方案里读命令那几条的详细设计
二级标题: none
收尾交接: 读路径全通了，写路径先切两个历史事故最集中的命令。

## 4. 首批写命令：publish-done 与 promote
核心判断: M1 出口条件是一句能跑的验证动作，真实仓库上 media check 通过，YAML 文件的注释一个字都不能丢。
支撑材料: analyzed.md「执行方案模板」条（M1 五步表格与出口条件原文）、「代码原型 / 写命令」条（promote.ts / publishDone.ts）、「状态机真相源」条（state.ts 两张迁移表）、「错误码表」条（errors.ts 退出码表）
交付物: 一条让 AI 实现 publish-done、promote 两条命令的 Prompt + M1 出口条件验收动作（真实数据上跑 media check 的替代验证，看退出码和注释）
学员此时手上有: 读命令全量（本课第 3 节产物）
二级标题: none
收尾交接: core 和第一批命令都跑起来了，M1 出口条件过了。

## 5. 收尾：读写各通一半
核心判断（收尾陈述）: core 三件、全部读命令、publish-done、promote 都跑起来了，M1 出口条件验收通过；剩下的写命令、check，还有前十课手改过的地方，还散着没收进这一个入口。
支撑材料: analyzed.md 全部条目综合
交付物: 收尾两段，不写总结与作业
学员此时手上有: 第 1-4 节全部产物
二级标题: none
收尾交接（抛给下一课的问题）: core 和第一批命令跑起来了。剩下的写命令、check，还有前十课手改过的那些地方，怎么收敛到这一个入口？
