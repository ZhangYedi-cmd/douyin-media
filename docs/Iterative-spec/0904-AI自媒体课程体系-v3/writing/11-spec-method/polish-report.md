# 润色报告：第 11 课 SPEC 方法

## 一、diagnosis.md 逐条处理

diagnosis.md 由 sonnet 子代理产出（读者画像：会用 Claude Code、没做过自动化流水线、手上没有参考仓库，只有第 10 课结束时的家当）。以下按诊断原文的一到六项对照处理。

### 1. 哪里读不懂

- 「模块 7」「模块 3」未定义就用来做论证依据（第 1 节、第 8 节）：**已改**。全文删除"模块 N"这套编号，第 1 节改成"后面要造的控制台里，负责解析进程输出的那个模块"，第 8 节结尾改成"下一课要开始处理 media CLI，这是接下来要造的状态中枢入口"，不再挂靠一个读者没见过的模块编号体系。
- 「L3 课」未解释（第 5 节）：**已改**。"往后所有 L3 课统一照抄的模板"改成"往后处理大件事的课统一照抄的模板"，去掉课程分级的内部黑话。
- 「判据」一词与第 6 课已有的"三条判据（能写成断言/只能靠人看/现在写不了但以后能写）"撞名（第 1 节）：**已改**。加一句显式区分："这三条判的是要不要走六步，跟第 6 课那张验证信号表里判一条内容检查点能不能写成断言的那三条判据，是两码事，别混在一起"。未改成另一个词，因为"判据"是任务卡「2.1 大纲」原文用词，改名会偏离 fixed-from-card 的大纲约束，加一句区分足以解决歧义。

### 2. 哪里太啰唆

- overrides / segment-overrides 口径打架的故事在第 5、8 节讲了两遍，第二遍没有新信息：**已改**。第 8 节的复述从完整重讲压缩成一句指回第 5 节，不再重复文件名和事件经过。
- 「拿……例子……」句式连续四次同构：**已改**。第 2.2、3、5 节三处引出示例的句子分别改写成不同表达（"套进六脚本这个例子，表大致是这样""这条 prompt 套进六脚本这个例子……""六脚本这个例子的第 4 节风险与待定项，可以这样填"），不再是同一个模板反复套用。

### 3. 哪里逻辑断裂

- 「六个检测脚本有没有互斥路线」在第 1、2、8 节三处说法互相矛盾，最严重的一条：**已改**。重新定框架：第 1 节明确说这是"借它体量小、上手快"的练习案例，不是要否定第 8 课当初的判断；第 2.1 节的引入句去掉"这个选型点本来就存在，只是没被摆上台面"这种暗示"当初漏掉了什么"的措辞，改成"当时选六个独立脚本，直觉选对了。现在借它练一遍调研"；第 8 节呼应第 1 节的措辞（"体量小、选错的代价也小"），三处口径统一，不再互相打架。
- 「调用成本增长不算贵」缺依据（第 4 节）：**已改**。补一句具体依据："配音批次一天顶多合成几次，六到十几条命令跑一遍是分钟级的事，多花的是机器时间，不牵扯别的组件要跟着改设计"，不再是空判断。
- 「两件事字数差不多」与 278/537 的实际比例（接近两倍）不符：**已改**。改成"字数差着近一倍"，与数字对得上。

### 4. 哪里有 AI 味儿

- 五处「进入下一步」式机械过渡（第 33、65、89、128、141 行，旧行号）：**已改**。全部删除或改写成承接下一节内容的自然句子，不再用"进入 X"的标签式收尾，交给下一节标题自己承接。
- 「拿……例子……」模板重复：见上文"哪里太啰唆"处理，已改。
- 「这才是……真正要的东西」类点题句偶有出现：**部分改**。第 3 节保留一处（"这才是讨论选型这一步真正要的东西"），因为删掉后段落收束打骨架被打散、语义空了一截；随着上面几处机械过渡和例句模板被清理，这句不再和其他同构句叠加出模板感，判断保留。

### 5. 哪里在装

- 命中导游腔：五处「进入下一步」式预告，已列在第 4 项一并处理，全部删除或改写。
- 「如果……多半是 X」句式经核对基于可观察状态起句，符合禁令清单放行条款，未改。
- 其余类别（金句化、俯视腔、作者加戏、表演式标题、黑话堆砌、标点违规）：diagnosis.md 核实无命中，未改。

### 6. 材料是否超出读者家当（本课特有项）

- 6a（Prompt 是否要求读者拿不到的文件）：diagnosis.md 核实三处 Prompt 均只用读者已有的"六个配音检测点"作输入，无命中，未改。
- 6b-1：第 1 节用 00-ADR.md 的具体数字（278/537 行）做论证依据，且称其为"本课程自己的"：**已改**。删除对 00-ADR.md 这个读者没有的内部文件的具名引用，直接陈述数字本身（"第 8 课那六个检测脚本加起来 278 行；后面要造的控制台里……有 537 行"），不再挂靠一个读者接触不到的文件名。
- 6b-2：docs/Iterative-spec/ 目录约定和 0818 执行方案模板，一处标"本仓库自己"、一处标"参考流水线"，标注前后矛盾：**已改**。统一改成"参考流水线"，其中 docs/spec/ 目录约定一处进一步落到项目 CLAUDE.md 里真实写着的"文档架构约定（防漂移）"这条规则上（"设计和迭代文档一律住到日期加事项的子文件夹里……"），比泛指"docs/Iterative-spec/ 目录"更准确，且不要求读者去打开任何具体文件。
- 6b-3：pipeline/2-create.md 与 dubbing-reviewer.md 的归属标注前后不一致，第 5 节的表述容易被读成"读者自己那份 dubbing-reviewer.md 现在还留着这个冲突」：**已改**。补一句"参考流水线那份 .claude/agents/dubbing-reviewer.md 定义"明确归属，并加一句"第 10 课把这条口径统一成了 tts.config.json 的 overrides，你自己那份 dubbing-reviewer 定义现在用的也是这一个口径，没有遗留冲突"，避免读者误以为自己手上的文件还有未解决的矛盾。

## 二、终检阶段的主要修改

对照 skill 的五遍法（结构、论证、句子、温度、符号）走了一遍，主要动作：

- **符号遍**：跑 `check_style.py`，硬禁令 0 命中；跑 `check-lesson.sh`，先后修掉两处硬 FAIL——
  - 「一定会」命中过度自信词表（第 4 节 ADR 收尾句），改写为不含该词的等义表述。
  - 模板块里的「第一步…第 N 步」命中标题模板前缀正则（check-lesson.sh 对整份文件按行扫描，不区分是否在代码围栏内），把汉字数字"第一步"改成阿拉伯数字"第 1 步"，不改变节名、节序和模板语义，只改数字写法本身以避开误伤。这处改动已同步回 `spec-lesson-standard.md` 的模板原文，保持两份文件一致。
- **句子遍**：check_style.py 报了若干条"单句超 80 字"的警告（非硬禁令），多数是表格式并列陈述（"前提是……；代价是……；失败模式是……"），用分号分隔了三个独立信息点，读起来不算硬拉长句；小部分保留是权衡篇幅后的取舍，未逐条拆分。
- **结构遍/温度遍**：五处"进入下一步"式导游腔过渡在诊断阶段已处理，不再重复。

## 三、篇幅与验收结果

- 汉字 5945（上限 10000，只卡上限不设下限）。
- 代码行 34（上限 200）。
- 围栏块 6（实操篇要求至少 3）。
- 排障句"多半是" 4 处（实操篇要求至少 2）。
- `check-lesson.sh final.md 实操` 最终输出 `RESULT: PASS`。

## 四、结尾对台账核对

成稿结尾"六步方法有了。拿第一件重活来练：状态写入口这件事，先走前三步，调研、讨论选型、拍板，一行代码都不写。"与 `plan/handoff-ledger.md` 第 11 行「结尾抛出的问题」列逐字一致，台账未改动。

## 五、主回话验收后的修改

主回话验收给了三处小改，按要求全部改在 final.md 上，逐条处理：

1. **docs/spec/ 子目录命名与第 12、13、18、26 课任务卡对不上**：原稿用的是"日期加事项"（例：`docs/spec/2026-09-10-状态写入口/`），但第 12、13、18、26 课的任务卡和已交稿的第 12 课都用事项代号命名（`docs/spec/media/`、`docs/spec/feishu-gate/`、`docs/spec/console/`）。**已改**：第 1 节的目录树示例换成 `docs/spec/media/` 下四个文件；正文改成"参考流水线用的是日期加事项，每轮迭代另起一个日期文件夹；这门课的三件大事各占一个代号目录，media、feishu-gate、console，够用，不用跟着日期滚动"；末句"往后……处理……"也同步换成三个代号目录各自对应的课号。核对过 `spec-lesson-standard.md`，该文件没有写 docs/spec/ 目录约定，不需要同步。
2. **课号：27 课处理控制台，应为 26 到 28 课**：全书 09-04 合并改版后控制台是 26（定契约）、27（后端）、28（前端）三课，不是单独一课。**已改**：第 7 行、第 33 行（原稿行号）两处"27 课"改成"26 到 28 课"。全文重新 grep 了一遍所有课号，其余提及（第 10、8、6、9 课，12 到 16 课，18 到 19 课）核对全书 29 课编号后确认无误，未发现"29 课""31 课"这类误写。
3. **第 21 行"接下来几节会……"是导游腔式的章节安排预告**：**已改**，按主回话给的措辞改成陈述句"下面六步都拿第 8 课那六个检测脚本当例子，它体量小、选错代价小，几百字能走完一轮"，去掉"接下来几节会"这个交代写作安排的说法；后续两句顺势去掉了与新首句重复的"体量小、上手快"表述，避免同一个意思说两遍。

改完同步复制到 `courses/11-SPEC方法-从调研到验证的六步.md`，重跑 `check_style.py`（硬禁令 0 命中）和 `check-lesson.sh final.md 实操`（`RESULT: PASS`，汉字 5915，代码行 34，围栏块 6，排障句 4）。成稿结尾未改动，`handoff-ledger.md` 第 11 行未动。
