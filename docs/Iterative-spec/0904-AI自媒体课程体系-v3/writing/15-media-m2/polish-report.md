# 润色报告：第15课 执行M2：全量命令与存量迁移

## 一、diagnosis.md 逐条处理

诊断按严重程度给了六个维度的问题清单（见 `diagnosis.md`），逐条处理如下。

### 维度六：Prompt 输入真实性（最严重的一类，全部已改）

1. 【已改】3.1节 Prompt 把 check 的判定逻辑写成"接到已有的规则计算模块里"，但第14课产物清单里 `check` 明确留到本课，不存在一个可供接入的现成模块。改法：Prompt 第1条改成要求 AI 把判定逻辑单独实现成一个新模块（吃仓库状态、不碰文件系统），不再预设它已经存在。
2. 【已改】3.2节末尾"陈旧锁30秒"的细节此前突兀出现，没交代依据。改法：把这个数字重新框成"第12课选文件锁、第13课执行方案里应该已经给出的具体秒数"，让它落在学员自己已经写过的文件里，而不是凭空冒出来的既定事实；同时把这段挪到紧跟退出码3之后，去掉了原来的话题跳跃感。
3. 【已改】2.1节"核心的引擎实现只有一份，第14课已经把它做出来了"缺依据。改法：直接点名这套引擎就是第14课交出的 core 包里 `writer` 部分，`promote`/`publish-done` 已经在用，让论断落在第14课产物清单能核实的东西上。
4. 【已改】2.1节 Prompt 里"第2节"的编号断言，读者可能以为是随口指定。改法：Prompt 前加一句，说明第2节是第11课钉死的执行方案模板固定节号，不是猜的。

### 维度三：逻辑断裂

1. 【已改，最严重】命令总数算术对不上："五条读命令"数不出第五条。改法：开篇明确交代 M1 已经跑通 `st`、`next`、`backlog ls`、`doctor` 四条读命令，`check` 是唯一留到本课的读命令，凑齐五条，跟第5节的总数对上。
2. 【已改】"已有的规则计算模块"这类无依据论断，处理见维度六第1、3条。
3. 【已改】陈旧锁细节的话题跳跃，处理见维度六第2条。

### 维度五：装（禁令清单核对）

1. 【已改】第一节开篇的排比三连加轻度金句收尾（"命令没写完……规则没写完……手改没收敛，产生的干脆是另一条谁都不知道的路"），改成三句平实的直陈句，去掉"谁都不知道的路"这种带文学腔的收尾。
2. 【部分改】"不是……是……"变形句式，诊断给了三处例子。逐条核实：check_style.py 脚本的硬禁令只拦截含"而是"的转折拿腔句（如"不是A而是B"），三处例句都不含"而是"，是普通的否定加肯定说明句，不属于脚本硬禁令覆盖范围。出于稳妥，改写了对仗感最强的一处（2.1节"事务这层麻烦不是为了应付某一条命令，是为了保证……"），其余两处（"八条规则不是随手拍出来的……""这个动作跟状态翻转不是一回事……"）保留，因为改写后反而更绕，且这类否定加肯定的直陈句本身是自然中文，不在禁令清单原文列出的变形范围内。
3. 【已改】"看似/看起来……其实"变形，第一节开篇重写时一并去掉。
4. 【已改】"先把话说在前头"的导游腔候选，删掉，直接从"写路径唯一不是设计出来的"这句判断开始。
5. 【不改，理由见下】结尾反问句"可你怎么证明写路径真的唯一，而不是自己以为唯一"，诊断认为带悬念钓鱼意味。这句是任务卡"2.1 大纲"第5节明确给定的结尾问题，也是 `handoff-ledger.md` 第15行"结尾抛出的问题"一栏的原文，属于跟台账对齐的必留内容，不属于表演式标题（它不是标题，是正文收尾），也不在 check_style.py 的反问硬禁令范围内，予以保留。

### 维度四：AI味（句式套路）

1. 【已改】四个小节结尾的极短钩子句结构过于一致。改了其中两处（第1节"先把剩下的写命令补齐"改成"剩下的写命令，先补上"；第2节"写命令全量齐了，进入 check"改成"七条写命令这边算是过了关，接下来是唯一的读命令 check"），另外两处（第3节结尾、第4节结尾）内容本身承担着承上启下的实质信息，且第4节结尾要对齐台账原文，保留不改。
2. 【部分改】三处 Prompt 后紧跟"拿到结果先看……如果……多半是……"结构。核实后发现只有4.1节是完整的"先看+如果+多半是"三段式，2.1节和3.1节后面的验收段落结构和措辞并不相同（2.1节是"拿到结果先看两处：A；B"，没有"如果/多半是"分句），诊断把这三处归为同一种重复结构略有夸大。予以保留，没有强行拆散已经写清楚的验收逻辑。

### 维度一：读不懂

1. 【已改】CHK-06"来源指针"未解释。改法：在规则表后的段落里加一句，说明这是 `meta.yaml` 记着取自哪条选题、选题池那条记着被取到了哪个目录，两头互指的同一对指针。
2. 【已改】"写计划"一词反复出现但未定义。改法：在2.1节首次出现处加括注，说明写计划就是落盘前把要改哪个文件、哪个字段、改成什么值列成的一张清单。
3. 【不改】CHK-08"新内容"判定依据未展开，诊断标注"程度较轻"。这一条留给学员自己在 Prompt 里跟 AI 敲定判定口径（依据是当天有没有新建的内容目录），是这一课 Prompt 本身要产出的细节之一，不是正文该替读者定死的答案，予以保留。

### 维度二：太啰嗦

1. 【已改，最严重】"写路径唯一性还没验证"这一个意思在第4节结尾、第5节标题、第5节正文两段、结尾反问里一共出现四次。改法：精简第5节正文，把原来两段各自重复"看起来收敛""但这只是看起来""没验证过就是个愿望"的表述合并成一段更紧的陈述，去掉重复的限定语，保留第4节结尾（自然承接）、第5节标题（任务卡固定标题）、结尾反问（对齐台账）三处不动。
2. 无其它啰嗦项。

## 二、终检阶段的主要修改

按 revision.md 五遍法过了一遍结构、论证、句子、温度、符号，主要动作：

- 符号：`check_style.py` 首次扫描命中 7 处破折号，全部改成冒号或直接断句，硬禁令清零。
- 句子：多处 80 字以上长句仍在，脚本判定为警告非硬禁令，结合语境保留了大部分（技术说明句需要一定长度才能把条件讲全），但对开篇排比句和事务引擎那句做了拆分。
- 论证：给"事务引擎已存在""陈旧锁阈值"两处补了第14课/第13课的具体依据，不再是悬空断言（详见维度六与维度三的处理）。
- 篇幅：为呼应背景包"状态收敛"作为四块重点之一要往厚里写的要求，在读者诊断之前先做过一轮加厚（flip 时间戳锚点机制、metrics record 7天窗口回填、backlog apply 合并归档两种动作、dashboard rebuild 全量重建、陈旧锁抢占提醒），把汉字数从初稿的 5165 提到诊断前的 6289，终稿在此基础上小幅增补解释性文字后为 6503，仍在 1 万字硬上限内。

## 三、脚本核对结果

- `check_style.py`：硬禁令清零，剩余 17 处均为"单句超80字"警告，按语境判断后部分保留、部分拆句。
- `check-lesson.sh <final.md> 实操`：`RESULT: PASS`。汉字 6503，代码行 41/200，围栏块 4，排障句（多半是）4，状态值核对（published/retro_done/review）全部 ok，命令与脚本名核对（media check/media flip/media promote/state.ts）全部 ok。
