# 润色报告（Step 6）

## 一、diagnosis.md 逐条处理

1. **读不懂：「旧版讲义」指代不明**（原第 158 行附近）。改：删掉对「旧版讲义」的引用，直接把少写一级 `..` 会报路径不存在的事实讲清楚，不再依赖读者没见过的对照物。

2. **太啰唆：小结表格与结尾段落重复**。改：删掉原来单独的「## 小结」标题（课 04 及任务卡的四节结构里都没有独立小结这一级，加上属于结构层面的越界），把产物对照表并进第 4 节正文；结尾段落删掉与表格重复的「稿子第一句你审过，第一章网页你验收过，配音你听过，封面比例你核过」这句四项并列列举，只留一次判断＋一次收尾问题。

3. **逻辑断裂：「坏掉的命令行工具」没出处**。改：补一句出处，「`tts-dub` 的 SKILL.md 里点出了原因」，和 L15、L1 两处教训的引用方式对齐，全文断言统一都点来源。

4. **AI 味儿：三节收尾套用同一种「X 做完了……下一步/接下来……」播报句式**。改：
   - 第 1 节结尾（原「网页做完了，剩下的字是空的，画面是静的，下一步把它录成一条真正的视频」）整句删掉，第 2 节开头本来就是同一件事（「网页做完先别急着录……」），两句留一句，顺带也解决了第 2 条的啰唆问题。
   - 第 2 节结尾改成「字幕层、分段文案、录制脚本的坑都填完了，这一节剩下的事是等声音。下一节把声音配出来，再回头把这条录制命令真正跑一遍」，用「等声音」这个具体状态代替空泛的「下一步」。
   - 第 3 节开头改成「final.mp4 这时候画面、声音、字幕三样都齐了，抖音主页上要露脸的还差一张封面」，从「产物清单+还差什么」的角度起笔，不再是「X 合上了，最后……」的同款收尾。

5. **哪里在装**，五处逐一处理：
   - 「走完你手上会多出四样东西」和禁令清单里点名的反例「读完你会拿到两样东西」字面撞车。改成「这四步做完，产物落在四个地方」，换一种不带交付预告腔的说法。
   - 「是下一课要接着问的事」删掉，是给下一课卖关子的预告句，删掉后收尾问题本身站得住，不需要这句托底。
   - 「稿子第一句你审过，第一章网页你验收过，配音你听过，封面比例你核过」整句删掉（见第 2 条处理）。
   - 「第一次跑流水线的人十有八九会在这里卡一下」删掉，这是在读者还没报错时替他预判会出错，不符合「拦截易错处只基于可观察状态」的规则；保留后半句「少写一级会直接报路径不存在」这个基于命令实际行为的客观陈述。
   - 三处用直角引号或普通双引号给日常词语加强调（「上一集」「前面说过」、「淡入」「从左滑入」）删掉引号，直接写成上一集、前面说过 / 淡入、从左滑入；`overrides` 字段里的「章/步」改用反引号包成行内代码 `` `章/步` ``，这是格式标记不是强调引号。

6. **【本课额外维度】audio-segments.json 输入缺口，最高优先级**。这是本轮修改里唯一牵动结构的一条。核实 `web-video-presentation` 的 `references/RECORDING.md` 和 `build/` 真实工程里 `scripts/auto-record.mjs` 的代码后确认：`npm run record` 对缺失的音频文件会直接抛错停住，技术上必须先有配音才能录制，这和原稿把「录制」放在第 2 节、「配音」放在第 3 节之后执行的安排有冲突，也正是读者卡住的地方。处理方式：
   - 第 2 节新增 `npm run extract-narrations` 这一步，产出 `build/audio-segments.json`，把第 3 节两条 Prompt 要读的这个文件先在正文里挂上来源。
   - 第 2 节保留 `npm run record` 命令本身和 `?subs=1` 的坑怎么修，但明说「这条命令要等音频真的合成出来才能跑」，不在这一节真的执行它。
   - 第 3 节配音验收完之后，回到第 2 节配置好的那条命令，在第 3 节末尾真正执行 `npm run record` 并做抽帧检查。
   - 四个小节的数目、标题、顺序都没有变，调整的只是"讲机制"和"真正跑"这两件事按技术上的先后关系分落在第 2、3 两节，这一处局部调整已经写进 `outline.md` 的开头说明第 3 条。

## 二、五遍终检

1. **结构遍**：对照 `structure.md` 检查标题法则（四个节标题均为「名词短语：白话补充」，无表演式标题、无悬念钓鱼）；检查小节承接（删掉冗余的第 1 节收尾句后，节与节之间靠内容本身承接，不再需要机械的过渡句）；确认收尾小节不再另起「小结」标题，和课 04 的结构一致。
2. **论证遍**：逐条论点核对支撑来源。「坏掉的命令行工具」补了 SKILL.md 出处（处理见上）；「拿横屏视频帧充数被打回」「录完抽帧才发现漏字幕」两处均点了 `pipeline/lessons.md` 里 L1、L15 的具体条目；路径少一级的问题用 `ls`/`realpath` 实测过两种可达路径，不是凭印象写。
3. **句子遍**：跑 `check_style.py` 报的三处「单句超 80 字」警告（Step 4 交稿时）逐句拆开；Step 6 再跑一遍发现新引入的一句超长（配音开场那句），继续拆开，改到零警告。
4. **温度遍**：核对陪伴三动作配额，全文 4 处「多半是」式条件卡点拦截（按钮漏标记、录制前音频缺失、鉴权没配、封面比例参数没传），均基于命令的可观察行为，不是预判读者会不会犯错；「我」全文未使用；「咱们」未使用；留了一两处口语化收尾（「数清楚目录深度再敲命令，比敲错重敲一次省事」）不去磨光。
5. **符号遍**：跑 `scripts/check_style.py`，硬禁令清零，警告 0 处。

## 三、脚本结果

```
python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py final.md
硬禁令清零。警告 0 处。

LC_ALL=en_US.UTF-8 bash tools/check-lesson.sh final.md 实操
RESULT: PASS
汉字 3182 / 上限 10000；代码行 77 / 上限 200；围栏块 15；排障句(多半是) 4
命令与脚本名核对：8 条全部 ok，无 WARN
```

## 四、主回话验收后的修改

验收通过，主回话点了两处小改，都在第 3、4 节，改完重复制到 `courses/05-用开源Skill跑通一条内容.md`。

1. **第 3 节鉴权口径和课 02 对不上**。原稿的 Prompt 1（配置契约）没定合成脚本放哪、命令行参数叫什么，Prompt 2 却直接喊出了 `scripts/synthesize.mjs --config … --segments …`，是契约没兑现就先用了产物。原稿第 150 行的排障句又写「MiniMax 读的是 `~/.mmx/config.json` 里的 `api_key`」，这是参考仓库那份 `tts-dub` 脚本的实现细节，不是学员按 Prompt 1 造出来的东西该有的凭据来源，也和课 02 第 3.3 节让学员把 key 存成环境变量 `MINIMAX_API_KEY` / `OPENAI_API_KEY` 的口径不一致。核实 `.claude/skills/tts-dub/scripts/providers/minimax.mjs` 的真实代码后确认，鉴权顺序其实是 `env MINIMAX_API_KEY` 优先、`~/.mmx/config.json` 兜底，改动方式是把课文里的说法和课 02 的口径对齐，不是编一个新事实：
   - Prompt 1 补一句契约：「合成脚本放 scripts/synthesize.mjs，命令行参数叫 --config 和 --segments；鉴权从环境变量读，MiniMax 读 MINIMAX_API_KEY，OpenAI 读 OPENAI_API_KEY，这两个变量第 02 课已经设过，不要另外编一份凭据文件」，脚本路径和参数名先在契约里定下来，Prompt 2 才有依据直接喊。
   - 排障句改成：「`[FAIL]` 那一行如果出现，多半是 `MINIMAX_API_KEY` 这个环境变量在当前终端里是空的，第 02 课那条 `wc -c` 自检再跑一次」，指向学员自己设过的凭据，不指向参考仓库那份配置文件。
2. **第 4 节出图命令和课 02 自检形式不一致**。核实 `~/.claude/skills/baoyu-image-gen/SKILL.md` 后确认，`## Usage` 一节写的官方形式就是 `npx -y bun ${SKILL_DIR}/scripts/main.ts …` 这条，全篇没有出现 `/baoyu-image-gen` 这种斜杠调用写法，课 02 用的是 Claude Code 里对同一个 skill 的简写。既然本课原来用的命令本身就是 SKILL.md 里的官方写法，不用改成课 02 那种，改动是在命令后面补一句说明：这条命令和课 02 自检时打的 `/baoyu-image-gen --prompt … --ar 9:16` 是同一个 skill 的两种调法，`SKILL.md` 的 Usage 一节写的官方形式就是这条直接调脚本，课 02 那条斜杠命令是 Claude Code 里的简写，两者最终跑的是同一份 `scripts/main.ts`，避免学员看到两种长得不一样的命令以为是两个工具。

改完重跑 `check_style.py`（硬禁令清零，警告 0 处）和 `check-lesson.sh final.md 实操`（`RESULT: PASS`，汉字 3273、代码行 80/200、围栏块 15、排障句 4、命令与脚本名 8 条全 ok），同步复制到 `courses/05-用开源Skill跑通一条内容.md` 并对交付副本重跑一遍确认一致。
