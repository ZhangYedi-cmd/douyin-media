# 读者诊断（Step 5，subagent 输出，原样存档）

读者画像：会用 Claude Code、没做过自动化流水线、手上没有参考仓库，只有前几课自己做出来的东西（附给它的是 inventory.md 第 07 课结束时一节）。诊断对象：`draft.md`。

## 1. 读不懂
- 第70行代码片段里的 `PRES`（`join(PRES, "public", "audio", ...)`）从未交代是什么，读者审这段"AI 交回来的骨架"时会卡在这个变量上，不知道它是不是就是前面说的"工程目录"参数。
- 第7行先用 `audio-segments.json`、`public/audio/<chapter>/<step>.mp3` 作为"输入"给出，字段含义要到第19行（1.1节）才补充解释，先用后解释，读第7行时是空的。

## 2. 太啰唆
- 第127行结尾段，"判断权还在你自己手上"这一个意思用了四种说法反复讲：先说"PASS 还是 FAIL 现在还是你自己拍板"，接着"你得自己去改""你得自己看该不该管"，再来一句"六个脚本能出数字了，可下判断的还是你"，最后还用反问"行不行？"收一次尾。同一句话说了四五遍。

## 3. 逻辑断裂
- 第123行说改动记进"build/ 内那份 tts.config.json 的 overrides 字段"，但读者手里的 tts.config.json 是挂在 brain/ 下的全局配置，草稿从没交代这份文件是怎么、什么时候在 build/ 里长出一份的，两处位置对不上，也没人解释这个复制机制。

## 4. AI 味儿
无独立于第5类之外的明显硬伤，主要问题并入下面第5类（结尾的反问收束）。

## 5. 装（禁令清单扫描）
- 第98、100行，depause 三条约束的说明里两次用了破折号（"不切词——保留静音两端……"、"绝不原地写——输入输出是同一个文件名时……"），破折号是明令禁止的（虽然裹在围栏块里躲过了脚本扫描，但仍是不该有的写法）。
- 第93行"它不是检查脚本，是改文件的工具"，读起来像"不是……是……"翻案句式的变形。
- 第127行结尾"六个脚本能出数字了，可下判断的还是你。……行不行？"，是倒装收束加反问收尾，且和前一句"下一课要把判断这件事从你手上交出去"意思重复，是硬凑出来的收尾感。

## 6.（本次额外要求）Prompt 输入核对 / 参考仓库文件误认
- 第105行"这个案例记在 pipeline/lessons.md 的 L16 里"：`pipeline/lessons.md` 不在读者的 inventory 里，读者从没见过它。L16 这个编号尤其误导——课程才进行到第 08 课，读者自己的项目不可能已经攒出十六条踩坑记录，这是把参考仓库里真实积累的历史文件，当成读者自己项目里已经写好的东西来叙述，没有用"参考流水线里"这种框定说法。
- 第123行"这条课程从第 07 课起统一按 pipeline/2-create.md 步骤 3 和 pipeline/lessons.md 的说法走"：把读者确实拥有的 `pipeline/2-create.md` 和读者完全没有的 `pipeline/lessons.md` 并列成同等可查的依据，读者没法去核对后者"的说法"到底写了什么。
- 第7行"逐段配音 public/audio/<chapter>/<step>.mp3"这个具体路径约定，直接当成既定事实使用，读者第一次看到这个路径就要拿它去拼后续六个脚本的输入，缺一句归属说明（这是 tts-dub 产出的，不是凭空冒出来的命名）。

---

## 处理结论（写作代理，进 Step 6 前先记结论，逐条落到 polish-report.md）

1. 采纳：`PRES` 补一句归属说明。
2. 采纳：`audio-segments.json`/`public/audio/<chapter>/<step>.mp3` 第一次出现时补一句来源（tts-dub 产出），不用等到 1.1 节才交代。
3. 采纳：加一句回顾 07 课已经讲过的账号级模板复制机制，说明 build/ 内 tts.config.json 从哪来。
4. 采纳：删掉围栏块里的破折号，depause 三条约束改写成正文编号列表，冒号代替破折号。
5. 采纳：改写"它不是检查脚本，是改文件的工具"，换成不带翻案句式的说法。
6. 部分采纳：结尾段落删掉中间的重复铺陈，但任务卡指定的最后两句（"六个脚本能出数字了，可下判断的还是你。让写稿的那个 AI 自己判自己的活，行不行？"）是台账校验用的固定收尾，逐字保留，不改。
7. 采纳：`pipeline/lessons.md` 的 L16 引用改写成明确框定为参考流水线材料的说法，不再让读者以为自己有这份文件。
8. 采纳：把 `pipeline/2-create.md`（读者自己的文件）和 `pipeline/lessons.md`（参考材料）分开表述，不再并列成同等依据。
