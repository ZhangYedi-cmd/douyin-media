# 润色报告（Step 6）

## 一、diagnosis.md 逐条处理

1. 【采纳】`PRES` 未交代来源：在样板脚本骨架代码块里加一行注释「PRES 就是契约里定的那个工程目录参数，缺省用当前目录」，不用回读上文才能懂这段代码。
2. 【采纳】`audio-segments.json`、`public/audio/<chapter>/<step>.mp3` 先用后解释：开篇第三段第一次出现这两个文件名时，加上「`tts-dub` 合成配音时产出的」「它按段落生成的」两处归属说明，不再是凭空冒出来的路径。
3. 【采纳】build 内 `tts.config.json` 位置逻辑断裂：在 3 节 overrides 落点那段补一句「这份 build 内的 `tts.config.json`，是上一课提过的账号级模板 `brain/tts.config.json` 复制进当条内容 `build/` 目录之后的那份副本，你合成配音时已经用过它」，接上第 07 课已经讲过的复制机制，不再是一个新冒出来的位置。
4. 【采纳】depause 三条约束块里的破折号：原来裹在围栏块里躲过了脚本扫描，但读起来仍是不该有的写法。整段改写成正文里的普通编号列表（不再用围栏包裹，因为这段本来就不是要粘贴给 AI 的 Prompt），破折号全部换成冒号或拆成独立短句。
5. 【采纳】「它不是检查脚本，是改文件的工具」翻案句式：改成「`depause.mjs` 审的方式要换一套，它改文件，不判断对错，退出码这条路走不通，重点要放在三条约束有没有真的落进实现里」，从正面说事实，不用「不是……是……」的转折腔。
6. 【部分采纳】结尾段落四五次重复「判断权在你」：删掉中间「跑完六个脚本，硬门槛那三个报了 exit 1，你得自己去改；候选那三个列了风险点，你得自己看该不该管」这一整句复述，只留一次「PASS 还是 FAIL 现在还是你自己拍板」。任务卡「2.1 大纲」和 `plan/handoff-ledger.md` 第 08 行指定的最后两句（「六个脚本能出数字了，可下判断的还是你。让写稿的那个 AI 自己判自己的活，行不行？」）是台账校验用的固定收尾，逐字保留，不算重复，没有删。
7. 【采纳】`pipeline/lessons.md` L16 引用被当成读者自己的文件：改写成「参考流水线自己的踩坑记录（`pipeline/lessons.md`，编号 L16）」，并补一句「这份记录是参考流水线自己的历史，不在你的项目里，记住这条教训就够了」，明确框定为参考材料。
8. 【采纳】`pipeline/2-create.md`（读者自己的文件）和 `pipeline/lessons.md`（参考材料）被并列成同等依据：拆开表述——overrides 落点的依据是「你自己 `pipeline/2-create.md` 里配音审批那道闸口，上一课已经把这条规则写了进去」；`pipeline/lessons.md` L18 只用来佐证「参考流水线自己也是这么走的」，不再和读者自己的契约文件混为一谈。同时发现原稿把 `pipeline/2-create.md` 称作「步骤 3」，这个步骤编号本身就是参考仓库的细节（第 07 课成稿从未用过这种编号，只说「配音审批这道闸口」），已经删掉，全文统一改用「配音审批这道闸口」。

## 二、终检阶段的主要修改

- 结构遍：标题、二级标题均照任务卡「2.1 大纲」原样，未改；开头三段承接第 07 课实际开篇结尾（含直接引用其收尾原句），符合「按它实际写了什么复述」的要求。
- 论证遍：三个硬门槛类脚本（`check-completeness.mjs`、`check-silence.mjs`、`check-source-sync.mjs`）和三个候选类脚本（`check-pace.mjs`、`check-pronunciation.mjs`、`check-av-consistency.mjs`）的退出码分布，全部对照仓库真实脚本源码核实过（三个 `process.exit(bad?1:0)`，三个全程无 `process.exit`），不是按任务卡描述直接采信；`depause.mjs` 的三条约束原样摘自 `content/2026-08-23/claude/build/rec/depause.mjs` 头部注释，未改写事实。
- 句子遍：跑 `check_style.py` 后按 voice.md 的「一句只说一件事」原则，把 5 处超过 80 字的长句在语义断点切开（第 58、80、84、100、108、110、118 行一带），硬禁令全程为零，未触发过一次。
- 温度遍：全文 3 处「多半是」排障句（分别在样板脚本审校、depause 复扫、depause 原地写这三处），符合实操篇最低 2 处的要求；「你」为默认人称，全文没有用「我」，「咱们」未出现；结尾保留任务卡指定的反问收尾，不算刻意造的金句，因为它是台账校验点，逐字对齐第 07 课式的收尾体例。
- 符号遍：`✗`/`✓` 两个符号在正文出现处一律用反引号包成行内代码（脚本输出的字面符号，不是用来装饰文字），代码块内容一次性检查过没有遗留破折号，跑 `scripts/check_style.py` 硬禁令清零。

## 三、留在稿子里、没有删的两点（判断依据）

- 5 处「单句超 80 字」的警告未继续拆分：这些句子是脚本名与判断逻辑的并列枚举（如「真相源同步、死气检查这两个硬门槛，判断逻辑分别是……」），拆成更短的句子会打断枚举的对应关系，voice.md 允许「职责分配句式」用一句话把多组对应关系锁死，判断这类句子读起来仍是内容需要的长度，不是逗号硬拉长句造的气势，予以保留。
- 结尾反问「让写稿的那个 AI 自己判自己的活，行不行？」未改写成陈述句：这是任务卡「2.1 大纲」第 4 块和 `plan/handoff-ledger.md` 第 08 行都逐字给定的收尾问题，改写会让下一课（09 课）的开头承接对不上台账，予以保留。
