# 润色报告（Step 6）

来源：diagnosis.md（subagent，model: sonnet，general-purpose）。以下逐条记录处理方式。draft.md 保持只读作为基线，全部修改在 final.md 上进行。

## 一、诊断问题逐条处理

### 1. 哪里读不懂

1. "job runner" 标题里出现、正文不回收。**已改**：第 5 节开篇补一句定义（"真正管这件事的是一个作业管理模块……参考流水线里这一层叫 job runner"），Prompt 块也把它点名为要实现的对象。
2. 第 2 节"任务队列里有没有活"提前引用第 5 节概念。**已改**：改成"有没有正在跑的写作业（作业具体是什么，第 5 节细讲）"，用 structure.md 允许的括注指出处，不占正文。
3. 第 6 节"第 15 课存量迁移那一课已经把它们改成了……"。**已改，且是本轮最重要的事实修正**：核对第 15 课成稿 4.3 节原文，发现第 15 课明确说了"这一课不布置这个任务，因为学员现在手上还没有飞书闸口"，飞书按钮调 `media flip` 是第 19 课服务写出来第一天就有的设计，不是第 15 课迁移过去的。改成"从第 19 课服务第一次写出来那天起，就是 subprocess 直接调 media flip"，出处改指向第 19 课，全文两处这么写的地方（正文段落和 Prompt 块）都改了。

### 2. 哪里太啰嗦

无实质问题，未改动。

### 3. 哪里逻辑断裂

1. "进队列前检查状态""重复提交直接拒绝"没交代登记机制归谁管。**已改**：在第 5 节引入 job runner 定义之后，明确写"job runner 自己要记着当前哪条内容正挂着哪个作业"，正文和 Prompt 块都补了这句，让读者知道这套登记是 job runner 自身的职责，不是凭空冒出的要求。
2. "第 19 课当时故意没有照默认配置走"的推理链条需要更清楚区分参考流水线的开关和读者自己代码的关系。**已改**：第 6 节第二段开头改成"你自己第 19 课的服务里没有这个开关，是直接把重做写死在自己起线程这条路上"，明确读者代码里不存在任何配置开关，不用误以为自己也该有一个。

### 4. 哪里有 AI 味儿

1. 六个小节结尾同用"A 都通了，但 B 还没做"模板。**已改**：六处收尾交接改写成不同句式（"六个页面各有了自己的数据源。……还没接上。""接口能应一次请求了，文件真的变了，前端怎么知道，是下一步要解决的事。""点一下就能记账的动作，四条全通了。剩下几个动作……""写路径的两条都跑起来了。这两类重活……""读写两条 API 现在都接好了，下一步该做界面……"），第 3 节的"读路径全通了，下一步做写路径"保留（短句本身就和其余五处结构不同，不需要再改）。
2. 全部标题统一"主题词：解释短语"格式。**不改**：这七个标题逐字来自任务卡「2.1 大纲」块，六步流程规定 Step 2/3 不出方案、不改大纲的一级标题，且这个格式是全系列（01 到 26 课）统一的标题惯例（structure.md 标题法则第 2 条），不是本课独有的缺陷。
3. "记忆锚点是一句话。"读来像教学腔标签。**不改**：这一句式在已定稿的第 19、25 课里逐字出现过，是系列既定写法，统一保留避免跟前面课风格脱节。

### 5. 哪里在装

1. 全文收尾反问句被判定为交付预告变体。**不改**：这句是任务卡与 `plan/handoff-ledger.md` 第 27 行强制要求逐字落地的收尾句，全系列每课都以类似的问题收尾，属于本课程既定的收尾规范，不属于风格禁令要拦的"导游腔"。
2. "不是假装你已经有它"带"不是……"翻案句残影。**已改**：删掉这半句，段落改成"这一课要用 Prompt 让 AI 把它加出来。产出是……"，直接陈述，不再用"不是"起势。

### 6. 本课专项：把参考仓库文件当成读者已有的东西

1. 【严重，已改】第 5 节"仓库里 `routes/jobs.ts` 实际注册了五个作业端点"没有"参考流水线"限定词，读起来像是读者自己仓库已经有这个文件。改成"参考流水线的作业路由一共注册了五类作业端点……你自己的 server 包这一步也只用做出这三类"，去掉具体文件名，把"实际注册了"这种既成事实语气换成"参考流水线"限定 + 读者要新建的措辞。
2. 【已改】第 6 节"对照第 19 课飞书服务里两处直接 spawn 无头 CC 改状态的代码段（确认发布、打回自动重做各一处）"。核对第 19 课成稿原文，发现第 19 课只实现了打回自动重做这一处真代码，确认发布当时只发了占位卡、没有按钮处理代码，"两处"的说法不准确。改写后区分两条路径：打回重做是"换代码"，确认发布是"新接一条，不用先找旧代码再改"，正文段落、对照表、Prompt 块三处同步改。
3. 【已改，见上文第 1.3 条】"第 15 课存量迁移"的错误出处，改指向第 19 课。

## 二、终检阶段的主要修改（五遍法）

- **结构遍**：七节结构和标题按任务卡固定大纲未动；开篇三段承接、记忆锚点、输入产出三件事俱全；收尾落在 handoff-ledger.md 第 27 行原句上，逐字核对过。
- **论证遍**：六处"多半是"排障句均落在真实机制上（合并窗口太短、watcher 漏目录、把自评成功当唯一依据），四处对比表格全部是可枚举事实，无解释性长句混进表格。
- **句子遍**：拆分了开篇输入清单那句和 SSE 三件配合那句的超长句，job runner 定义句也拆开；`check_style.py` 剩余的单句超 80 字警告多为分号连接的并列短句（如 "publish/rework/apply-proposal 各自成败怎么判"这类三段式），读起来是完整的一套并列关系，未强行切断以免打散职责分配句式。
- **温度遍**：全篇未出现"我"（除 Prompt 块内读者对 AI 说话的第一人称，符合惯例）；"咱们"未使用；卡点拦截（"多半是"）3 处，够本课作为实操篇的下限；未发现需要额外找回的松句子缺口，也未发现温度手法超配额。
- **符号遍**：`python3 check_style.py final.md` 硬禁令清零，警告全部是句长提示，已在句子遍处理过其中最长的几处。`LC_ALL=en_US.UTF-8 bash check-lesson.sh final.md 实操` 输出 RESULT: PASS。

## 三、事实核对记录（供交付回复引用）

逐项在仓库里 grep 确认，来源见 analyzed.md；无未核实项。第 15 课/第 19 课的两处事实性误引已在本轮改正（见上文 1.3 和 6.2）。

交稿前最后一遍复核又抓到一处：第 5 节原来写"模型自评报错，状态其实已经翻对了；模型自评成功，状态却压根没动"，对照第 25 课成稿 5.1 节原文重新核对，发现第 25 课的两个真实案例分别是"创作任务撞会话限额，自评报错、状态确实也没推进"（自评和状态一致，不是反例）和"发布任务子进程声称成功，meta.yaml 状态却停在审核通过没变"（这才是自评和状态对不上的真实案例）。原句前半句是凭空编的对照，已删掉，改成只引第 25 课真实存在的那一个案例（发布任务声称成功、状态未变）。

## 四、交稿后主回话复核的四处修正

1. 第 4 节 `backlog-apply` 只写了 `media backlog apply <id>`，漏了必填的 `--action`/`--proposal` 两个参数。核对第 21 课成稿"资产读写表"一节原文："命令本身要求带一个指向报告文件路径的参数，不是可选项，没有报告路径这条命令直接拒绝执行"，以及参考仓库 `actions/whitelist.ts` 的 `backlogApplyArgs` 实现（`--action`、`--proposal` 两个参数）。已改：表格、正文新增一段、Prompt 块三处同步补上这两个参数和对应的请求体字段，并点明这条和第 21 课"落笔必须绑一份提议报告"是同一件事。
2. 第 5 节作业超时表（20/45/10 分钟）取自参考流水线 `jobs/defs.ts` 的真实值，但原文没有说明这是起步值还是要求值。已改：表后加一句"这三档超时是参考流水线的起步值，你自己按任务实际耗时定，不用照抄这三个数字"。
3. 第 3 节 watcher 监听列表里的 `pipeline/logs` 是参考仓库才有的目录，读者第 03 课骨架里 `pipeline/` 只是五份契约文档，没有 `logs/` 子目录。已改：监听列表换成读者第 03、21 课实际交付过的 `content/`、`harness/logs/`、`harness/tasks.md`、`dashboard.md`，并在 Prompt 里加一句"仓库里没有的目录不要写进去"。
4. 第 1 节详情接口原来没有承接第 28 课成稿"决策区不在前端抄状态机，只按接口返回的当前允许迁移到哪些状态渲染按钮"这条设计。核对第 28 课成稿原文和参考仓库 `projections.ts` 的 `allowedTransitions` 字段实现，确认这个字段该由本课的 `GET /api/content/:slug` 提供。已改：表格数据来源列补"含当前允许的迁移"，正文加一段说明，Prompt 块新增第 5 条要求（列出 core 包 state 模块迁移表当前允许翻到的状态，每条标是否要理由），验收段落加一句核对这个字段跟 state 模块迁移表对不对得上。

四处改完，重跑 `check_style.py` 硬禁令清零，`check-lesson.sh 实操` RESULT: PASS，`writing/27-console-server/final.md` 与 `courses/27-后端-读写两条路径.md` 已重新同步为同一份内容。
