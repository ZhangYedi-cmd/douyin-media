# 润色报告：第 20 课《发布 Skill 与 dry-run》

## 一、diagnosis.md 逐条处理

诊断由 sonnet 子代理完成（读者画像：用 Claude Code/Cursor、没做过自动化流水线、按顺序做完前 19 课的学员），额外加了一条本课专项检查（Prompt 输入是否读者手上没有、是否把参考仓库文件当成读者自己的文件）。以下按诊断给出的严重程度处理。

### 高严重度

1. **`4-publish.md` 撞名混用（第 2 节 Prompt 1 第 2 步等多处）**：诊断指出全文把"每条内容自己的 `content/<slug>/4-publish.md`"和"阶段契约 `pipeline/4-publish.md`"当成同一份文件混着引用，AI 执行 Prompt 时可能把刚编辑的阶段契约当成某条内容的物料去解析，判定为架构性错误。已改：
   - 核实 `content/_template/4-publish.md`（第 3 课六份模板之一）本来就已经定好六个字段，`pipeline/4-publish.md`（第 3 课立的阶段契约）当时只有输入/输出/状态翻转/闸口四项，两份文件是撞名不同物——这个结构类比第 19 课已经点破过的 `pipeline/3-review.md` 与 `content/<slug>/3-review.md`。
   - 重写第 2 节开篇段落，明确区分两份文件各自缺什么、这一课分别要对它们做什么。
   - 重写 Prompt 1，第 2 步显式写成"读 `content/<slug>/4-publish.md`（每条内容自己那份，不是 `pipeline/4-publish.md` 那份阶段契约）"，消除指代不明。
   - 全文所有裸写 `4-publish.md` 的地方（第 5、37、76、102、110、127、150、154 行一带）逐一改成"内容自己那份 `4-publish.md`"或直接写 `content/<slug>/4-publish.md`。
   - 规格表的"来源字段"列去掉了原来重复写 `4-publish.md` 的写法，改成表格标题下一句话统一说明来源文件，避免每行都出现容易被误读的裸文件名。

2. **回填逻辑与真实源码不符（第 4 节）**：诊断核对 `publishDone.ts` 源码，确认 `--url` 只写 `meta.yaml` 的 `publish_url` 字段，`media publish-done` 从不触碰 `4-publish.md`；但原稿的核对项让读者去 `4-publish.md` 里查"实际发布时间/作品链接/用的账号"三行回填，这个动作从未被 Prompt 建造过。已改：
   - 核实 `content/_template/4-publish.md` 本身就带着"发布回填"一节（实际发布时间、链接两项），这不是本课新建的结构。
   - 明确写清楚：回填是 skill 真发成功后**直接编辑** `content/<slug>/4-publish.md` 这份文件，账号另记一笔，这个动作和调用 `media publish-done` 是两件平行的事，不经过这条 CLI 命令。
   - 第 4 节结尾核对项相应改成"发布回填那两行"（实际发布时间、链接），去掉了不存在的"账号写进 4-publish.md 由 publish-done 完成"这层暗示。

3. **`不是……是……` 句式在全文形成修辞骨架（6 处）**：诊断指出这个未被脚本硬禁令捕获（脚本只禁"不是……而是……"，本文用的是不带"而"的变体）但仍是明显的 AI 味句式，尤其开篇记忆锚点和收尾判断句首尾呼应，构成整篇的修辞骨架。逐条判断后处理：
   - 第 5 行"记忆锚点是一句话：……不是走流程，是……"：这是任务卡「2.1 大纲」块给定的记忆锚点原句（`plan: fixed-from-card`），不改动措辞，但把它后面我自己写的呼应句"不是靠 skill 自己嘴上说成功"改成"skill 自己嘴上说成功不算数"，去掉呼应模式。
   - 第 33 行"这一步不是一条 Prompt 钉死，是……"：任务卡 2.1 大纲第 2 节核心判断的原句式，L2 教法档的固定表述，保留。
   - 第 78 行（原）"skill 默认的行为不是执行，是摆出来"：我自己的写法，非任务卡强制，改成"skill 默认要做的事，是把命令和物料摆出来等人看，不直接执行"，拆掉对称结构。
   - 第 145 行（原）收尾判断句"证据不是 skill 没报错，是……"：核对第 19 课 final.md 第 149 行"这一课能拿出来的证据不是服务没报错，是状态文件和审计记录里能查到的那几行"，确认这是系列固定收尾句式（同一表述在第 19 课已用过），保留以维持系列一致性。
   - 第 72 行（原）"而不是拿空字符串接着往下拼命令"：改成"没有拿空字符串接着往下拼命令"，去掉"而不是"连接词。
   - 第 7 行"但引的是形状，不是给你抄的现成文件"：核对第 19 课 final.md 第 7 行"但引的是形状，不是给你抄的现成文件"，逐字相同，确认是系列固定表述，保留。

### 中严重度

4. **`--tags` 说明语病（表格第 43 行）**：原句"`sau` 会自动补 `#`，留着不去重复加就变成 `##`"主谓不清。改成"发布时 `sau` 会自动把 `#` 补回去，没去掉原来的号就会叠成 `##`"。

5. **approved 内容前提未列入输入（第 2、3 节 Prompt）**：两条 Prompt 都要求"拿一条 approved 内容"实测，但开篇"本课的输入"没把这条内容列进去。已改：开篇输入段落新增"第 19 课走完审核流程、状态翻到 `approved` 的那条内容，它自己那份 `content/<slug>/4-publish.md` 现在是填好的"。

6. **示例代码块空值/实值混杂（第 4 节模板块）**：原稿"真发成功后 4-publish.md 该长什么样"混了空字段和 `assets/cover.png`、`assets/final.mp4` 这类具体值，读者分不清是照抄的文件名还是随手举例。已改：直接改用 `content/_template/4-publish.md` 的真实原文（逐字核对过），不再自造混合示例，同时这份文件因为整段落在代码围栏内，`**` 加粗符号按规则豁免硬禁令。

7. **字段"还没定"的说法在第 7、33 行重复**：已改：第 7 行开篇段落收窄为一句带过（"还没有物料字段和 dry-run 规矩"），完整的四项列举只在第 33 行段落展开一次。

### 低严重度（判断后未改，理由如下）

8. **"记忆锚点是一句话……拆成 N 件事去核对"结构**：诊断标为导游腔边界案例。核对第 13、16、17、19 课 final.md，均用同一固定结构展开记忆锚点，是系列体例而非本课单独注水，不改。

9. **收尾句"选题池会空，大脑不会自己长"轻度拟人**：这是 `plan/handoff-ledger.md` 第 20 行"结尾抛出的问题"列的逐字原文，交稿要求逐字落在台账上，不改动措辞。核对其余几课收尾句也都带轻度比喻或拟人，属体例延续。

10. **`media publish-done` 示例缺 scheduled→published 收尾分支**：诊断核对 `publishDone.ts` 源码确认这是真实存在的第三种分支（`meta.status` 已是 `scheduled` 时再次调用同一命令、不带参数即可收尾成 `published`），原稿三个示例漏了这种情况。已改：第 4 节命令示例从三条扩到四条，按"当前状态"分四种情况列出，并在示例前加一句说明"命令怎么分流看的是这条内容当前的状态，不是你传了哪个参数"。

### 交稿前追加核对（诊断之外，自行发现）

11. **`pipeline/4-publish.md` 骨架的"闸口"当时是不是完全没提 dry-run**：三关自查时又通读了一遍第 3 课成稿 `courses/03-立骨架与目录约定.md` 第 3.4 节，发现它的课文原话是"发布契约管的是全流水线唯一一处真正不可逆的动作，所以闸口写了两层：先按 dry-run 拼出最终命令和物料，推回去做二次确认，人点确认之后才真发"——闸口那一句当时已经带过一笔 dry-run 的意思，只是没有展开成能照着执行的判据表和字段清单。原稿第 7、33 行写成"还没有物料字段和 dry-run 规矩""也没定 dry-run 那条规矩"，把这笔带过的内容算成了完全空白，判定为轻微失实。已改：两处都改成"闸口那句只带过一笔 dry-run，字段和判据都还没有展开"这类措辞，只说这一课把它从一句话展开成判据表，不再声称它是这一课凭空新增的规矩。

### 未采纳的诊断（无，全部诊断均已处理或说明保留理由）

## 二、五遍终检（Step 6 后半）

1. **结构遍**：标题为内容描述（无钓鱼/对仗/数字党），五节均以内容名词短语命名；开篇三段不抒情不背景铺垫，直接接第 19 课结尾原句；节间用问题交接衔接，无空转过渡词；收尾节沿用系列"做到了什么/还看不到什么/抛问题"结构，逐字对齐台账第 20 行。
2. **论证遍**：关键判断均落在四件套支撑之一——真实源码（`publishDone.ts`）、官方参考文件（`SKILL.md`/`field-mapping.md`/`sau-setup.md`）、真实教训记录（`pipeline/lessons.md` L5-L8）、反例（L5/L6 两条踩坑）。未发现空泛论点。
3. **句子遍**：逐段过风格规则，修正若干长句和黑话残留；跑 `check_style.py` 后剩余 12 处警告全部是"单句超 80 字"这一类，逐条核对性质与第 19 课 final.md 的 11 处同类警告可比，判断为系列既有的行文节奏，不强行切碎（切碎会牺牲论证的连贯性）。
4. **温度遍**：卡点拦截（"多半是"）3 处，覆盖登录、字段读取、dry-run 三个环节；"我"仅出现在 Prompt 代码块内（AI 视角第一人称，7 次，均在围栏内不计入配额）；叙事正文 0 处"我"、0 处"咱们"，符合作者退后要求；未发现俯视腔、猜心句、导游腔、金句化收束（不含台账/大纲强制原句）。
5. **符号遍**：`python3 check_style.py final.md` 硬禁令命中 0 处。

## 三、交稿后第二轮：主回话核出的三处「参考仓库当成读者自己」

主回话复核后指出三处仍把参考仓库的东西说成了读者自己的，逐条核实、逐条改：

12. **`pipeline/lessons.md` 两处（第 2、4 节）**：核对第 3 课成稿原文"踩坑的故事只登记在 `pipeline/lessons.md`，你自己的这份现在是空的，等真踩到坑再写第一行"，读者到第 19 课也从没真正发布过任何内容，L5-L8 这几条教训只存在于参考流水线的 `lessons.md` 里。已改：第 2 节"`pipeline/lessons.md` 记的 L5、L6 两条教训"改成"参考流水线的 `pipeline/lessons.md` 记着两条教训……你自己的 `pipeline/lessons.md` 第 3 课立起来时还是空的，这两条算是提前替你踩过的坑"；第 4 节"第 19 课之前那批发布真实失败过两次"这种把参考仓库发布史算成读者亲身经历的说法删掉，改成"参考流水线的 `pipeline/lessons.md` 记着两次真实失败又原样重试一次就过的案例"。

13. **`content/_template/4-publish.md` 六个字段的来源（第 2、4 节）**：核对第 3 课成稿，那一课只让 AI 按一张"谁产/何时产/必产吗"的对照表生成六份空模板，全文 grep 不到"话题标签""建议发布时段""发布回填"这几个具体字段名，原稿"第 3 课那份当时就定好了……这六项""出审前你已经照它填过至少一次"两句站不住。已改：第 2 节开篇段落改成"字段名不用你现想：参考流水线的模板里，`4-publish.md` 定的是……这六项，这一课就照这个规格来"，并加一句"你自己第 3 课生成的那份、以及第 19 课那条走完审核流程的内容已经填过的那份，字段名可能跟这六项对不上，先照这六项把它们对齐"；Prompt 1 里新增一步，让 AI 检查读者自己的 `content/_template/4-publish.md`，字段名对不上的地方改过来对齐；第 4 节展示的模板代码块改标成"参考流水线的模板长这样"，块后说明改成"第 2 节那一步已经把你自己那份文件的字段对齐到这六项……出审时应该已经填成真实值"，不再声称第 3 课就已经填过。

14. **第 2 节引第 19 课的撞名说法**：核对第 19 课成稿，全文 grep 不到"撞名""同名"这两个词，"跟第 19 课提过的 `pipeline/3-review.md` 和 `content/<slug>/3-review.md` 那对撞名文件是同一类情况"这句是不准确的引用归属。已改：直接删掉这句引用，撞名的解释改成本课自包含的一句话："两份文件名字撞了，指的不是同一样东西：一份是每条内容自己的物料文件，一份是全局阶段契约。"

三处改完重跑三关，`courses/` 与 `writing/20-publish-skill/final.md` 已用 diff 确认字节级一致。

## 四、脚本结果（第二轮改后）

```
python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py final.md
→ 硬禁令清零。警告 12 处（均为单句超 80 字，判断保留）。

LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh final.md 实操
→ 汉字 4302 / 上限 10000；代码行 54 / 上限 200；围栏块 7；排障句(多半是) 3
→ 装腔词表：通过；过度自信词表：通过；标题模板前缀：通过
→ 状态值核对：approved/published/scheduled 全部 ok
→ 命令核对：media flip / media publish-done 全部 ok
→ RESULT: PASS
```
