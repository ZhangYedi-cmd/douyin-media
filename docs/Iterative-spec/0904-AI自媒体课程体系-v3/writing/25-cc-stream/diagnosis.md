已通读全文，并核对了参考仓库里 `tools/console/packages/cc-stream/`、`core/src/lock.ts`、`server/src/jobs/verdict.ts`、`06-施工记录.md` 等源文件，以及前 24 课的 draft 用来验证连续性。诊断如下，按严重程度降序。

## 综合问题清单（最严重的几条）

**1. types.ts、normalize.ts 全篇没有对应的 Prompt 块，六个文件的承诺兑现不了四分之二**
- 位置：第 9 行承诺"产出...一个 cc-stream 包（六个文件：types.ts、transport.ts、normalize.ts、milestones.ts、spawn.ts、index.ts）"；全文实际只有 5 个 Prompt 块（65 行 milestones.ts、90 行 transport.ts+spawn.ts+index.ts、130 行裁决函数、151 行锁、178 行终止逻辑）。
- 问题类型：逻辑断裂 / 第 6 条（Prompt 输入可执行性）
- 为什么是问题：这门课的读者不写实现代码，全靠 Prompt 驱动 AI 生成文件。第 1、2 节花了大量篇幅讲 types.ts 的七种事件、normalize.ts 的去重逻辑，但从未给出一条"新建 types.ts"或"新建 normalize.ts"的 Prompt。第 90 行的 Prompt 只说"最后建 index.ts，把这几个函数和用到的全部类型统一导出"，隐含类型已经存在，但类型从未被要求生成过。对照参考仓库的真实 normalize.ts（144 行，含 resolveResultText 的 Bash stdout/stderr 优先级判断、tool_use_result 多形状兜底等相当复杂的逻辑），这不是可以靠"顺便带出"糊弄过去的细节缺失，是读者真按这门课操作会卡住的硬缺口。

**2. 第 6.2 节：前一句话和紧跟的表格自相矛盾**
- 位置：189 行"两种场景下要做的事其实是同一件：重新去读这条内容自己的 meta.yaml 和留痕文件"；对照 191-194 行的表格。
- 问题类型：逻辑断裂（前后矛盾）
- 为什么是问题：表格里"人工取消"一行的兜底做法是"复跑一次裁决函数，现场读 meta.yaml"，"进程或服务意外重启"一行却是一套三分支判断（有终局记录用终局记录／只有中途事件标不确定／两者都没有算没跑过）。这两件事明显不是"同一件"，前面的断言被自己紧接着的表格推翻。

**3. "终局记录""留痕文件""中途事件"这几个关键词从未被定义**
- 位置：189、194、196 行（第 6.2 节）
- 问题类型：读不懂 / 指代不明
- 为什么是问题：这是整个恢复机制的核心概念——重启之后靠什么文件把结论兜回来——但"终局记录"具体是哪个文件、什么格式，和前面第 4.1 节讲过的 spawn.ts 原始 JSONL 落盘（logPath）是不是同一份东西，第 5 节裁决函数读的 meta.yaml 算不算"终局记录"，全文都没交代。读者读到这里会不知道自己该去查哪个文件。

**4. 第 6.1 节 Prompt 暗示读者已有一版终止实现，但第 4.1 节明确说这部分被留白**
- 位置：85 行"具体怎么终止...放在第 6 节单独讲"；96-97 行 Prompt "opts 留一个可选的超时时长，到时候触发终止（怎么终止下一步会补）"；对照 178-183 行 Prompt "把子进程的终止实现改成先礼后兵...原来只有超时自动触发这条路径会走这层保护，现在要让调用方手动喊停时也走同一套逻辑"。
- 问题类型：逻辑断裂 / 第 6 条
- 为什么是问题："改成""原来"这两个措辞要求 AI 去修改一份已经存在、且已经对超时路径生效的先礼后兵实现。但按第 4.1 节的 Prompt，终止机制从头到尾都没写，只留了一个参数占位。读者此刻手上并没有"原来"那一版可供"改"的代码，这条 Prompt 的措辞和课程自己前面的交代对不上。（这套"原来…现在…"的话术是从参考仓库的真实 git 历史里搬来的，仓库里确实先有超时版本、后来才扩展到手动取消，但读者在这门课里的构建路径没有这段历史。）

## 分维度小结

**1. 读不懂**
- "咨询锁"（142、151 行）：全系列课程首次出现这个术语，没有解释为什么叫"咨询"（相对于操作系统强制锁而言，只是约定俗成、靠进程自觉遵守），只讲了机制没讲名字的含义。中等严重度。
- "半文档化的原始事件对象"（15、21 行）：这个说法直接搬了源码注释的措辞，但没有展开——它的实际含义是"CC 的 stream-json 输出没有官方稳定契约，字段可能随版本变化"，这层含义关系到后面"解析失败直接跳过"这条设计为什么合理，但没讲透。较轻。
- 第 6.2 节的"终局记录"等术语已在上面列为独立条目，不重复计分。

**2. 太啰嗦**
- 第 1 节（13、15、17 行）："三层各自不认识业务"这一个意思连续说了三遍：第 13 行"业务知识只留在第三层的规则表里"，第 15 行"三层代码本身，没有一行认识发布、配音、复盘这些词"，第 17 行又用反例"如果归一层或者里程碑引擎的代码里直接判断某个工具调用是不是在发布"重申一遍。第三次没有带来新信息，纯粹重复。

**3. 逻辑断裂**
- 已在综合清单第 2、4 条列出，属本维度最重的两处。
- 全文其余部分（三层解析→七种事件→里程碑→拉起进程→裁决→锁→收尾→恢复）的推进顺序本身是顺的，没有发现其他跳步或结论无依据的地方；对照真实源码（lock.ts、verdict.ts、spawn.ts、normalize.ts）逐条核实，数字和机制描述（三十秒陈旧阈值、十秒宽限期、二十行截断、十二份日志一百一十一个 thinking 块）全部准确，不属于编造。

**4. AI 味儿**
- 全文五次使用几乎同一个句式模板"现象 X，多半是原因 Y；这种时候该做的是/该改的是 Z"（107、138、148、175、196 行）。这个句式本身符合"拦截易错处只基于可观察状态"这条规则允许的写法，但五次雷同的排布让人一眼看出是套公式写出来的排障提示，读到第三次就能预判下一句怎么说，模板感明显。
- 全文结构高度重复："一段说明＋一张对照表＋一个 Prompt"这个三段式在六个小节里出现了六次（层级表、事件表、里程碑表、裁决类型表、终止信号表、恢复规则表），单独看每张表都有信息量，但连续六次同一节奏，读到后半篇容易觉得是照模具灌出来的。
- 开篇"记忆锚点是一句话。"（7 行）带一点元叙述色彩（提示读者"这是要记住的锚点"），但这是系列固定的开篇设备（第 21 课用的是"这一课的记忆锚点是"），程度很轻，且是全系列统一体例，不单独算这一课的毛病。

**5. 装（对照禁令清单逐类扫）**
- 标点：全文正文标点干净，没有破折号、直角引号、星号、emoji；唯一出现的直引号在 69 行的 Prompt 代码块内，块内不受限制，不算违规。
- 金句化 / 倒装收束 / 对仗顺口溜：无。
- 导游腔与交付前预告（"你大概会有三个疑问""按下不表""先说明一下"等原话）：无直接命中；85 行"这么关键的一步不能一笔带过，放在第 6 节单独讲"带一点交代写作安排的味道，但更接近技术文常见的章节指路，程度很轻，不算硬伤。
- 猜心句（"你可能会想"）：无。
- 俯视腔（"其实很简单""显然""切记""很多人都做错了"）：无。
- 作者加戏（"我当年""这一句是关键"）：无。
- 表演式标题：无，各级标题都是内容描述，"三个坑与一次真跑"这类数字标题是对实际内容条目数的如实概括，不是数字党式的悬念钓鱼。
- 黑话堆砌：无，专业词汇（SIGTERM/SIGKILL、JSONL、OAuth token 等）都配了解释，不是空转的黑话堆砌。

**6. Prompt 输入的真实持有情况（本课额外核查项）**
- 第 5.2 节（151 行）"看一下 media CLI core 包里已有的锁实现...给无头作业复用同一套"：措辞准确，明确框定为"复用已有的"，没有暗示读者自己已经写过 cc-stream 专属的锁，处理得当。
- 第 5.1 节裁决函数、第 3 节里程碑引擎、第 4.1 节 transport/spawn/index：都正确框定为"这一课要新造的东西"，没有误导读者以为自己已经有现成实现。
- 唯一的问题是综合清单第 1、4 条：types.ts/normalize.ts 缺 Prompt（属于"该造的东西没给出制造指令"，而非"把参考仓库的东西说成读者已有"），以及第 6.1 节的"改成"措辞隐含读者已有一版可改的终止实现，而这版实现在本课的构建路径里从未真正出现过。
