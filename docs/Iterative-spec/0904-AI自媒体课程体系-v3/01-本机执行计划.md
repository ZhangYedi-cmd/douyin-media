# 01 本机执行计划：33 课串行写作，每课一个子代理

> 日期：2026-09-04 ｜ 上游：`00-ADR.md`（课程目标、33 课大纲、D1 到 D16 决策）
> 执行方式：主回话当调度与验收，每课派一个全新的写作子代理跑 `/ai-arch-tutorial-style` 六步流水线，子代理内部再派一个读者诊断子代理。33 课严格串行，第 N 课的成稿是第 N+1 课的第一材料。
> 本文件给执行者看，执行者可以是我这个主回话，也可以是接手的任何一个会话。假设执行者没读过 Multica 上的讨论。

**目标**：在本机从第 01 课起把 33 课全部写完，落盘到本目录 `courses/`，每课过三道验收关，全链承接核对通过后提交。

**做法**：先做阶段 0 把子代理需要的全部背景固化成文件（背景包、任务卡、台账、验收脚本），这样每个子代理只读文件就能拿到全部上下文，不依赖主回话的记忆。然后阶段 1 一课一课串行跑，主回话在每课之间做验收。阶段 2 收尾。

**工具**：Claude Code Agent 工具（写作子代理和诊断子代理全部用 sonnet，09-04 你的口径；主回话居中调度与验收）、`~/.claude/skills/ai-arch-tutorial-style/`（1.9.0）、`tools/check-lesson.sh`（从旧目录 HEAD 版本改出）、git。

## 全局约束

每个任务默认包含下面这些，子代理提示词里必须原样带上（已固化在背景包里，子代理读背景包即可）。

1. 课程目标：让学员通过这条流水线学会跟 AI 干活，掌握 SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环。抖音是载体。
2. 读者零仓库。每课 Prompt 的输入只能是学员前几课自己造出来的东西，不许以「读仓库里某个文件」开头。这是 v2 被退回的根因。
3. 学员不写一行实现代码。学员只写中文需求与判据、SPEC 文档、验收结论。
4. 篇幅不设固定值，按内容重要性定，灵活；唯一硬线一课不超过 1 万汉字（脚本判 FAIL）。四块重点（SPEC、Loop、状态收敛、反哺）和心法课往厚里写，工具操作课讲清流程就收。（09-04 你的口径。）视频制作（剪、配音、多音字、封面）走一遍流程即可。判断标准：讲「工具怎么用」压，讲「这类问题怎么设计」留。模块 2 的 07 到 10 课讲的是 Loop，不压。
5. 代码行硬上限：实操 200 行，方法论 30 行。实操篇围栏块至少 3，排障句「多半是」至少 2。
6. 写作语气：禁装逼、禁过度自信、做暖心工程师，压过一切。`check_style.py` 硬禁令清零才能交稿。
7. 零编造：状态值必须在 `tools/console/packages/core/src/state.ts` 里；命令、脚本名、路径写之前先 grep 仓库；数字要有出处。
8. 一课一个产物，一句话说得出学员多了什么；每节要有判据或产物。某课 Prompt 超过 5 条就该拆节。
9. 严格串行，承接第一真相源是上一课成稿，写前读它的开篇和结尾两段。失败重试一次，仍失败停下上报，不跳过。
10. 仓库路径：`/Users/yedizhang/yedi-study/douyin-media`。旧目录 `docs/Iterative-spec/0901-AI自媒体课程体系/` 已删除，凡是引用它的地方按本目录对应文件替换。

## 目录结构

```
docs/Iterative-spec/0904-AI自媒体课程体系-v3/
├── 00-ADR.md                     决策记录（已就位）
├── 01-本机执行计划.md              本文件
├── plan/
│   ├── 00-写作背景包.md            每个子代理第一份必读，阶段 0 任务 0.2 产出
│   ├── cards/NN.md                 33 张任务卡，任务 0.5 产出
│   ├── handoff-ledger.md           承接台账，任务 0.6 产出
│   ├── inventory.md                学员产物累计清单，任务 0.6 产出
│   ├── progress.md                 进度与断点记录，任务 0.1 建，每课更新
│   └── old-courses/                旧稿素材：v1 的 18 篇、v2 的第 02 课，任务 0.3 导出
├── tools/check-lesson.sh           单篇验收脚本，任务 0.4 产出
├── writing/NN-<slug>/              每课六步中间产物（analyzed / outline / draft / diagnosis / final / polish-report）
├── courses/NN-<课名>.md            成稿
└── appendix/                       课程材料附录，阶段 2 产出
```

---

## 阶段 0：准备（七个任务，0.2 到 0.6 可以并行派子代理）

### 任务 0.1 建目录与进度文件

- [ ] 目录已建（`plan/cards`、`plan/old-courses`、`courses`、`writing`、`tools`、`appendix`）。
- [ ] 写 `plan/progress.md`，初始内容：

```markdown
# 进度与断点

| 课 | 状态 | 子代理开始 | 交稿 | 验收 | 备注 |
|---|---|---|---|---|---|
| 01 | 待写 | | | | |
（02 到 33 同上）

状态取值：待写 / 写作中 / 待验收 / 退回修改 / 通过。
断点续跑规则：状态是「写作中」的课，看 writing/NN-*/ 下六个文件哪个还没有，从缺的那一步续，不重跑前面的步骤。
```

- [ ] 提交：

```bash
git add docs/Iterative-spec/0904-AI自媒体课程体系-v3
git commit -m "docs(course-v3): ADR、执行计划与目录骨架"
```

### 任务 0.2 写作背景包 `plan/00-写作背景包.md`

这是解决「每个子代理都了解背景和约束」的核心件。每个写作子代理的提示词第一句就是读它。内容如下，原样落盘：

```markdown
# 写作背景包：动笔前必读

你在写《AI 自媒体流水线》课程 v3 的一课。全书 33 课，你只写其中一课，但要知道全书在干什么。

## 1. 这套课要教什么

让学员通过从零搭一条抖音自动化流水线，学会怎么跟 AI 干活：掌握和 AI 协作的流程，懂得怎么引导 AI，掌握 Loop 思维和架构设计。抖音只是载体。学完的产物目标是 1:1 实现参考仓库 `/Users/yedizhang/yedi-study/douyin-media`：media CLI、飞书人审闸口、本地控制台、十个自封装 skill、一个子代理、五份阶段契约、brain 五份文件。

四块重点掌握项往厚里写：SPEC CODING、Loop 搭建、CLI 与状态收敛、反哺大环。这四块字数不设限，讲透为准。

## 2. 读者是谁，手上有什么

读者会用 Claude Code 或 Cursor，没做过自动化流水线，认知停在单轮对话。

读者手上没有参考仓库。他只有课文，和前几课自己做出来的东西。`plan/inventory.md` 列了每一课结束时学员手上应该有什么，写本课之前查一遍：本课每一条 Prompt 的输入，必须是那张表里本课之前已经出现的东西。让读者「读 pipeline/2-create.md」而他还没写过这个文件，就是错的。这是上一版 15 课被整体退回的原因，不要再犯。

你自己可以读仓库，那是你的材料。但仓库里的信息只能用来保证你写的东西真实存在、判据正确，不能用来替读者补全他没有的前提。你读了源码觉得「这样就懂了」，恰恰说明课文没讲清楚。

## 3. 四条铁律

1. 学员不写一行实现代码。学员写的只有三类东西：中文的需求与判据、SPEC 文档、验收结论。代码、配置、脚本一律由 AI 产出。课文里出现大段代码就说明写偏了。代码块只用来展示「AI 交回来应该长什么样」，供对照。
2. 视频制作本身（怎么剪、怎么配音、多音字和语速、封面怎么画）走一遍流程即可，不深讲。判断标准：这一段讲的是「这个工具怎么用」还是「这类问题该怎么设计」，前者压，后者留。模块 2（07 到 10 课）表面讲配音质检，实际讲 Loop 架构，不属于可精简范围。
3. 零编造。状态值必须在 `tools/console/packages/core/src/state.ts` 里存在；命令、脚本名、文件路径写之前先在仓库 grep；百分比和统计数字要在正文就近给出处，没有出处就删。本仓库的经验值不写成普适规律，三轮上限是项目约定不是统计结论。
4. 禁装逼、禁过度自信、做暖心工程师。这条压过所有写作规则。命中一处整篇退回。具体样子见 `~/.claude/skills/ai-arch-tutorial-style/SKILL.md` 的「成稿绝对不能出现」一节。

## 4. 教法分三档

按有没有互斥的选型、选错了返工贵不贵分档，不按行数。

| 档 | 判据 | 教法 |
|---|---|---|
| L1 轻 | 实现路线唯一；单文件；错了重跑一次 | 一条 Prompt 钉死判据，加三点验收 |
| L2 中 | 多文件或带 references；有约定要定，没有互斥路线 | 一张规格表让学员填，加两三条 Prompt |
| L3 重 | 有互斥路线；跨组件契约；返工贵 | 完整 SPEC 六步：调研、讨论选型、决策、写 SPEC、执行、验证 |

L3 只有三件：media CLI（12 到 16 课）、飞书闸口（18 到 19 课）、控制台（27 到 31 课）。六步的写法由第 11 课钉死，后面七门 L3 课只引用第 11 课，不重新定义。ADR 固定四项：决策、理由、被否方案、反悔成本。里程碑出口条件必须是一句能跑的验证动作。

## 5. 每课长什么样

叙事单元是动作步骤，每课三到六步。每步四段：为什么要这一步；让 AI 做什么（内联一段 10 到 20 行的 Prompt，只针对这一步；产出物有多种可能的先让 AI 出清单，人确认再生成）；拿到结果审哪两三点；出错多半是什么。

开篇一段：承接上一课的产出（具体到产物名、数量、判据）、本课输入、本课产出几件东西。不写痛点故事。每课一个记忆锚点，全篇归到它下面。收尾一两段：本课做到了什么、还看不到什么、下一课补什么。不写「总结与作业」。

一课一个产物：这课结束时学员手上多了什么可落盘的东西，一句话说得出来。每节要有判据或产物，只讲道理的节合并或删。一课 Prompt 超过 5 条就该拆节。

字数不设限，代码行有硬上限：实操 200 行，方法论 30 行。实操篇围栏块至少 3 个，排障句「多半是」至少 2 处。

## 6. 承接

33 课严格按 01 到 33 的顺序写，你拿到的材料第一条是上一课的成稿全文。动笔前先读它的开篇和结尾两段，本课开篇复述上一课产出时按它实际写了什么复述，不按计划。`plan/handoff-ledger.md` 里本课那一行是校验用的：写完拿成稿结尾对一遍，对不上不一定是错，但要当场改台账并在「改过没」列打勾。

## 7. 全书 33 课一览

模块 0 起步：01 走查、02 装备、03 立骨架、04 账号大脑。
模块 1 单条内容：05 用开源 Skill 跑通一条内容、06 验证信号流向谁。
模块 2 质检闭环：07 反馈信号从哪来、08 造六个检测脚本、09 裁判与选手分离、10 自愈回路与三轮熔断。
模块 3 SPEC 与状态中枢：11 SPEC 方法六步、12 media 调研与选型、13 media 写 SPEC、14 执行 M1、15 执行 M2 与存量迁移、16 验证与收敛。
模块 4 交付闸口：17 闸设在哪、18 飞书通道 SPEC、19 飞书实现与按钮回流、20 发布 Skill 与 dry-run。
模块 5 治理线：21 双线闭环与资产读写表、22 复盘归因、23 选题池与外网情报、24 治理总调度与自审。
模块 6 无人值守：25 无头作业与进程流归一、26 终局裁决与进程收尾。
模块 7 控制台：27 读设计稿定契约、28 后端读路径、29 后端写路径、30 前端骨架与前两页、31 前端余下四页与联调。
模块 8 收官：32 形态坐标与不能关的出口、33 1:1 验收。

## 8. 交稿三关

1. `python3 ~/.claude/skills/ai-arch-tutorial-style/scripts/check_style.py <final.md>` 零命中。
2. `LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh <final.md> <实操|方法论>` 输出 RESULT: PASS。汉字数那一行只报数不判，看到超参考值是正常的，不要为它砍字。
3. 材料可溯：文中每个路径、命令、脚本名能在仓库 grep 到；脚本报 `[WARN] 未在仓库找到` 的逐条确认并在交付回复里列出。
```

- [ ] 写完后自己读一遍，问：一个没参加过任何讨论的写作代理，只读这份加任务卡，会不会写出「读 pipeline/2-create.md」这种句子。会就补第 2 节。

### 任务 0.3 导出旧稿素材到 `plan/old-courses/`

旧目录已删，但 HEAD 里还有。旧稿只当素材，不当结构，不当事实源。

- [ ] 导出：

```bash
cd /Users/yedizhang/yedi-study/douyin-media
OLD="docs/Iterative-spec/0901-AI自媒体课程体系"
NEW="docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses"
git ls-tree --name-only HEAD "$OLD/courses/" | while read -r f; do git show "HEAD:$f" > "$NEW/v1-$(basename "$f")"; done
git show "HEAD:$OLD/courses-v2/02-用开源Skill跑通第一条内容.md" > "$NEW/v2-02-用开源Skill跑通第一条内容.md"
git show "HEAD:$OLD/02-重写方案-以AI架构师范式.md" > "$NEW/v2-02-重写方案.md"
git show "HEAD:$OLD/03-批量重写执行计划.md" > "$NEW/v2-03-批量重写执行计划.md"
ls "$NEW" | wc -l
```

预期输出 `21`（18 篇 v1、1 篇 v2 样板、2 份 v2 计划）。

- [ ] mac mini 上写好的 v3 第 01 到 07 课成稿全文在 Multica 各 issue 评论区（PLUT-76 到 PLUT-82）。本轮全部重写，不导入成稿，但把它们导出来当素材，写作代理可以看它的口径，不能抄：

```bash
M="/Applications/Multica.app/Contents/Resources/app.asar.unpacked/resources/bin/multica"
export MULTICA_WORKSPACE_ID=3aad2771-ff96-479b-9f49-8d0723d1d445
for n in 76 77 78 79 80 81 82; do
  "$M" --profile desktop-api.multica.ai issue comment list PLUT-$n --output json \
  | python3 -c "
import json,sys
cs=[c for c in json.load(sys.stdin) if c.get('author_type')=='agent' and c.get('type')=='comment' and '# 成稿全文' in c['content'] or ('final.md' in c['content'] and len(c['content'])>8000)]
print(cs[0]['content'] if cs else '')" > "$NEW/mini-v3-$(printf %02d $((n-75))).md"
done
wc -c "$NEW"/mini-v3-*.md
```

预期每个文件 10KB 以上。哪个为空就手动打开那个 issue 的评论把成稿贴进去。

### 任务 0.4 验收脚本 `tools/check-lesson.sh`

- [ ] 从 HEAD 导出旧脚本：

```bash
git show "HEAD:docs/Iterative-spec/0901-AI自媒体课程体系/tools/check-lesson.sh" > docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh
```

- [ ] 改三处。第一处，汉字上限只报数不判（决策 D14）。把

```bash
echo "汉字 ${cjk} / 上限 ${maxc}；代码行 ${code} / 上限 ${maxcode}；围栏块 ${blocks}；排障句(多半是) ${fix}"
[ "$cjk" -le "$maxc" ] || { echo "[FAIL] 汉字超限"; fail=1; }
```

改成

```bash
echo "汉字 ${cjk} / 参考值 ${maxc}（只报数不判）；代码行 ${code} / 上限 ${maxcode}；围栏块 ${blocks}；排障句(多半是) ${fix}"
[ "$cjk" -le "$maxc" ] || echo "[INFO] 汉字 ${cjk} 超出参考值 ${maxc}，字数不设限，不算失败"
```

第二处，脚本头部注释里的用法路径改成本目录。第三处，`repo=` 那一行用 `git -C "$(dirname "$0")" rev-parse --show-toplevel` 定位仓库根，本目录在仓库内，不用改，但要验证：

```bash
LC_ALL=en_US.UTF-8 bash docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/check-lesson.sh docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/old-courses/v2-02-用开源Skill跑通第一条内容.md 实操 | tail -15
```

预期能跑到 `RESULT:` 行，汉字那行显示「只报数不判」，状态值核对能读到 state.ts。注意 `LC_ALL` 必须设，否则 `grep -o '[一-龥]'` 按字节数汉字，PLUT-73 和 74 记录过这个坑。

- [ ] 提交：`git commit -m "docs(course-v3): 验收脚本，汉字只报数不判"`。

### 任务 0.5 转换 33 张任务卡到 `plan/cards/NN.md`

（09-04 补充：每张卡还要有「2.1 大纲」块，格式见 `plan/outline-format.md`，由 6 个 sonnet 子代理按模块并行补，再做一次跨模块一致性核对。第 11 课定下的六步标准单独落 `plan/spec-lesson-standard.md`。）

mac mini 上贾维斯建的 33 个 Multica issue（PLUT-76 到 108）描述就是每课的任务卡，八块结构：定位、本课要讲什么、学员交付、材料清单、承接、流程、验收与交付、接力与失败处置，末尾一段共同约束。本机沿用它们的内容，改路径和流程。原始 JSON 已拉到 `/private/tmp/claude-501/-Users-yedizhang/04e5eb7a-cc05-4666-aad7-e6269d1ac7e5/scratchpad/issues/PLUT-NN.json`，临时目录会丢，转换脚本里先重新拉一遍。

- [ ] 转换脚本落盘到 `tools/make-cards.py`，内容：

```python
#!/usr/bin/env python3
"""把 Multica PLUT-76..108 的 issue 描述转成本机任务卡 plan/cards/NN.md。"""
import json, re, subprocess, pathlib

M = "/Applications/Multica.app/Contents/Resources/app.asar.unpacked/resources/bin/multica"
WS = "3aad2771-ff96-479b-9f49-8d0723d1d445"
ROOT = pathlib.Path("/Users/yedizhang/yedi-study/douyin-media")
V3 = "docs/Iterative-spec/0904-AI自媒体课程体系-v3"
OLD = "docs/Iterative-spec/0901-AI自媒体课程体系"
OUT = ROOT / V3 / "plan/cards"
OUT.mkdir(parents=True, exist_ok=True)

REPL = [
    ("/Users/yedi/douyin-media", str(ROOT)),
    (f"{OLD}/courses-v3/", f"{V3}/courses/"),
    (f"{OLD}/writing/v3-", f"{V3}/writing/"),
    (f"{OLD}/writing/handoff-ledger.md", f"{V3}/plan/handoff-ledger.md"),
    (f"{OLD}/tools/check-lesson.sh", f"{V3}/tools/check-lesson.sh"),
    (f"{OLD}/courses-v2/", f"{V3}/plan/old-courses/v2-"),
    (f"{OLD}/courses/", f"{V3}/plan/old-courses/v1-"),
    ("`04-补全计划-零仓库从头实现.md`", "`00-ADR.md`"),
    ("04-补全计划-零仓库从头实现.md", "00-ADR.md（补全计划已并入 ADR，§ 号按 ADR 章节对应：§2.1 工具表见 ADR §5，§3 六步见 ADR D4，§4 推进顺序见 ADR §4 末表，§5 逐课见 ADR §4 大纲表）"),
]

def fetch(n):
    out = subprocess.check_output([M, "--profile", "desktop-api.multica.ai", "--workspace-id", WS,
                                   "issue", "get", f"PLUT-{n}", "--output", "json"])
    return json.loads(out)

for n in range(76, 109):
    d = fetch(n)
    lesson = n - 75
    desc = d["description"]
    # 1. 路径替换
    for a, b in REPL:
        desc = desc.replace(a, b)
    # 2. 第 8 块（Multica 接力）整块替换为本机流程
    desc = re.sub(r"## 8\. 接力与失败处置.*?(?=\n---\n\n## 共同约束)",
        "## 8. 交付后\n\n交稿后不做接力。把三样东西写进你的最终回复：成稿路径、check-lesson.sh 最后 12 行输出、未核实项清单（仓库里 grep 不到、已按规则删掉或改写的名词与数字）。主回话验收后决定下一课。\n\n失败怎么办：硬禁令没清零、材料不足被 Step 1 拦下这类能自愈的，原地重试一次。重试仍失败就停下，在回复里写清卡在哪一步、报错原文，不要自己降低标准硬交。\n",
        desc, flags=re.S)
    # 3. 材料清单里 mac mini 写的等价路径 writing/v3-NN/final.md 改成本机 writing 目录
    desc = re.sub(r"writing/(\d\d)/final\.md", r"writing/\1-*/final.md", desc)
    # 4. 第 01 课以外，把「上一课成稿」那条改成本机 courses/ 路径（已由 REPL 覆盖），再补一句
    header = (f"# 第 {lesson:02d} 课任务卡\n\n"
              f"> 来源：Multica {d['identifier']} 描述，2026-09-04 转换。先读 `{V3}/plan/00-写作背景包.md`，再读本卡。\n"
              f"> 本卡里凡是引用 `00-ADR.md` 章节号的地方，按 ADR 实际章节找；找不到就在回复的未核实项里说明。\n\n")
    (OUT / f"{lesson:02d}.md").write_text(header + desc, encoding="utf-8")
    print(lesson, d["title"])
```

- [ ] 跑：`python3 docs/Iterative-spec/0904-AI自媒体课程体系-v3/tools/make-cards.py`，预期打印 33 行，`ls plan/cards | wc -l` 得 33。

- [ ] 逐卡人工过一遍，重点四件事（主回话做，约 40 分钟）：
  1. 材料清单里每条路径在本机 `ls` 得到。已知缺的：`tools/social-auto-upload/README.md` 本机没有，第 02、20 课改为 GitHub 上 dreammis/social-auto-upload 的 README（写作代理用 WebFetch 取）；`docs/Iterative-spec/2026-09-02-AI架构师教程写作分析/00-分析报告.md` 本机没有，凡引用它的改成 `~/.claude/skills/ai-arch-tutorial-style/references/structure.md`；`04-补全计划` 的 §5 逐课表已由 ADR §4 大纲表代替。
  2. 第 11 课的教法档写的是「方法论」，但它的交付物是四件套模板，模板放围栏块里会超方法论篇 30 行的代码上限。把第 11 课的验收脚本参数改成 `实操`，并在卡里注明原因。
  3. 第 17 到 19 课确认口径是飞书，按 `tools/feishu-bot/` 代码 1:1，课里说明这是仓库的历史实现，仓库现行通道另有 ADR。
  4. 每张卡末尾的「共同约束」段落保留，再加一行：「字数不设限，心法类内容讲透为准。」

- [ ] 提交：`git commit -m "docs(course-v3): 33 张任务卡"`。

### 任务 0.6 承接台账与学员产物清单

两份表都从 33 张卡的「学员交付」和「承接」三行推出来。派一个子代理做，提示词：

```
读 docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/cards/01.md 到 33.md 全部 33 张任务卡。产出两份文件，不要写任何卡里没有的内容。

1. plan/handoff-ledger.md：33 行主表，列：课号 / 课名 / 计划交付什么（抄卡的第 3 块）/ 上一课产出（抄卡第 5 块第一行）/ 本课开头接的点（第 5 块第二行）/ 结尾抛出的问题（第 5 块第三行）/ 改过没（留空）。表头上写一句：本表是校验用的活台账，写作代理写完成稿如果结尾和本表对不上，当场改本表并在「改过没」列写「已改：<新措辞>」。
   然后做一次链条核对：第 N 行「结尾抛出的问题」和第 N+1 行「开头接的点」是不是同一件事。不是的逐条列在文件末尾「待人裁决」一节，不要自己改。

2. plan/inventory.md：学员产物累计清单。第 N 行 = 第 1 到 N 课交付物的累计，按类别分：仓库文件与目录 / 命令 / skill 与子代理 / 文档（SPEC、契约、brain）/ 概念与判据。写法示例：「第 03 课结束时学员手上有：CLAUDE.md、pipeline/ 五份契约、brain/ 五个空文件、content/ 骨架、docs/00-toolchain.md、docs/00-walkthrough.md」。这张表给写作代理判断「本课 Prompt 的输入学员有没有」用，所以每一行要能直接对照。
```

- [ ] 主回话看「待人裁决」一节，有对不上的在这里定，改卡。
- [ ] 提交。

### 任务 0.7 样板课：第 01 课走一遍全流程再放行

阶段 1 的每课流程先用第 01 课试一次，确认背景包、任务卡、脚本、子代理提示词模板四样东西合在一起能产出合格稿。第 01 课过了主回话验收，再进入串行。第 01 课不过，改的是模板不是稿子。

---

## 阶段 1：主链，33 课串行

### 每课的标准流程（主回话执行，从 01 到 33 重复）

- [ ] **步骤 1 更新进度**：`plan/progress.md` 本课状态改「写作中」，记时间。
- [ ] **步骤 2 派写作子代理**。Agent 工具，`subagent_type: general-purpose`，`model: sonnet`，提示词用下面模板，只替换尖括号：

```
你是课程写作代理，负责写《AI 自媒体流水线》v3 的第 <NN> 课 <课名>。仓库在 /Users/yedizhang/yedi-study/douyin-media，所有相对路径以它为根。

先按顺序读完下面四样，全部读完再动笔：
1. docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/00-写作背景包.md（课程目标、读者前提、四条铁律、教法分档、每课形状、交稿三关）
2. docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/cards/<NN>.md（本课任务卡：要讲什么、学员交付、材料清单、承接）
3. docs/Iterative-spec/0904-AI自媒体课程体系-v3/plan/inventory.md 的第 <NN-1> 行（学员写到本课之前手上有什么。本课每条 Prompt 的输入必须在这一行里，或者是本课前面步骤刚造出来的）
4. <NN 大于 01 时：上一课成稿 docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/<上一课文件名>，先读开篇和结尾两段，本课开篇按它实际写了什么复述>

然后用 /ai-arch-tutorial-style skill 的六步流水线写，产物目录 docs/Iterative-spec/0904-AI自媒体课程体系-v3/writing/<NN>-<slug>/，只改一处：Step 2、3 不出方案不问人，大纲照任务卡「2.1 大纲」块展开成 outline.md，frontmatter 写 plan: fixed-from-card，局部调整要在 outline.md 开头说明，节的数目和顺序不动。其余照 skill 的 references/workflow.md 走：Step 1 材料盘点存 analyzed.md（任务卡第 4 块的材料逐条真的打开读，材料不足 5 条不进下一步，不许编）；Step 4 写 draft.md；Step 5 派一个 sonnet 子代理做读者诊断存 diagnosis.md，给它的读者画像是「会用 Claude Code、没做过自动化流水线、手上没有参考仓库、只有前几课自己做出来的东西」，并把 inventory.md 第 <NN-1> 行附给它，让它在原有五个维度之外多查一项：本课哪条 Prompt 的输入是读者手上没有的；Step 6 改成 final.md，写 polish-report.md。

交稿三关（背景包第 8 节）跑完才算完：check_style.py 零命中；check-lesson.sh 参数 <实操|方法论> 输出 RESULT: PASS，汉字数只报不判，不要为它砍字；材料可溯。

然后做两件事：把 final.md 复制到 docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/<NN>-<课名>.md；拿成稿结尾对 plan/handoff-ledger.md 第 <NN> 行，对不上就改台账并在「改过没」列写「已改：<新措辞>」。

不要改 pipeline/、brain/、.claude/、harness/、tools/console、tools/feishu-bot 下的任何文件。不要改背景包和任务卡，觉得卡有问题写进回复。

回复只给四样：成稿路径；check-lesson.sh 最后 12 行；未核实项清单（仓库 grep 不到、已删或改写的名词和数字）；台账是否改动及改成了什么。
```

- [ ] **步骤 3 主回话验收**，按顺序，任一不过就退回：
  1. 随机读两段加收尾，问两个问题：这段是在讲清楚事还是在证明作者厉害；每个判断说清了范围和不灵的时候没有。命中一处整篇退回，不看其他项。
  2. 自己重跑一次 `check-lesson.sh`，看 RESULT 和 `[WARN]` 条目，对照子代理给的未核实项清单。
  3. 读开篇一段：承接的是上一课成稿实际写的东西（产物名、数量、判据），不是计划里的摘要；有本课输入、有本课产出件数。
  4. 抽两条 Prompt，对 `inventory.md` 上一行：输入都是学员已有的。这一条是 v2 翻车点，每课必查。
  5. 数 Prompt 块：分散在步骤里，最后一节没有 Prompt 堆；总数超过 5 看该不该拆节。
  6. 抽一个步骤：为什么、让 AI 做什么、审什么、出错多半是，四段齐不齐。
  7. 读收尾：和 `handoff-ledger.md` 本课行一致，或台账已同步改。
  8. L3 课（12 到 16、18、19、27 到 31）多查一条：六步的写法、对比表列、ADR 四项和第 11 课成稿一致，没有另起一套。
  9. 心法类课（06、11、17、21、32）多查一条：有没有为了收篇幅把该展开的判据压成一句话。有就退回让它展开，字数不是理由。
- [ ] **步骤 4 退回**：用 SendMessage 把不过的项发给同一个子代理改，不重开新代理，它的上下文还在。最多两轮，第三轮仍不过停下来找我看模板哪里有问题。
- [ ] **步骤 5 通过后**：`progress.md` 状态改「通过」，提交：

```bash
git add docs/Iterative-spec/0904-AI自媒体课程体系-v3
git commit -m "docs(course-v3): 第 <NN> 课 <课名>"
```

- [ ] **步骤 6 模块边界的加码验收**（04、06、10、16、20、24、26、31 课通过后各做一次）：把本模块几课的开篇和收尾连起来读一遍，看锚点串不串得成一条线；把 inventory.md 本模块末行和 ADR §4 的阶段出口条件对一次，学员按这几课做完能不能过那个出口。对不上的记进 `progress.md` 备注，等全书写完在阶段 2 一起处理，不回头改已通过的课，除非是硬错。

### 逐课要点（写作代理看卡，这里给主回话的提醒）

| 课 | 参数 | 主回话额外要盯的 |
|---|---|---|
| 01 | 方法论 | 样板课。第一次跑，重点看模板本身：子代理有没有真的读完四样东西，Step 5 有没有附 inventory |
| 02 | 实操 | 安装命令要从各项目官方 README 取，交稿后主回话逐条人工核一遍。sau 本机没装，写作代理用 WebFetch 读 GitHub README |
| 03 | 实操 | CLAUDE.md、五份契约只给骨架和判据，不给全文（D13） |
| 04 | 实操 | 四份填满一份留空；「不许出现判不了的词」这条判据要落在 Prompt 的审查点里 |
| 05 | 实操 | 全书唯一一门工具操作课，视频细节压到最薄；tts-dub 降为 L1 |
| 06 | 方法论 | 心法课，多讲。四代演进只用「验证结果回给谁」一刀切 |
| 07 到 10 | 实操 | Loop 主线，不压。10 课结尾必须留下「一堆手改的 meta.yaml」，第 15 课要回收 |
| 11 | 实操（改过） | 全书最重要的课之一。六件事钉死后要在成稿里能直接被后面七课引用。多讲 |
| 12 | 实操 | 开篇明写不产一行代码。四组互斥路线各走调研与辩护 |
| 13 到 16 | 实操 | 里程碑出口条件必须是能跑的验证动作。16 课要有往返测试和 audit.jsonl |
| 17 | 实操 | 心法课，多讲。不可逆动作表 |
| 18、19 | 实操 | 飞书四个决策点在 18 定死；19 只实现不重议。说明企业或团队账号门槛 |
| 20 | 实操 | dry-run 铁律；真发标「有号的自己试」 |
| 21 | 实操 | 心法课，多讲。治理线只提议不落笔 |
| 22 到 24 | 实操 | 24 课要显式点破反哺横跨 21、22、24 三课 |
| 25、26 | 实操 | 25 课引用 console jobs 目录之前，先确认学员此时还没有 console（27 课起才造），写法要改成「你将在模块 7 造的那个进程」 |
| 27 | 实操 | 先只写这一课，看 SPEC 出来有多厚，再决定 28 到 31 怎么切。设计稿 docs/design/ 九页里 review.html 和 harness-task-detail.html 标明不在 1:1 范围 |
| 28 到 31 | 实操 | 每课一个能跑的出口 |
| 32 | 方法论 | 心法课，多讲。回收全书 32 个锚点，第 1 课的锚点要按 01 成稿原句收 |
| 33 | 实操 | 验收清单分两栏：结构项逐条对，内容项只看有没有、判不判得了（ADR P2） |

### 断点与配额

- 一课约 30 到 60 分钟（mac mini 前 7 课均值 26 分钟，L3 课会更长）。33 课串行约 20 到 30 小时，分几天跑。
- 每课的六步产物都在 `writing/NN-*/`，会话中断后看 `progress.md` 找「写作中」的课，再看目录里缺哪个文件，派一个新子代理从缺的那步续，提示词加一句「writing/<NN>-<slug>/ 下已有 X、Y，从 Z 步续，不重跑前面」。
- 撞到用量上限时不要改流程硬跑。停下，等重置，从断点续。

---

## 阶段 2：收尾

### 任务 2.1 全链承接核对

派一个子代理：

```
读 docs/Iterative-spec/0904-AI自媒体课程体系-v3/courses/ 下 33 篇成稿的开篇一段和收尾两段，以及 plan/handoff-ledger.md。逐对核 32 组接口：第 N 课结尾抛出的问题，和第 N+1 课开篇接的点，是不是同一件事，第 N+1 课复述的上一课产出和第 N 课实际交付的一致不一致（产物名、数量、判据）。另外核 plan/inventory.md：每课 Prompt 的输入是否都在前一行清单里。
输出一份 plan/handoff-review.md：对得上的一行带过，对不上的写课号、原句、差在哪、建议改哪一课。不改稿。
```

主回话按它的清单定改哪课，改完重跑那课的三关。

### 任务 2.2 课程材料附录 `appendix/`

按 ADR D13 装三类：数据契约模板全文（从仓库 `content/_template/` 与 `content/_backlog/backlog.yaml` 头部导出 meta.yaml、4-publish.md、5-retro.md、backlog.yaml 头部、报告模板）、console 设计稿（`docs/design/` 九页加 css，README 里标明 review.html 和 harness-task-detail.html 不在 1:1 范围）、脱敏样例数据（从 `content/*/*/5-retro.md` 挑两三条脱敏，供第 22 课）。写一份 `appendix/README.md` 说明每件东西给哪一课用。

### 任务 2.3 课表 README 与提交

`courses/README.md`：33 课表、每课一句话锚点（从成稿抽）、教法档、学员交付。ADR 状态行改「33 课已写完，待人审」。最后一次提交。

### 任务 2.4 Multica 侧要你自己做的

- 删掉 mac mini 上两个自动机（起跑与巡检，cron 是每年 9 月 4 日）：`multica autopilot list` 找 id 后 `multica autopilot delete <id>`。
- PLUT-83 停在 in_progress，翻成 cancelled 或留着都行，别翻 todo，否则 mac mini 会接着写第 08 课，和本机重复。
- PLUT-75 上给鹰眼留一句：本轮改本机执行，它那条链停。

---

## 自查

对照 ADR：D1 到 D16 每条在本计划里有落点（D1、D2 在背景包第 2、3 节和验收第 4 项；D3、D4 在背景包第 4 节和验收第 8 项；D5 在背景包第 3 节；D6 到 D9 是内容决策，落在对应课的卡里；D10 在 10、15 课要点；D11 是编号已定；D12 在 04 课要点；D13 在 03 课要点与任务 2.2；D14 在任务 0.4 和背景包第 5 节；D15 在背景包第 5 节；D16 在阶段 1 流程）。ADR §7 的 P2 落在 33 课要点，P8 落在 25 课要点，P1、P3、P4 已由你 09-04 拍板。

没有落点的：ADR P6 提到的「巡检自动踢一次」，本机执行不用自动机，不适用。
