# webvideo-loop 教学仓 · 施工记录

> 日期：2026-08-21 ｜ 依据：`00-ADR.md` + `01-实施计划.md` ｜ 状态：**第一阶段完成，验收全过**
> 交付物：`~/yedi-study/webvideo-loop`（独立 git 仓，2 commits，身份 yedizhang <yedizhangclaude@gmail.com> repo-local）

## 一、施工方式

- WP0 主回话自建（契约落地）→ WP1–WP4 四个 sonnet 子 agent 并行 → WP5 主回话收口。
- E2E 验收按人审补充要求：**`claude -p` 独立无上下文进程**（sonnet + 工具白名单）跑全链，主回话只当验收官。
- 双闸保真：出审人审、发布二次确认各用独立的 `claude -p` 调用（headless 无法中途等输入，两次调用天然构成两闸）。

## 二、各包完成情况与主回话裁决

| 包 | 结果 | 裁决/返工点 |
|---|---|---|
| WP0 骨架+契约 | ✅ | — |
| WP1 数据 | ✅ 22 文件 | retro 日志名改为执行日期并对齐引用（派工书笔误）；契约补「目录日期 = promote 日期」定义 |
| WP2 工具 | ✅ 35 测试全过 | 采纳其 7 条契约解释（含窗口演化限累计字段、generated_at 幂等——已回写契约 §6）；追加两轮修订：doctor 归档态缺媒体降 WARN、generated_at 锚定 published+7d |
| WP3 创作链 | ✅ 端到端实测出 mp4 | 自修真 bug：edge-tts 失败留 0 字节文件会被「已存在即跳过」误判（已成源码教材素材）；file:// 打不开 ESM 页为浏览器限制，README 已写绕法 |
| WP4 SOP+skills | ✅ 15 文件 | 裁决三疑义：promote 六处记账含 dashboard（契约疏漏）、CLAUDE.md 补选题池例外条款、retro 报告名带 slug 防同日撞名 |
| WP5 收口 | ✅ | 修 doctor H1 检查不认模板注释头的 bug；示例条目补真 metrics.json |

**契约中途修订 4 处**（promote 六处 / retro 文件名 / doctor 归档态 / §6 两条澄清），均经 SendMessage 即时同步给在途 agent，无漂移。

## 三、E2E 验收记录（ADR §七 逐条）

1. **环境**：node/ffmpeg/edge-tts/playwright chromium 本机全通，README quickstart 命令与实际一致。✅
2. **生产线**：`claude -p "按 pipeline/daily-run.md 跑今天的生产"`（零上下文 sonnet）→ 取题 2026-08-21-006 → promote 六处记账 → 写稿（**审稿闸真实工作**：R1 FAIL 钩子过长，R2 PASS）→ edge-tts 配音（瞬时网络失败重试成功）→ 录屏 74.5s 成片（ffprobe 复检）→ cover.svg → doctor 全绿 → flip review 停线。**亮点**：WebSearch 不在白名单，agent 主动换了不依赖引用数字的选题角度——内容红线自持。✅
3. **人闸闭环**：3-review.md 人审留痕「通过」→ 第二次 `claude -p` dry-run 打印物料后**主动停下等「确认发布」四字** → 第三次调用确认 → publish-done 三处记账，check 0 errors。✅
4. **治理线**：mock-metrics 生成 → `claude -p` 跑 retro 任务卡 → **两条埋规律均被零上下文 agent 归因发现**（钩子缺反差词→2s 留存低 20pt；强 CTA 评论率 5.35% vs 基线 1.2%），且正确识别「第 2 数据点 + 跨赛道」达到转正门槛、产提议**未碰 benchmarks.md**（铁律守住）→ 人审采纳回写大脑、index.jsonl applied 翻 true。**Loop 完整闭环。**✅
5. **check + doctor --all**：0 errors / 24 pass 2 warn（归档态设计内）0 fail；脱敏扫描（关键词拆片/corp 邮箱/个人路径）零命中。✅
6. **示例条目**：snipaste-review 五件套终态 + 真 metrics.json + E2E 又产出第二条完整样例 speech-to-text-free-test（保留入库，AI 真实产出的活教材）。✅

## 四、安全事件（重要教训）

**建仓时 git 身份继承了全局 corp 邮箱**，5 个 commit 作者全部踩红线。按既定规则处置：**删 `.git` 整仓重建历史**（当日新仓无远端，零损失），repo-local 配置 `yedizhang <yedizhangclaude@gmail.com>` 防复发。
教训：新建任何可能推 GitHub 的仓库，`git init` 后第一件事 = repo-local 身份配置 + 首 commit 前验 `git config user.email`。（全局 memory 已有此红线，本次是「建仓流程没把它变成前置步骤」——流程缺口，非知识缺口。）

## 五、遗留项（下阶段/可选）

- 本机无 rsvg-convert，封面停在 cover.svg（契约允许）；教学文档可提示 `brew install librsvg` 得 PNG。
- `node --test tools/` 在 Node v24 报 MODULE_NOT_FOUND（Node 行为），正确命令 `node --test tools/*.test.mjs`——已按此使用，README 未提测试命令，写教学文档时注明。
- 并行 agent 共仓时 `git add -A` 会互相扫入半成品（本次发生两回）：后续多 agent 施工按路径提交或干脆最后统一提交。
- **第二阶段（教学文档三幕教程）未启动**——按 ADR 规划另立文档，目录沿用 `0821-webvideo-loop/`（03- 起编号）。
