# 06-施工记录（2026-08-18 实施日志）

> 角色：实施过程的**事件流水 + 裁决台账 + 人工待办清单**。方案是什么看 00-05；这里只记「实际发生了什么」。
> 执行架构：Claude 主会话任总指挥（编排/闸口/人审中转/热修），实现 = Sonnet subagent 一包一主（A-E），每包完成即派独立验收 agent 复核（§8 机制）。原计划交本地 Codex 执行，用户试用后弃用，改由 Claude subagent 全承接。

## 一、时间线（关键事件）

| 时刻(约) | 事件 |
|---|---|
| 15:45 | 测试基建预置：tools/console 独立仓 init，vitest workspaces（5 包冒烟绿）+ playwright/midscene e2e 脚手架 + 关键词安全闸 guard |
| 15:55 | **信息安全事件 #1**：midscene 配置携内部网关域名入库 + commit 作者为 corp 邮箱 → 按红线整仓删 `.git` 重建历史；敏感值迁入 gitignored `.env.e2e`；guard 增加 git 身份检查 |
| 16:20 | 预装五包拍板依赖（消并行 npm install 竞态）；W1 三路并行派发：A(core+CLI)、B(cc-stream)、D(ui S1/S3)，全 Sonnet |
| 16:45-17:30 | B 交付 → 验收 PASS；D 交付 → 验收 PASS（验收员用 Playwright 完成浏览器实测）；期间热修：@types/react 基线缺口（登记+补装）、antd v5→v6 裁决保留 |
| ~17:00 | A 被用户误中断（工作已提交至第 6 步）→ 派接班 agent；接班者又撞 token 限额 → 限额恢复后续跑完成 |
| 19:50+ | A 交付（13 命令全量、336 用例绿、顺修 npm link 静默空转 bug 与裸依赖越界）→ 验收 **PASS**，**G1 过闸** |
| G1 后 | C(server) 提前开工（验收不阻塞下游原则）；E 前半（第 8-10 步存量迁移）放行 |
| G1 后 | **dashboard 三区手术**（唯一人审闸，用户批准含未决项默认）：真实 dashboard.md 插三区标记 + 首次 rebuild 成功，diff 严格限标记内，commit 存档 |
| E 交付后 | E 前半完成（六写入口切 media，含自查出的第 5 个 set_status 调用点）；契约漏派的「daily-run 步骤 4 出审」由编排方裁决补齐；E 验收进行中 |
| 目标切换 | 用户设定无人值守目标：完成全部任务后自动 commit + push |
| 安全复查 | **信息安全事件 #2**：发现主仓也有 GitHub remote → 05 红线 9 的封禁词原文改拆片描述；未推送 5 个 commit 的 corp 作者邮箱 rebase 重写为 GitHub 身份；被跟踪文件全量扫描零命中 |

## 二、各包验收结论（详情见各验收报告，此处只记结论与要点）

| 包 | 结论 | 关键发现 |
|---|---|---|
| A core+CLI | **PASS** | ① yaml 库 `doc.toString()` 实测对全部 25 个真实文件丢注释对齐 → 实现改「Document API 定位 + 原始字节区间替换」，判为忠于红线精神的主动声明偏离，认可；② 修复 npm link 符号链接致 `media` 全局命令静默空转（不修全局命令不可用）；③ 契约两处自误：`workspace:*` 非 npm 语法（已更正 01）、CHK-01/CHK-05 判定范围不一致（已裁决，见三-7） |
| B cc-stream | **PASS** | ① fixture 探测产物自带内部路径，实现者主动脱敏且验收确认协议结构保真；② capture 脚本真跑抓出 bash 多字节变量名 bug（`$OUT` 后跟全角括号），已修；③ 去重归层（normalize 而非 transport）判为对拍板摘要的合理精化，拍板已对齐表述 |
| D ui S1/S3 | **PASS** | ① 验收员浏览器实测（主题色端到端 rgb(47,111,235)、剪贴板逐字符、抽屉真实弹出）；② react 19 零内置类型 = 拍板依赖表基线遗漏，补 @types/react(-dom) 并登记；③ 三项设计偏离均判收（Bundler 解析/类型临时镜像带 S2 硬化销账/FileDrawer Context 注入） |
| E 前半（8-10 步） | **PASS** | 六写入口全部落地核对（含出审补漏）；set_status 残留 8 处裁断采纳（历史日志+规划文档不算）；publish.py 死代码处置判正确；三 commit 回滚独立性验证；唯一文档漂移（gardener 引言块旧措辞）已热修（d60d231）；feishu 重启维持**待人工**（环境限制非失职） |
| C server | **PASS**（附 2 跟进项） | 验收真跑全套（起服/SSE/并发/降级恢复）；三处声明偏离全部裁收（exit-code 按 JSON 信封分类、promote body 需 slug、writer 禁令 vitest 断言替代 eslint——均已回记 02）；发现项 #1 allowedTransitions 幽灵治理迁移已热修（9094222，151 测试绿）；#2/#3 测试盲区已派补测 agent |
| （原始交付摘要）C server | 交付 | 151 server 测试/全仓 507 绿；真仓活体验证含一次真实 claude -p 作业全链路（apply-proposal，$0.47，verdict=git diff+applied 翻转）、kill -9 恢复、S6 文件轨迹推进实测；实测抓 3 bug（config 路径深度/EACCES 兜底/harness report 路径规范化）+ 1 基建雷（根 .gitignore `logs/` 吞 core fixture，已修并补交 247a1ca）；三处声明偏离待验收裁断 |
| D-S2 数据层 | 交付（4b79ce5） | api/sse/store 三件 + HealthLights + 类型镜像销账；发现 health/jobs 不走信封的契约细节并做自适应解包；上报 server 缺 exports 字段 → 编排热修 a724c10 并统一契约导入路径；token 按 03 契约用 localStorage（任务书误写 sessionStorage，契约优先）；两项活体验证（revision 重拉/断线降级）留 W3a 补测 |
| D-W3a（P1+动作层+P3）/ D-W3b（P6+LineChart）/ E 后半（S8+文档降级）/ C 验收 | 四路并行中 | 2026-08-18 晚间派发 |

## 三、编排层裁决台账（当日新增，之前的见 04 文末「裁决与销账记录」）

1. antd v5→v6：保留 v6（预装偏差账在编排方；决策实质是「上 antd + token 映射」非锁主版本），回记拍板 F3。
2. 测试跑器 node:test+tsx → **vitest**（用户拍板），回记 01；e2e 验收层 = playwright + midscene（用户拍板），模型走内部网关（配置只存 `.env.e2e`）。
3. 「daily-run 步骤 4 出审」写入口契约漏派 → 归第 8 步，编排方直接补切 `media flip <slug> review` 并回记 01。
4. `set_status` 字面残留 8 处判不改：历史运行日志=不可篡改，规划文档=不在迁移白名单；验收标准修正为「live 代码与现行文档零命中」。
5. publish.py 死代码的 set_status 调用一并切 media publish-done（避免悬空调用），判正确处置。
6. append-only 日志（audit/jobs/原始事件流）不适用 temp-then-rename 红线，裁断记入 05 §3-5。
7. CHK-05 不扩 retro_done：4 条老欠账转 dashboard 人工调查表追踪（用户批准的归区默认项）。CHK-01 对 EP01（2026-06-10-001 历史例外）的永久 false-positive 是真实运营摩擦，**待办：给 check 加数据级豁免机制**（如 backlog 条目 `exempt_checks` 字段），G2 前后处理。
8. 契约优先级公约：执行方案（怎么做）> 拍板（为什么），真语义矛盾才上报，记入 05 §1。

## 四、信息安全事件与处置（完整记录）

- **事件 #1（console 仓）**：midscene 接入时内部网关域名进了文件与 commit message，且 git 作者为 corp 邮箱。处置：整仓删 `.git` 重建历史（用户既定原则：不做 commit 整改）；敏感值全部迁入 gitignored `e2e/.env.e2e`（含蓝本项目路径注释）；新增 `scripts/guard-keywords.mjs`（关键词拆片构造防自命中，接 `npm test` 最前，含 git 身份检查）；repo-local git 身份改 GitHub 邮箱。全历史终验 0 命中。
- **事件 #2（主仓）**：无人值守收尾前发现主仓也有 GitHub remote（此前按「主仓不推」假设在 05 红线里落了封禁词原文）。处置：05 红线 9 改拆片式描述（唯一权威清单=guard 脚本）；未推送 5 commit 作者邮箱 rebase 重写；`git ls-files` 全量扫描零命中；已推送历史作者为本机邮箱（非 corp），无既往泄漏。
- **B 包加分项**：fixture 里 system/init 的 plugins[].path 携内部组织路径段，实现者入库前主动脱敏，验收确认结构保真。

## 五、人工待办（用户出手项，不阻塞施工）

| # | 事项 | 时机 |
|---|---|---|
| 1 | feishu-bot 重启实测：在有 `.venv`+`config.yaml` 的环境 `bash tools/feishu-bot/start.sh restart`，实测一次「通过/打回/拒绝」回调（验证 `_flip` 3 秒窗 + audit 记账）。本机两文件物理不存在且服务本就停着 | 任意 |
| 2 | 明日 daily-run 后回看 `tools/console/logs/audit.jsonl`：取题/出审/发布应各有对应行（第 8 步验收的持续观察；编排方也会自查） | 明日 |
| 3 | M2 出口「连续 7 天状态变更均有 audit 行」 | 7 天窗后 |
| 4 | dashboard 人工调查表里的存量运营待办（EP04/open-weight-5 发布确认、context-engineering 可见性、gemini 限流、5 条补链）——与本工程无关的既有欠账，机器区现在会持续提醒 | 任意 |

## 六、commit 台账

- **tools/console**（重建后）：`cf05a54` 基建奠基 → `adb857f` guard 补身份检查 → `14222aa` 预装依赖 → A/B/D 各步 feat 系列 → `1f3c463` ui 类型修复 → `542036f` capture 脚本修复 → `71116a2` 归区提案行号修正 →（W2 继续追记）
- **主仓**（重写作者后）：`bf956dd` dashboard 三区手术 → `6337857`/`24e4ef6`/`4c7b8be` E 第 8/9/10 步 → `ccbd20d` 步骤 4 出审补漏 →（收尾时统一提交方案包与 design 整理）

## 七、W2/W3 追记

- **E 后半交付**（0d573cb S8 / b23795f 第 11 步）：飞书场景 3/4 切 console job runner（console_api 开关回滚、旧路径逐字节保留、禁静默 fallback）；文档降级三目标完成 + `media check` 注册治理线日巡（periodic:1d）。验收进行中。
- **编排裁决（E 后半未决项）**：① 25 个存量 content/*/meta.yaml 的状态机注释按**历史存档**保留不改；模板已降级（c376f2d），M5 出口按「现行文档与模板」口径判定；② douyin-publish/feishu-notify 的 SKILL 残留 + feishu-notify 架构图 S8 后过期 → 合并为 **G2 文档收尾任务**（待 W3 落地后一并处理）；③ check 基线更新为 errors:3 / warns:20（新增 CHK-08 时间驱动告警属预期——今日无内容目录，工作台施工日）。
- **D-W3b（P6+LineChart）交付并提交**（f9182be）：79 项 ui 测试；跨层跟进两条——server funnelQuotes 为空数组桩（G2 裁决）、completion 单位口径待真实数据验证（G3 观察）。
- **C 补测收口**（9f076f0）：+39 用例（verdict 全矩阵 / JOB_DUPLICATE mock 生命周期 / config 路径防回归），零 bug 暴露，全仓 594 绿。C 验收跟进项清零。
- **E 后半验收 PASS**：S8 五细步逐条核实（旧路径逐字节保留独立验证）、第 11 步降级"只删规则不删叙述"逐行核对；偏差 1（_publishing 保留=条款 3/4 张力的唯一解，已回记 02）；E 包全线关账。
- **G2 文档收尾清单**（W3 落地后一并处理）：feishu-notify SKILL 架构图 S8 后过期需重画、douyin-publish(2处)/feishu-notify(4处) status= 字面残留评估指针化、pipeline/2-create.md:22 与 lessons.md:16 的 L9 历史叙事字面值评估；**代码小修**：ui AuditTrail.tsx 的 'orange' 硬编码色值改 token 预设（W3ab 验收 minor，修后重建 dist）。
- **W3a+b 联合验收 PASS**：S4/S5/S6/S10 逐条实测（警报 key 集合与 media check 完全相等、SSE 纯驱动更新有浏览器活体证据、JobPanel 磁盘兜底、决策按钮矩阵单测+截图）；W3a 两处顺手修复复核有效；全仓 594 测试绿。
- **D-W3a 交付**（04be94b，S4/S5/S6 全活体验证）：P1 待办分级合并、动作层 dry-run 确认流（演练条目建-验-删闭环）、P3 决策区随状态切换；顺手修 bootstrapToken hash 抢跑与 FileDrawer 路径适配两处 bug。**上报两跨包缺陷**：① core `flip --reason` YAML 写入损坏（裸行无 # 前缀 + 新 key 拼行——打回/否决/重做全经此路，高优先）；② server 冷启动 alerts 空数组直到首次 watcher 事件。
- 尾段三路并行开跑：缺陷修复 agent（两 bug）、D-W3c（P4/P5/P2 + S11 收口）、W3a+b 联合验收。
- **D-W3c 交付**（5 commits：S7 选题池 / S8 治理线含 ProposalList key 修 / S9 看板 / S11 收口）：六页全齐、dist 入库、ui CLAUDE.md 落地、真 fresh-clone 模拟通过；发现 start.sh 全角逗号 bug（编排热修 4282a38）；已知空缺记录：server deriveProposals 恒 prose（结构化提议路径待 server 侧实现，与 funnelQuotes 空桩同类，首版接受）。验收进行中。
- **缺陷修复收口**（e8bb5e3 core / b082fd9 server）：① flip --reason 损坏根因 = yaml 包对「续行注释长度」与「孤儿注释归属」两处 range 陷阱，修复保持「定位+字节区间替换」架构，真实 meta 副本实测 diff 仅白名单行且可重 parse；② 冷启动 alerts 恒空根因 = 去抖首拍语义，修正为首拍透传（已回记 02 §2.5）。全仓 630 测试绿。既有测试曾把 bug 行为编码为预期（合成 fixture 掩盖）——「真实文件 fixture 优先」的教训再次验证。

---
（后续事件继续追记）
