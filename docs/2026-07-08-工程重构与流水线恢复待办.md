# 工程重构与流水线恢复待办（2026-07-08 审计结论）

> 来源：2026-07-08 全仓架构审计对话。原则：先地基（git）、再止血（恢复生产）、后重组（目录）、最后瘦身（文档）。
> 每项标注 owner（人/AI）与验证方式。执行时逐项勾销。

## 阶段 0 · 人要亲手做的（不阻塞其他阶段，但尽快）

- [ ] **EP04 发布确认**：去创作者中心确认 EP04 是否真已发布（meta.yaml 停在 scheduled，2026-06-19 20:00 定时提交后无人回填）。确认后由 AI 统一回填 meta.yaml + backlog(2026-06-10-004) + dashboard 三处。
- [ ] **EP13/14/15 作品链接**：从创作者中心拿链接，交 AI 回填各自 4-publish.md 与 dashboard。

## 阶段 1 · 地基：版本控制（AI，半小时）

- [ ] 写 `.gitignore`：`node_modules/`、`content/*/*/build/`、`content/*/*/demo/`、`*.mp4`、`*.mp3`、`tools/social-auto-upload/`、venv/缓存类。
- [ ] `git init` + 首次提交（brain / pipeline / automation / content 文档层 / .claude）。
- 验证：`git status` 干净；`.git` 体积在几十 MB 内（重资产未入库）。

## 阶段 2 · 止血：恢复生产 + 选题池消化（AI 改，人审 expired 清单）

> 背景：daily-run 自 06-30 起连续空跑（清系列模式已收官无题可取）；选题池 9 条 idea 全是 06-14 入池、24 天无人消化、时效题已过期；ideate 自 06-14 后再没跑过。模式切换已获人口头授权（2026-07-08）。

- [ ] **daily-run 切回双赛道**：改 `automation/daily-run.md`「当前模式」段（清系列 → 选题驱动，恢复步骤 2 的 douyin-ideate，深度:流量≈6:4），同步改定时任务 prompt（douyin-ai SKILL.md）。验证：干跑一次取题逻辑，能从非系列 idea 出题不再报"队列空"。
- [ ] **backlog 加 `next_up` 指针**：文件顶部加 `next_up: <id>` 字段；daily-run 取题改为"next_up 优先 → 无则按 score 取队首"。人挑题 = 改这一个字段，不靠文件物理位置。验证：置一个 next_up 干跑取题命中它。
- [ ] **expiry sweep 机制**：钉进 douyin-ideate——每次跑时对 idea 条目清扫：timeliness≥4 且入池超 7 天 → `expired`；深度题（timeliness≤2）可长留。验证：规则写入 skill 的 scoring/SOP，下次 ideate 自动执行。
- [ ] **现存 9 条 idea 出「留/杀」清单**：按上述规则逐条判，交人审后翻状态（不自动杀）。
- [ ] **跑一次 douyin-ideate 补新水**：池子 24 天没进料，恢复模式后先补一轮调研。
- [ ] **停产/异常事件飞书通知**：daily-run 空跑、取题失败、配音余额 1008 这类阻塞事件，经 feishu-notify 推人，不再只写本地日志（06-30~07-05 连喊 6 天没人看见的教训）。验证：模拟一次空跑，飞书收到卡。

## 阶段 3 · 目录重组（AI，纯搬运 + 全仓改引用，需在阶段 1 之后）

- [ ] **automation/ 解体**：`daily-run.md` + `logs/` 迁入 `pipeline/`；`harness/` 提到顶层；`schedule.md` 内容并入 daily-run 参数节后删除（拟定时间表已与实际不符，三个"待确认"从未填）。
- [ ] **tts.config.json → brain/**（账号的声音=账号资产），同步改 `pipeline/2-create.md` 中 `../tts.config.json` 相对路径引用。
- [ ] **`content/_research/作品列表.xlsx` → `harness/data/`**（复盘数据属治理线，不是选题调研）。
- [ ] **tools/ 分家**：`social-auto-upload`（第三方 295M）外置或 submodule；`feishu-bot`（自研）留下、venv 进 gitignore。
- [ ] **全仓改引用**：grep `automation/harness`、`automation/daily-run`、`tools/social-auto-upload` 等旧路径，改 CLAUDE.md / dashboard / harness README / 各 skill / Claude memory。
- 验证：grep 旧路径零命中；daily-run、douyin-retro、douyin-publish 各干跑一次路径不报错。

## 阶段 4 · 文档瘦身与防漂移（AI 改写，内容变化大，单独人审）

- [ ] **dashboard.md 降级为派生视图**：只留状态表（从各 meta.yaml 生成）+ 数据汇总；系列叙事长段落迁 logs；已发布条目移出"在制"。目标：dashboard 不再是第三份手工状态源。
- [ ] **pipeline/*.md 瘦身为阶段契约**：只写输入/输出/状态翻转/闸口清单；tts-dub、baoyu-image-gen 等具体命令下沉对应 skill；EP02/EP03 踩坑教训收编到单一位置（建议 `pipeline/lessons.md`）。此约定（"SOP 是契约、细节住 skill"）写进 CLAUDE.md 防复发——治理线 `harness/retro.md` ↔ `douyin-retro` 同一模式一并处理。
- [ ] **`content/_template/` 改成真实 schema**：区分档案层（2-script/meta/4-publish/封面）与工作区（build/demo，gitignore）；砍掉或标注系列模式不产的 1-brief/3-review/5-retro。
- [ ] **docs/ 定位只读 ADR 存档**：harness README 等处引用 docs 里的"现行规则"（如 TODO 表说明）收编回 harness 本体；docs 只存历史快照。
- [ ] **状态记账单一 owner**：backlog→published 只由 douyin-publish 发布收尾翻动（现状是散落义务，已两次漏翻靠 daily-run 补）；三处重复的记账约定文本收敛到一处、其余引用。

## 依赖与顺序

阶段 1 → 阶段 2（业务最急，文件改动小）→ 阶段 3（搬运，需 git 兜底）→ 阶段 4（改写，逐项人审）。
阶段 0 与其余并行，越早越好（EP04 是系列唯一未闭环）。
