# 工作日自动跑 · 2026-06-18

模式：清系列日更（EP02→EP15 顺序）。今日产出 1 期，跑到「出审」停。

## 取题（确定性出队）
- 队首 = claude-code-source-series 中 status:idea、episode 最小 → **EP03 `2026-06-10-003`「你每月给 Claude 充的钱，一半花在它"记性太好"上」**（token 经济学）。
- promote：backlog `idea→picked`，回填 `content_path: content/2026-06-18/ep03-token-economics`。建目录、`meta.yaml`（source=2026-06-10-003）、`1-brief.md`（plan_file 指针）。

## 创作（pipeline/2-create 口播链路）
- **核源码事实**（真相源 `…/claude-code-source/claude-code-main`，与 EP02 同仓；plan frontmatter 路径已过期）：逐条溯源全部命中——
  TF-IDF 权重 3.0/2.5/1.0（toolIndex.ts:34-36）｜自动压缩阈值 13k/30k/50k + 熔断3次（autoCompact.ts:62,79-81,99）｜压缩复用主会话缓存 + 实验「不复用≈98% miss」（compact.ts:455-457）｜缓存失效诊断 77% 是描述变更（promptCacheBreakDetection.ts:35-37）｜CORE_TOOLS 白名单约30项（tools.ts:137）｜穷鬼模式 /poor 跳过 extract_memories/prompt_suggestion/verification_agent（poorMode.ts + prompts.ts:372）。Prompt cache 1/10 = Anthropic 公开定价。
- `2-script.md`：冷开钩子（"越聊越贵"自成立，不依赖上文）→ 痛点（大模型无记忆）→ 三招（缓存/裁剪/压缩）→ 穷鬼模式彩蛋 → 结尾钩。
  - **结尾钩对齐实际下一集**：plan 原写"下集聊权限(EP05)"，但日更按 episode 升序下一集是 **EP04（Esc/abort）**，故承接钩改指 EP04。
- 网页：复用 EP02 `build/` Vite 模板（midnight-press 主题），重写 5 章 26 步（coldopen/cache/tools/compress/ending）+ 配套组件/CSS。
  - 踩坑：本机 npm registry 给 vite/rolldown/tsx 是「源码版」无预编译 dist → `npm install` 后 vite 跑不起来。改为整体复用 EP02 已验证的 `node_modules`（含预编译 dist），build/record 全通。
  - 截图校验 16 帧，关键帧（钩子/77%诊断/TF-IDF/穷鬼终端/CTA）排版干净、系列风格一致。
- 配音：`tts-dub synthesize.mjs`（账号音色 moss_audio_4dd8142e，speed1.1），26 段全成。死气扫描 13 段 >0.45s 内部静音 → `depause.mjs`(cap0.25/minact0.45) 全压平，复扫 0 段。
- **配音审批闸（dubbing-reviewer subagent）**：R1 **PASS**（完整性/真相源同步/零死气/语速无真离群/念法 normalize 覆盖到位）。非阻断建议已记录。
- 录屏：headless `record-manual.cjs`（2080×1280→crop1920×1080）+ `mux.sh`（setpts 去拉伸，factor≈0.99）→ `assets/ep03-token-economics.mp4`。
- 封面：`baoyu-image-gen`（gemini-3-pro-image，9:16，--ref EP01 cover）→ `assets/cover.png`。复用 EP01 方块像素吉祥物本体，省钱/护币姿态，标题「你充的钱 / 一半喂了它的『记性』」。

## 出审前 · 成片终检闸 A–F（全在最终 mp4 上验）
- A 声画一致 ✓（抽 cache/5、ending/1、compress/3：画面==字幕==口播；audio-segments==narrations）
- B 音频自然度 ✓（段内死气 0 段 >0.45s；段间 0.2s gap 为正常转场）
- C 钩子冷启动 ✓（首句自成立）
- D 事实/合规 ✓（全溯源、无臆造；persona 口语无论文腔；无导流/绝对化）
  - **合规返工**：narration+画面「最骚的」→「最妙的」（避敏感词 骚）。按铁律重抽→删改段→重合成 compress/3→重建 master→**重录+重混**，重录后 factor≈0.99、改段画面/字幕已同步为「最妙」。
- E 封面 ✓（9:16 竖版、EP01 吉祥物本体、标题承担钩子）
- F 音画同步 ✓（factor 0.99，首/中/尾抽帧对齐）

## 出审
- `meta.yaml` status=`review`，timestamps.review=2026-06-18；`dashboard.md` 已更。
- 经 `feishu-notify` 推审核卡（口播稿+封面）到飞书等人审。
- **停在此，绝不发布**（发布必人审 + dry-run 铁律）。

## 产出物
- `content/2026-06-18/ep03-token-economics/`：`2-script.md`、`assets/ep03-token-economics.mp4`(1920×1080,164.0s,~12.8MB)、`assets/cover.png`(9:16)、`assets/ep03.srt`、`meta.yaml`。

---

# 工作日自动跑（二轮·同日）· 2026-06-18 · EP04

> EP03 当日早些已发布(19:05)；本轮是定时 agent 第二次跑，产 **EP04** 到「出审」停。模式同：清系列日更。

## 取题（确定性出队）
- 队首 = claude-code-source-series 中 status:idea、episode 最小 → **EP04 `2026-06-10-004`「你按下 Esc 的那 0.1 秒，Claude Code 内部发生了什么」**（中断/abort 链路）。EP01–03 已发。
- promote：backlog `idea→picked`，回填 `content_path: content/2026-06-18/ep04-esc-abort-chain`。建目录、`meta.yaml`(source=2026-06-10-004)、`1-brief.md`(plan_file 副本)。

## 创作（pipeline/2-create 口播链路）
- **核源码事实**（真相源 `…/claude-code-source/claude-code-main`，与 EP02/03 同仓）：三机制逐条溯源全部命中——
  中止树/父子单向传播/reason 下传（abortController.ts:67-99，注释「Aborting the child does NOT affect the parent」）｜interruptBehavior():'cancel'|'block'、**默认 block**（Tool.ts:435-437）｜软打断只砍 cancel 工具（StreamingToolExecutor.ts getAbortReason:237-255，getToolInterruptBehavior 默认 block:260-266）｜**全仓唯一声明 cancel 的工具 = SleepTool**（SleepTool.ts:78，grep 仅此一处）｜硬 Esc=全停（useCancelRequest.ts handleCancel Priority1，source 'escape'）｜WeakRef 双向弱引用+子 abort 自动摘监听器{once:true}（abortController.ts:84-97）｜50 监听器上限（DEFAULT_MAX_LISTENERS=50:6）。
  - **★ 据实纠偏 plan**：plan 大纲把机制二写成「读文件 cancel / 写文件 block」——**非源码事实**（默认 block、仅 SleepTool opt-in cancel、且 cancel/block 只作用于软打断）。脚本改述为「默认 block＋仅无损工具(Sleep)声明 cancel」，保留「不是所有任务都能立刻停」的精神，不臆造工具配置。红线：不编造。
- `2-script.md`：冷开钩子（「你按一下 Esc…文件一个都没被写坏」自成立、不依赖上文）→ 场景(七八任务并行)→ 中止树 → 两种打断(cancel/block) → WeakRef 防泄漏 → 升华+结尾钩+互动。
  - **结尾钩对齐实际下一集**：日更按 episode 升序，下一集是 **EP05（权限管线「凭什么敢让 AI 在你电脑上跑命令」）**，非 plan 随手写的并发(EP06)。承接钩据实改指 EP05。
- 网页：**整目录复用 EP03 `build/`（含已验证 node_modules，不跑 npm install——pitfall#1）**，重写 5 章 24 步（coldopen/tree/interrupt/weakref/ending）+ 配套组件/CSS（midnight-press 主题）。tsc+vite build 一次过(48 模块)。
  - 踩坑：`cut` 变体 SVG 类名 `tr-cut` 与剪刀组 `.tr-cut{opacity:0}` 撞车，整棵树被隐藏 → 剪刀组改名 `tr-scissors` 修复。
  - 截图校验 24 帧，关键帧（Esc 钩子/剪树/红波下传/单向对比/cancel-block 列表/僵尸监听器/WeakRef/EP05 预告）排版干净、系列风格一致。
- 配音：build 内 `synthesize-audio.sh`→`minimax-t2a.mjs`（直调 T2A HTTP，账号音色 moss_audio_4dd8142e，speed1.1），24 段全成。英文术语 normalize：WeakRef→「weak ref」、Esc→「E S C」（只改配音、字幕保原文）。
  - 死气：silencedetect 扫 → `depause.mjs`(cap0.25/minact0.45/-38dB) 压平 18 段；两段边缘(0.45/0.468s)二次 minact0.40 压平，复扫 **0 段** >0.45s 内部死气。
- **配音审批闸（dubbing-reviewer subagent）**：R1 **PASS**（6 检查点：完整性/真相源同步/语速无真离群/多音字念法/零死气/音画文本一致）。NON-BLOCKING(tree/5 略快6.53音节/s)已采纳 → segment-overrides speed0.97 重合成。
- 录屏：headless `record-manual.cjs`(2080×1280，ArrowRight 确定性推进+校验补按) → webm；`build-master.cjs` 出 master.wav(211.16s，与调度 diff 0)；`mux.sh` setpts 去拉伸(factor 1.008)+crop1920×1080+混音 → `assets/ep04-esc-abort-chain.mp4`(1920×1080,211.2s,~17.9MB,h264+aac,mean -21.1dB)。
- 封面：`baoyu-image-gen`(gemini-3-pro-image,9:16,--ref EP01 cover) → `assets/cover.png`(1536×2752)。复用 EP01 方块像素吉祥物本体，按 Esc 键 + 背后中止树熄灭，标题「按下 Esc 的那 0.1 秒 / 内部到底发生了什么」。

## 出审前 · 成片终检闸 A–F（全在最终 mp4 上验）
- A 声画一致 ✓（抽 5 点：钩子/剪树/cancel-block/WeakRef/EP05 钩，画面==字幕==口播；audio-segments==narrations）
- B 音频自然度 ✓（段内死气 0 段 >0.45s；段间 0.2s gap 正常转场）
- C 钩子冷启动 ✓（首句自成立、不依赖上文）
- D 事实/合规 ✓（三机制全溯源、纠正 plan 非源码说法、无臆造；persona 口语无论文腔；无导流/绝对化营销词，「第一层/最服气」为口语非违禁）
- E 封面 ✓（9:16 竖版、EP01 吉祥物本体、标题承担钩子）
- F 音画同步 ✓（factor 1.008，5 点抽帧对齐）

## 出审
- `meta.yaml` status=`review`，timestamps.review=2026-06-18；`dashboard.md` 已更；`4-publish.md` 已产（标题/简介/话题3-5个/封面/媒体，字段键名匹配 feishu meta.py 解析器；不写可解析「建议发布时段」）。
- 经 `feishu-notify` 推审核卡（口播稿+封面）到飞书等人审。
- **停在此，绝不发布**（发布必人审 + dry-run 铁律）。

## 产出物
- `content/2026-06-18/ep04-esc-abort-chain/`：`2-script.md`、`assets/ep04-esc-abort-chain.mp4`(1920×1080,211.2s,~17.9MB)、`assets/cover.png`(9:16)、`assets/ep04.srt`(24 cues)、`4-publish.md`、`meta.yaml`。
